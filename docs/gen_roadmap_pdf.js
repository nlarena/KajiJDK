const fs = require('fs');

// =============================================================
//  Generador de informe PDF — Roadmap del JDK en Rust
//  Estilo basado en los informes de IAM (portada + TOC + tablas
//  + header/footer con banda de color). Render: Chrome --print-to-pdf.
// =============================================================

const FECHA = new Date().toLocaleDateString("es-AR", { day: "2-digit", month: "long", year: "numeric" });
const AUTOR = "Esteban Nicolás Larena";

// ---- inline markdown-ish: `code`, **bold**, *italic* ----
function esc(s) {
  if (s === undefined || s === null) return "";
  return String(s).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
}
function inline(text) {
  let t = esc(text);
  t = t.replace(/`([^`]+)`/g, '<code>$1</code>');
  t = t.replace(/\*\*([^*]+)\*\*/g, '<strong>$1</strong>');
  t = t.replace(/\*([^*]+)\*/g, '<em>$1</em>');
  return t;
}
// Badge de horizonte: B=Base, A=Avanzado, C=Cumbre
function badge(code) {
  const map = {
    B: ['Base', 'b-base'],
    A: ['Avanzado', 'b-adv'],
    C: ['Cumbre', 'b-sum'],
  };
  const [label, cls] = map[code];
  return `<span class="badge ${cls}">${label}</span>`;
}
function tableRows(rows) {
  return rows.map(r => '<tr>' + r.map(c => `<td>${inline(c)}</td>`).join('') + '</tr>').join('\n');
}
function table(headers, rows) {
  const head = '<tr>' + headers.map(h => `<th>${inline(h)}</th>`).join('') + '</tr>';
  return `<table><thead>${head}</thead><tbody>${tableRows(rows)}</tbody></table>`;
}
// Tabla de hitos: cada hito = { id, name, hz, exito, items:[{hz,txt}] }
function hito(h) {
  const items = h.items.map(it => `<li>${badge(it.hz)} ${inline(it.txt)}</li>`).join('\n');
  return `
  <div class="hito">
    <h3><span class="hito-id">${esc(h.id)}</span> ${esc(h.name)} ${badge(h.hz)}</h3>
    <ul class="hito-items">${items}</ul>
    <p class="exito"><strong>✓ Éxito:</strong> ${inline(h.exito)}</p>
  </div>`;
}

// =============================================================
//  CONTENIDO
// =============================================================

const TBL_PILARES = [
  ["**JVM** (el *runtime*)", "Carga y **ejecuta** bytecode. Parser de `.class` → intérprete → objetos → heap → GC.", "Rust"],
  ["**Compilador** (`javac`)", "Convierte `.java` en bytecode `.class`: lexer → AST → análisis semántico → emisión.", "Rust"],
  ["**Bibliotecas** (`java.base`)", "`Object`, `String`, `System`, colecciones… escritas *en Java*, compiladas por el compilador propio.", "Java"],
];

const FASE_A = [
  { id: "A0", name: "Parsear el .class (≡ javap)  ✅ núcleo logrado", hz: "B",
    exito: "**logrado** — el volcado coincide **byte a byte** con `javap -v` (y brief) sobre 12 fixtures.",
    items: [
      { hz: "B", txt: "Lector de bytes (cursor big-endian) — el `Reader` (`u1`/`u2`/`u4`)" },
      { hz: "B", txt: "Constant pool (17 clases de entrada; 1-indexed, Long/Double = 2 slots)" },
      { hz: "B", txt: "Header: magic, versiones, flags, this/super/interfaces" },
      { hz: "B", txt: "fields, methods, attributes: `Code`, `LineNumberTable`, `SourceFile`, **`StackMapTable` (los 7 frame types)**" },
      { hz: "B", txt: "Desensamblado de bytecode (tabla de opcodes completa) con comentarios `// …` resueltos" },
      { hz: "B", txt: "Volcado `javap` brief y `-v` byte-idéntico; flags de visibilidad (`-public`/`-protected`/`-package`/`-p`)" },
      { hz: "A", txt: "*Pendiente (no esencial):* `Signature`, `BootstrapMethods`, `InnerClasses`, `Exceptions`, `ConstantValue`, anotaciones, `Record`, `NestHost/Members`, `LocalVariableTable`" },
    ] },
  { id: "A1", name: "Intérprete mínimo  ✅ logrado", hz: "B",
    exito: "**logrado** — ejecutar un método que sume dos enteros (`Add.add`).",
    items: [
      { hz: "B", txt: "*Frame*: pila de operandos + variables locales" },
      { hz: "B", txt: "Contador de programa (PC) y *loop* de despacho de opcodes" },
      { hz: "B", txt: "Opcodes: `iconst`, `iload`, `istore`, `iadd`, `ireturn`" },
      { hz: "B", txt: "Parseo de descriptores de método (`(II)I`)" },
    ] },
  { id: "A2", name: "Control de flujo y métodos  ✅ logrado", hz: "B",
    exito: "**logrado** — corren `factorial` recursivo (120) y `fibonacci` (55).",
    items: [
      { hz: "B", txt: "Saltos: `if_icmpgt`, `goto`, comparaciones" },
      { hz: "B", txt: "*Method area* (metadatos de clases cargadas)" },
      { hz: "B", txt: "`invokestatic` + pila de frames (llamadas anidadas)" },
    ] },
  { id: "A3", name: "Objetos y heap  ✅ logrado", hz: "B",
    exito: "**logrado** — herencia (`Dog extends Animal`) y dispatch dinámico verificados: `a.sound()` sobre una referencia `Animal`/`Speaker` corre `Dog.sound`.",
    items: [
      { hz: "B", txt: "Heap + allocator simple; arena de bytes (`Vec<u8>`) + `malloc` bump" },
      { hz: "B", txt: "*Layout* de objetos (campos, con herencia) y de clases (con *vtable*)" },
      { hz: "B", txt: "`new`, `getfield`/`putfield`" },
      { hz: "B", txt: "`invokevirtual`/`invokespecial`/`invokeinterface` + dispatch dinámico (vtable + itable)" },
      { hz: "B", txt: "Arrays (objetos y primitivos int-category, con ancho fiel)" },
    ] },
  { id: "A4", name: "Robustez del runtime  ✅ logrado", hz: "B",
    exito: "**logrado** — un `try`/`catch` atrapa una `Boom` (con unwinding entre frames y `finally`), y `Base`/`Derived` inicializan en orden super-primero.",
    items: [
      { hz: "B", txt: "Excepciones: `athrow` + tablas de excepción + *stack unwinding*; `finally`; excepciones **implícitas** (NPE, índice, cast, tamaño negativo)" },
      { hz: "B", txt: "*Linking*: resolución bajo demanda + **verificador** (type-checking con *StackMapTable*); errores de linkage (`NoClassDefFound`/`NoSuchMethod`)" },
      { hz: "B", txt: "Inicialización de clase (`<clinit>`, perezosa, super-primero)" },
      { hz: "B", txt: "Jerarquía de class loaders (bootstrap/app + delegación)" },
    ] },
  { id: "A5", name: "Nativos + GC  ✅ logrado", hz: "A",
    exito: "**logrado** — `System.out.println` imprime de verdad y el GC recolecta (y **compacta**) basura.",
    items: [
      { hz: "A", txt: "Puente a métodos nativos (I/O real) — `System.out.println` imprime de verdad" },
      { hz: "A", txt: "*Intrinsics* — `getClass`, `hashCode`, `Math`, `arraycopy`, `String`/`Class`…" },
      { hz: "A", txt: "GC mark & sweep — y más: **compactante**, free list con *coalescing*, política de fragmentación, 4 disparadores sobre *safepoint*, y **marcado transitivo** correcto" },
    ] },
  { id: "A6", name: "Cumbre del runtime  🚧 en progreso", hz: "C",
    exito: "**parcial** — **verificador JVMS-estricto**, **GC generacional**, el **set de opcodes completo** (`invokedynamic` incluido) e **hilos de SO con paralelismo real ensanchado** (GIL removido; cómputo + lecturas + asignación lock-free en `JVM_THREADS=os`, con el `unsafe` Miri-verificado) **y el JMM relajado** (campos no-volatile lock-free con `Relaxed`, `volatile` con `Acquire`/`Release`, Eden atómico) logrados; falta locks por-estructura finos y/o el JIT.",
    items: [
      { hz: "C", txt: "**✅ Verificador de bytecode JVMS-estricto** — verifica **con o sin `StackMapTable`** (inferencia por punto fijo como fallback, y *cross-check* de la tabla contra la inferencia); gate **estructural** (§4.9: targets, no caerse del final, `jsr`/`ret`, tabla de excepciones); tipos de locales estrictos + cota de `max_stack`; reglas de **objetos sin inicializar** (`<init>` una sola vez); **handlers de excepción** tipados; y **acceso/linkage** (regla `protected`, `count` de `invokeinterface`)" },
      { hz: "C", txt: "**✅ Sistema de tipos completo** — `int`/`long`/`double`/`float` ejecutados *y* verificados: cómputo, conversiones, comparaciones, división con excepción, categoría-2 (params/campos/estáticos/arrays/frames), y el **lattice de referencias** (covarianza de arrays + `join`/LUB)" },
      { hz: "C", txt: "**✅ GC generacional** — young (Eden + 2 *survivors*) con colector por **copia** (minor: evacúa/promueve por edad, estilo Cheney con *forwarding*), **write barrier + remembered set** para las raíces `old→young`, y Old con **mark-sweep-compact**; *minor* disparado al llenarse Eden, *major*/full por occupancy o explícito" },
      { hz: "C", txt: "**✅ Referencias débiles** (`java.lang.ref`) — `WeakReference` + `ReferenceQueue`: el major trata `referent` como débil y, al morir el referente, lo limpia (`get()→null`) y **encola** la referencia. Base para `PhantomReference`/`Cleaner` (post-mortem)" },
      { hz: "C", txt: "**Hilos / concurrencia** (*en progreso*) — objetivo: **multithread profesional**, por fases. **✅ Fase 0 — green threads**: `java.lang.Thread` + `start`/`join`/`sleep`, scheduler cooperativo round-robin en el safepoint, varios call stacks por-thread, GC sobre las raíces de **todos** los threads, terminación; verificado (`Threads.run → 100`) y visible en el step-visualizer. **✅ Monitores**: `synchronized` de **bloques y métodos** (`ACC_SYNCHRONIZED`, sin opcode — el VM toma/suelta el monitor al entrar/salir del frame), reentrancia, señalización `wait`/`notify`/`notifyAll` sobre *wait-set*/*blocked-set*, `IllegalMonitorStateException`, `wait(timeout)` y monitor ***GC-safe*** (las claves se remapean por el *forward* del GC, así que se compacta con monitores tomados). **✅ Substrato de SO + GIL (E1+E2)**: `JVM_THREADS=os-gil` corre cada `Thread.start()` en un `std::thread` real tras un **GIL** (`Arc<Mutex<SharedVm>>`, un opcode por lock), con `park`/`unpark` reales y `sleep` en tiempo real — correcto pero **serializado** (referencia/oráculo). **✅ API de `Thread` (H1)**: `currentThread`/`yield`/nombre/id/`isAlive`, `getState()` con los seis estados de `Thread.State`, `new Thread(runnable)`, `start` dos veces → `IllegalThreadStateException`, e `interrupt()` que despierta `sleep`/`join`/`wait`. **✅ Sacar el GIL (E3/H3 — paralelismo real, subconjunto conservador)**: `JVM_THREADS=os` corre los opcodes **frame-local** (aritmética int/stack/ramas) **lock-free en paralelo** sobre un `RunningCtx` por-hilo con code cache; solo `SharedVm` se lockea; el GC que mueve objetos frena a todos con un **safepoint stop-the-world cooperativo**. El `Arc<Mutex<JVM>>` de toda-la-VM desapareció. **✅ Ensanchado**: (W1) frame-local completo (int/long/float/double, shifts, conversiones, refs, ramas); (W3) lecturas compartidas concurrentes bajo `RwLock` (`getfield`/`arraylength`/array-loads); (W2) **asignación lock-free** (`new`/`newarray`, Eden en un arena `UnsafeCell` **verificado con Miri**). **✅ JMM (H4 — modelo de memoria relajado)**: campos no-volatile **lock-free** (`Relaxed` atómico), `volatile` con `Acquire`/`Release` (`AtomicU64` sin *tearing* para `long`/`double`), Eden sobre un substrato atómico 8-alineado **Miri-verificado**; test de publicación `volatile` que coincide `green≡os-gil≡os`. **✅ CAS (H5)**: `compareAndSwap` intrínseco (`cas_u32`/`cas_u64` sobre el `AtomicRegion`) + `AtomicInteger`/`AtomicLong`/`AtomicReference` (la CAS de referencia pasa por el write barrier). **✅ Núcleo + primeras utilidades de `java.util.concurrent` (H6)**: **AQS** (`AbstractQueuedSynchronizer`) en modo **exclusivo y compartido**, con `ReentrantLock`/`Condition`, `ReentrantReadWriteLock`, `Semaphore`, `CountDownLatch`, `CyclicBarrier`, `ArrayBlockingQueue`/`LinkedBlockingQueue`/`PriorityBlockingQueue`/`DelayQueue`, el **framework `Executor`** (`ThreadPoolExecutor` + `ScheduledThreadPoolExecutor` + `submit`/`Future`/`FutureTask`/`Callable`), un **`ConcurrentHashMap`** (encadenamiento + *lock striping*) y un **`CompletableFuture`** (`supplyAsync`/`thenApply`/`thenCompose`, completación excepcional con `exceptionally`, y `thenCombine` que fusiona dos futuros con un `BiFunction`) —todo en Java sobre AQS (o el monitor), validado `green≡os-gil≡os`; motivaron `Object.equals`, `java.util.function`/`Objects`, `Comparator`/`Comparable`/`Delayed` y `System.nanoTime`—. **✅ Periféricos de `Thread`**: **`ThreadLocal`** (aislamiento por-hilo real — lista de asociación colgada del `Thread` actual, keyed por identidad GC-safe; verificado con 4 hilos OS), **prioridad** (get/set + rango `[1,10]`) y **daemon** (atributo + regla de ciclo de vida). **⬜ Falta**: el **volumen** restante de j.u.c (deques, *resize*/lecturas lock-free del `ConcurrentHashMap`), locks por-estructura finos y `UncaughtExceptionHandler`; y un **bug residual** conocido — una referencia *stale* puede alcanzar un frame bajo **GC intenso concurrente** en `JVM_THREADS=os` (guard parcial landeado; próximo paso ThreadSanitizer; `green`/`os-gil`, el oráculo, **no** afectados). El paralelismo de cómputo del LLM vive en **kernels off-heap** (rayon/CUDA), no en el heap Java. *La hoja de ruta completa (JMM, CAS, `java.util.concurrent`) tiene su propia sección («Concurrencia»).*" },
      { hz: "C", txt: "**✅ Cobertura del set de opcodes — 199/199 alcanzables: completo** — cerrados `nop` (0x00), `goto_w` (0xc8), el prefijo **`wide`** (0xc4: índice de local de 16 bits, la única forma de direccionar los slots pasados el 255; `wide iinc` mide 6 bytes, el resto 4 — salió barato porque los handlers de locales ya reciben `slot: usize` y nunca se enteran del ancho, así que ensanchar es puro *decoding*) y **`multianewarray`** (0xc5: aloca recursivamente **sólo** los `dimensions` niveles indicados —`new int[3][]` deja los internos en `null`—, validando todos los conteos *antes* de alocar nada; los niveles superiores guardan referencias y sólo el más interno usa el ancho real del elemento, y las referencias hijo pasan por `store_reference` para que el *remembered set* vea los punteros `old→young`. Hubo que **modelarlo también en el verificador**, sin lo cual la clase corría con el verificador claudicando en silencio). Los 3 de `jsr`/`ret`/`jsr_w` quedan **excluidos por diseño** —**JVMS §4.9.1** los prohíbe en class files de versión 50.0+ (Java 6 en adelante)— con la postura **leer sí, ejecutar no**: el desensamblador los soporta completo (requisito de A0, que se mide contra `javap` sobre `java.base`) y el gate estructural del verificador los rechaza. Nota de diseño: `subrutinas-jsr-ret.md`. El número honesto es entonces **199 de 199 alcanzables: completo**" },
      { hz: "C", txt: "**✅ `invokedynamic`** (0xba) — **no era un opcode, era un subsistema**, y corre: **5 de las 6 fábricas** que emite `javac`. **`StringConcatFactory`** (todo `\"a\" + b` desde Java 9), **`SwitchBootstraps.typeSwitch`** (`switch` sobre patrones de tipo *y* de enum), **`ObjectMethods`** (`equals`/`hashCode`/`toString` de un `record`, los tres desde una sola entrada de `BootstrapMethods`, distinguidos por el nombre del call site), **`LambdaMetafactory.metafactory`** (lambdas y method references) y **`ConstantBootstraps.invoke`** (constantes dinámicas). La sexta, `altMetafactory`, necesita serialización y queda **fuera de alcance**. **`LambdaMetafactory` genera una clase real** al vuelo (con el escritor de `.class`): implementa la interfaz funcional, con campos para las capturas y un método SAM que reenvía al handle — igual que el JDK (que spinnea con ASM), ya sin el atajo del objeto sintético. Y con el **modelo de objetos de `java.lang.invoke`** cerrado (abajo), `ConstantBootstraps.invoke` **se movió a Java** (es sólo `handle.invokeWithArguments(args)`); los otros *bootstrap methods* siguen como intrínsecos, con `java.lang.constant` en Java. Ruta completa, correcciones y mediciones: **`invokedynamic-ruta.md`**" },
      { hz: "A", txt: "**✅ `ldc` de literales de clase** (`Foo.class`, `int[].class`) — empuja el mirror `Class<…>`, cacheado por Class ID (así `Foo.class == Foo.class`) y **sin inicializar** la clase: un literal no es *uso activo* (§5.5). Los de array reusan el mirror sintético que arma `anewarray`, ya que una clase de array no tiene `.class` que cargar" },
      { hz: "C", txt: "**✅ La VM puede invocar Java** (`call_java`) — empuja un frame propio y lo corre con un bucle de `run_one` anidado, devolviendo el resultado. Resultó ser el **caso general de lo que la inicialización de clases ya hacía**: `<clinit>` era la variante sin argumentos ni resultado. Importa porque **los intrínsecos dejan de ser terminales**: un nativo que sólo puede calcular y devolver tiene que reimplementar lo que necesite de la biblioteca. Con esto, `String.valueOf(Object)` llama al `toString()` del objeto, un `record` pregunta el `equals`/`hashCode` de sus componentes, y un condy ejecuta su bootstrap" },
      { hz: "C", txt: "**✅ Modelo de objetos de `java.lang.invoke`** (`MethodHandle`/`MethodType`/`Lookup`) — **cierra 0xba**. Las clases viven en **Java** (`bootstrap/java/lang/invoke/`), con `MethodHandle.invoke`/`invokeWithArguments` **nativos** — polimorfia de firma (§2.9.3), interceptados en `invokevirtual` *antes* de la resolución normal: el único intrínseco que de verdad corresponde. **El premio se cobró:** `ConstantBootstraps.invoke` es ahora **Java** (`return handle.invokeWithArguments(args)`), lo que exigió hacer el **cache de condy raíz de GC** (se remapea con el *forward* del colector). `ldc` de `MethodHandle`/`MethodType` —que `javac` **nunca** emite— se prueba con **class files hechos a mano por el escritor de `.class`** (su estreno). Kinds: static/virtual/special/**constructor** + **mirrors de primitivos** (`int.class` → `Integer.TYPE`, sin boxing). Y **`LambdaMetafactory` genera clases reales** al vuelo (vía el escritor de `.class`), implementando la interfaz funcional y reenviando al handle — como el JDK, ya sin el atajo del objeto sintético" },
      { hz: "C", txt: "JIT (bytecode → código nativo)" },
    ] },
  { id: "A7", name: "Conformidad JVMS  ✅ auditoría 2026-08-12 — 13/13 cerrados", hz: "C",
    exito: "**auditoría CERRADA, 13/13** (2026-08-12) — el runtime contra la JVM Specification y los JSR estructurales (detalle: `holdon.md`). Los 8 hallazgos gruesos (default methods, uncaught exceptions, `ArrayStoreException`, `StackOverflowError`, `OutOfMemoryError`, autoboxing, `clone`, `Class.getName`) y los 5 menores (anotaciones runtime, Soft/Phantom refs, `System.exit`/`Runtime`, periféricos de `Thread`, init de interfaces), **cada uno validado por el oráculo** (`green≡os-gil≡os` + 10× os-parallel). Tres resultaron **bugs reales** que nadie había visto: los mirrors de clases array vivían en Eden, `anewarray` nombraba mal los arrays anidados, y el `<clinit>` de una interface con default methods no corría nunca. De paso apareció un 2º reproducer del heisenbug os-parallel. Lo único que queda es lo marcado *fuera de alcance* con su razón (`getAnnotation` sin dynamic proxies, `Cleaner`, shutdown hooks, `finalize()`).",
    items: [
      { hz: "C", txt: "**✅ Default methods de interfaces (JSR 335, Java 8)** — estaba **roto** (`NoSuchMethodError`: la vtable heredaba sólo de la superclase). Ahora fusiona los defaults de las superinterfaces (BFS desde las directas = regla *maximally-specific* para todo lo que emite javac; la clase siempre gana; un método abstracto —sin `Code`— nunca toma slot). Cubre `invokeinterface` **e** `invokevirtual`. Los static de interface ya funcionaban. Test → 115 (`green≡os-gil≡os`)" },
      { hz: "C", txt: "**✅ `Throwable` completo** — `getMessage`/`toString` (nativo: lee el nombre de clase runtime) + **stack traces**: la VM captura la traza (`\\tat pkg.Class.method`, innermost-first) en el *unwind* —punto único de faults implícitos y `throw`— y la guarda en el objeto (interning GC-safe); `printStackTrace()` la imprime. Constructores `(String)` en toda la jerarquía" },
      { hz: "C", txt: "**✅ `ExceptionInInitializerError`** (JVMS §5.5) — un `<clinit>` que lanza **propagaba… nada**: se tragaba el fallo y el `getstatic` devolvía 0. Ahora propaga al código que disparó la init (un *floor* de unwinding por `call_java` + `pending_exception`), envuelve si no es `Error`, y deja la clase **erroneous** → `NoClassDefFoundError` al reuso" },
      { hz: "C", txt: "**✅ Uncaught exception** (§2.10) — estaba **roto**: una excepción sin catch hacía `panic!` de **toda la VM**. Ahora imprime el formato exacto del JDK (`Exception in thread \"Thread-1\" java.lang.RuntimeException: …` + la traza capturada en el throw) y termina **solo ese thread** — los joiners despiertan, `main` sigue; si es `main`, termina el programa. Test → 42 (`green≡os-gil≡os`). Falta la API `setUncaughtExceptionHandler`" },
      { hz: "C", txt: "**✅ `ArrayStoreException`** (§6.5) — `aastore` hace el chequeo dinámico (clase runtime del valor vs tipo de elemento, `is_subtype` con covarianza; null siempre válido). Test → 42. **Bonus: 2 bugs latentes expuestos y arreglados** — los mirrors de clases array se alocaban en **Eden** (el minor GC los movía → índice/headers colgados → ASE espurio en stores válidos; ahora **Old-pinned** como todo mirror) y `anewarray` nombraba `[L[J;` en vez de `[[J` (intérprete + verificador). Regresión → 64" },
      { hz: "C", txt: "**✅ `StackOverflowError`** (§6.3) — `MAX_FRAMES = 2000` en `push_frame_locked` (el embudo de los 5 invokes), chequeado **antes** de adquirir el monitor (no filtra locks de `synchronized`); `call_java` cubierto vía `pending_exception`. Test → 42" },
      { hz: "C", txt: "**✅ `OutOfMemoryError`** (§6.3) — agotamiento **recuperable para las alocaciones de bytecode** (`new`/`newarray`/`anewarray`/`multianewarray` → `try_malloc` → throw); las internas del VM conservan el panic, documentado. Test → 42 (recursión rooteando 512 KiB/frame agota los 16 MiB reales)" },
      { hz: "C", txt: "**✅ Autoboxing / wrappers** (JLS §5.1.7) — los 8 wrappers reales: `Integer`/`Long`/`Short`/`Byte` con `valueOf` + **cache −128..127** (identidad `==` verificada: `100==100` true / `200==200` false), `Character` (0..127), `Boolean` (TRUE/FALSE canónicos), `Double`/`Float`. Test → 42. Colateral: destapó un **2º reproducer del heisenbug os-parallel** (single-thread ~50%, `Long.<clinit>` + Eden lleno → `ArithmeticException` espuria; `java/BxDbg{T,Y}.java`)" },
      { hz: "C", txt: "**✅ `Object.clone()` + `Cloneable`** (JLS §10.7) — copia shallow interceptada en `invokevirtual` e `invokespecial` (`super.clone()`), `Cloneable` → `CloneNotSupportedException`; **arrays incluidos** (`\"[I\".clone()` pre-vtable). Clon alocado en **Old** (una alocación Eden podría mover el fuente a mitad de copia); refs vía write barrier. Test → 42" },
      { hz: "C", txt: "**✅ `Class.getName()`/`getSimpleName()`** — nativos sobre el mirror, formato JDK completo (clases con puntos; arrays `[Ljava.lang.String;` / `String[]`/`int[]`). Test → 42 (incl. `String.class.getName()` vía ldc de Class)" },
      { hz: "B", txt: "**✅ Anotaciones en runtime** (JSR 175) — `Class.isAnnotationPresent(Class)` sobre el `RuntimeVisibleAnnotations` ya parseado. *Fuera de alcance:* `getAnnotation()` devolvería un **objeto** anotación → hacen falta clases proxy por `@interface` (dynamic proxies + modelo de `Method`)" },
      { hz: "B", txt: "**✅ `java.lang.ref`: `Soft` + `PhantomReference`** — phantom con `get()`→`null` pero `referent` intacto (el GC encola igual; el override impide resucitar). La política soft vive en el **GC**: el referent se traza **fuerte** salvo en una colecta pedida por **presión** de memoria, donde muere como un weak. Verificado en ambas direcciones (42 normal / 36 bajo presión). *Fuera de alcance:* `Cleaner`; `finalize()` no se implementa por decisión" },
      { hz: "B", txt: "**✅ Ciclo de vida: `System.exit` + `Runtime`** — interceptado en `invokestatic` (el bridge de nativos solo sabe *continuar*; terminar exige un `Step`): descarta los frames **sin** liberar monitores ni desenrollar —el apagado ordenado que `exit` no debe hacer— y propaga el status desde cualquier thread. Contrastado con el `java` real. *Fuera de alcance:* shutdown hooks" },
      { hz: "B", txt: "**✅ Periféricos de `Thread`** — **`UncaughtExceptionHandler` invocado de verdad en Java**, y **antes** de popear el último frame: el lookup aloca y ese frame es la única raíz de GC que sostiene la excepción; un handler que lanza queda contenido por el `exception_floor`. Más `sleep(long,int)`, `onSpinWait()` y `ThreadGroup` (registro enteramente en Java)" },
      { hz: "B", txt: "**✅ Regla fina de init de interfaces** (§5.5) — **era un bug**: el `<clinit>` de una interface con default methods nunca corría (ni directo ni indirecto). Ahora un BFS junta las superinterfaces que declaran un método con `Code`, y **devuelve vacío si la clase es una interface** — la regla \"una interface no inicializa sus superinterfaces\" queda en el tipo de retorno, no en un `if`" },
      { hz: "B", txt: "**Cubierto ya auditado**: class file 199/199, verificador, JMM, indy 5/6, j.u.c núcleo, records/patterns. **Fuera de alcance por diseño**: módulos (JSR 376) y NIO en la VM, `finalize()`, y lo que depende de reflexión completa (Fase C)" },
    ] },
];

const FASE_B = [
  { id: "B0", name: "Lexer  ✅ logrado", hz: "B",
    exito: "**logrado** — tokeniza el juego **completo** de Java (las 115 `TokenKind` de JDK 25).",
    items: [
      { hz: "B", txt: "Scanner con **maximal munch**; comentarios; literales `int`/`long`/`float`/`double`/`char`/`String` (hex/bin/octal, **text blocks** `\"\"\"…\"\"\"` — el lexema crudo; el destripado §3.10.6 lo hace el parser)" },
      { hz: "B", txt: "`TokenKind` espejo **fiel** del enum de javac (115 constantes; las keywords contextuales `var`/`record`/`sealed`… van como identificador)" },
    ] },
  { id: "B1", name: "Parser  ✅ logrado (lenguaje completo, sin bordes)", hz: "B",
    exito: "**logrado** — AST por *recursive descent* de clases/miembros, sentencias y expresiones con toda la precedencia; **genéricos**, **lambdas**, **referencias a método**, **clases anónimas y locales**, **type witness** y **anotaciones** incluidos.",
    items: [
      { hz: "B", txt: "Gramática → AST. **Auditado contra el §14**: están **todas** las sentencias —incluida la **clase local** (§14.3)— bloque, declaración local (múltiple, `final`, y con declarador al estilo C `int y[]`), `;`, etiquetada, expresión, `if`, `assert`, `switch` (dos puntos y flecha, con patterns y guardas), `while`, `do`, `for` (básico/multi-init/infinito) y `for-each`, `break`/`continue` **con y sin etiqueta**, `return`, `throw`, `synchronized`, `try`/`catch`/`finally` con multi-catch y recursos, y `yield`. Más los **inicializadores** `static { }` y `{ }`, el **inicializador de array** `{1,2,3}` (§10.6) y el `instanceof` con **pattern**" },
      { hz: "B", txt: "*Precedence climbing* + *backtracking* (declaración local vs. expresión, *cast* vs. paréntesis, **cierre de genéricos** — parte `>>`/`>>>` con *undo log*)" },
      { hz: "B", txt: "**Genéricos**: argumentos de tipo (`List<String>`), *wildcards* (`? extends`/`? super`), parámetros con cotas (`<T extends A & B>`), el **diamante** `<>`" },
      { hz: "B", txt: "**Visualizador de AST** (árbol ASCII) + volcado de tokens en el binario `javac`; y **`javac-step`**: visor **paso a paso de las pasadas** (tokens → AST → tabla → chequeo)" },
      { hz: "B", txt: "**✅ Formas de expresión modernas** — cerradas esta iteración, todas parseadas y guardadas fielmente en el AST (compilarlas es de B2/B3, y hasta entonces la barrera del emisor las corta): **lambdas** (§15.27, con el desambiguador `(`-cast-vs-paréntesis-vs-parámetros por *lookahead* de la flecha), **referencias a método** (§15.13, incl. `T[]::new` por los corchetes vacíos), **clases anónimas** (§15.9.5, cuerpo de clase con nombre vacío), **clases locales** (§14.3) y el **type witness** (`Collections.<String>emptyList()`) —que además **se aplica**: fija los parámetros de tipo del método en vez de inferirlos, así un override deliberado incompatible ahora falla—" },
      { hz: "B", txt: "**✅ Text blocks** (§3.10.6) — el lexer toma el lexema crudo `\"\"\"…\"\"\"` y el parser **decodifica** el valor con el algoritmo del JLS **en orden**: (1) normaliza los terminadores de línea a `\\n` (CRLF/CR), (2) descarta la **línea de apertura** (tras `\"\"\"` solo puede ir *white space* + un terminador), (3) quita la **indentación incidental** —el mínimo común de sangría de las líneas **no-blancas** y de la del **delimitador de cierre**— y los **blancos finales** de cada línea, y (4) **recién entonces** procesa los escapes, con los dos propios del text block: `\\s` (un espacio que **sobrevive** al destripado de finales, porque este corre antes) y la **continuación de línea** `\\<LF>` (una barra al final de línea se **traga** el salto). El resultado es un `String` común que el resto del pipeline emite como cualquier literal (`ldc`), verificado de punta a punta" },
      { hz: "B", txt: "**✅ Anotaciones** (§9.7) — antes se **descartaban** (`skip_annotation`); ahora `modifiers()` las lee junto a los modificadores y las **cuelga** de la declaración (clase, método, campo, parámetro). Se parsean enteras: marcador, valor único, pares con nombre, arreglos y anotaciones anidadas. Metadata, no bytecode: emitir el atributo `RuntimeVisibleAnnotations` es de B3. **Lección de la auditoría:** los inicializadores `static { }`/`{ }` también se *parseaban y se descartaban* —el código desaparecía sin que nada fallara—; hoy se conservan. *Parsear y tirar* es peor que no parsear, porque no deja rastro" },
      { hz: "A", txt: "**✅ Bordes cerrados** — el parser ya no tiene huecos: las anotaciones sobre un **parámetro de tipo** (`<@Foo T>`) y sobre una **constante de `enum`** ahora se **retienen** (`TypeParam.annotations`/`EnumConstant.annotations`), las **anotaciones de tipo/uso** (§9.7.4, `List<@NonNull String>`, `@Foo int`) se **aceptan** —se descartan por ser metadata ajena a la *erasure*— y el **type witness de constructor** (`new <T>Foo()`) ya **no da error**. Y se cerró la última: la anotación de **nivel de array** (`String @A []`, una por dimensión en `int @A [] @B []`) se **acepta y descarta** —tras la base, un `@` suelto solo puede ser de nivel de array—. **El parser queda sin bordes**" },
    ] },
  { id: "B2", name: "Análisis semántico  ✅ pasadas 1 y 2 + chequeos", hz: "B",
    exito: "**logrado (núcleo)** — acepta un programa bien tipado y rechaza uno con error de tipos/nombres, con **overload resolution** y **genéricos** reales; semántica **completa** de JDK 25 como norte, citada a la JLS 25.",
    items: [
      { hz: "B", txt: "**✅ Pasada 1 — Enter/MemberEnter**: tabla de símbolos completa — paquetes, tipos **anidados**, `enum`/`record`/`@interface`, **genéricos con cotas**; **class finder real** (lee `.class` del classpath, incl. el atributo **`Signature`** → jerarquía genérica del JDK); resolución + validación con errores **posicionados**; síntesis implícita; y el **grafo tipado persistido** como salida" },
      { hz: "B", txt: "**✅ Pasada 2 — Attribute**: resuelve nombres + tipa **los cuerpos**, **decorando el AST in situ** (tipo por nodo, *binding* local/campo/método, slot de cada local con categoría-2, **variables de patrón** de un `case T v` con su slot, y el **constructor resuelto** de cada `new` para que el codegen sepa qué descriptor emitir) — la salida que consume el codegen" },
      { hz: "A", txt: "**✅ Overload resolution** en **3 fases** (JLS §15.12.2: estricta → *boxing* → *varargs*), con *most specific* y detección de ambigüedad" },
      { hz: "A", txt: "**✅ Genéricos** (etapas A·B·C): subtipado con *containment* de wildcards (invariancia), *erasure*, sustitución del receptor (`List<String>.get` ⇒ `String`), `lub`/`glb`, e **inferencia** de los argumentos de tipo de una llamada (Cap. 18: reducción → incorporación → resolución)" },
      { hz: "A", txt: "**✅ Errores a nivel de expresión** — cada error lleva la línea:col del **nodo**, no del método (`int b = a + noSuchVar;` apunta a `noSuchVar`, no a la sentencia); documentado en `docs/pasada2-attribute.md` y `docs/semantica-jdk25.md`. **✅ Indulgencia con externos acotada** — antes, un miembro no hallado en un tipo del classpath se aceptaba en silencio. La raíz eran dos: no se cargaba la **cadena de supertipos** (un método heredado, `s.hashCode()` de `Object`, podía no aparecer), y los nombres del `.class` venían con puntos donde el *finder* esperaba barras (la superclase no cargaba). Cerrados los dos: se cargan los supertipos **transitivamente**, y la indulgencia quedó **acotada** — un **nombre inexistente** (`s.noExiste()`, `ArrayList.noExiste()`) ahora se reporta si la jerarquía está **completa**; sigue indulgente solo la **sobrecarga que no matchea** (nuestra resolución no cubre toda firma del JDK) y las jerarquías **incompletas** (classpath del JDK ausente → red de seguridad). Con esto **B2 no tiene deudas de rigor conocidas**" },
      { hz: "A", txt: "**✅ Chequeos semánticos** (esta iteración, en la pasada **Check** y en Attribute): **control de acceso** (§6.6 — `private`/`protected`/paquete sobre cada uso), **excepciones chequeadas** (§11.2 — toda checked que se lance tiene que estar capturada o en el `throws`; requirió **retener la cláusula `throws`**, que el parser descartaba, y guardarla en la tabla), **`this`/`super` en contexto estático** (§8.4.3.2), **reasignar un campo `final`** fuera de constructor/inicializador (§8.3.1.2) y el **acceso a tipo anidado cualificado** (`Outer.Inner.v()`, §6.5.5 — el único que *rechazaba código válido*). El chequeo de acceso y el de excepciones corren sobre el árbol **fuente**, antes del desugar, para no tropezar con el código sintético (el constructor del `enum` llama a `Enum.<init>`, que es `protected`)" },
    ] },
  { id: "B3", name: "Generación de bytecode  ✅ núcleo + StackMapTable + invokedynamic", hz: "B",
    exito: "**logrado** — el `javac` propio compila objetos, bucles y excepciones, y el `.class` **corre en tu JVM** (`fib(10)=55`, `fact(5)=120`, `new M(10); m.add(5); m.get()` → 15) **y pasa el verificador JVMS-estricto**, que hace *cross-check* de nuestra `StackMapTable` contra su propia inferencia.",
    items: [
      { hz: "B", txt: "**✅ Pipeline completo en `compile()`** — las cinco pasadas en el orden de javac: `Enter → Attribute → Flow → Desugar → (re-Attribute) → Codegen`. **Flow va antes que Desugar** (sus errores apuntan al código fuente, no al bajado) y se **re-atribuye después** (las reescrituras dejan nodos sin decorar y el emisor necesita tipo/binding/slot). Un programa con errores semánticos **no emite** `.class`" },
      { hz: "B", txt: "**✅ Constant pool** con deduplicación + escritor de `.class` (v69) propio: `Utf8`/`Class`/`NameAndType`/`Methodref`/`Fieldref`/`Integer`/**`String`**/**`Long`**/**`Double`**/**`Float`**, con el hueco que exige la JVMS tras cada `Long`/`Double` (ocupan **dos** entradas); sección de **campos** y atributos `Code`/`SourceFile`" },
      { hz: "B", txt: "**✅ Tipos anchos y `String`** — `lconst`/`dconst`/`fconst`, `ldc2_w` para constantes de categoría 2, `ldc` para `String`, y la **promoción numérica binaria** (§5.6.2) con los 12 opcodes `x2y` (`longA + 1` inserta el `i2l`), con los desplazamientos como excepción (§15.19)" },
      { hz: "B", txt: "**✅ Objetos** — `new`+`dup`+`invokespecial <init>`, `getfield`/`putfield`, `getstatic`/`putstatic`, `invokevirtual` con receptor, `checkcast` y los estrechamientos `i2b`/`i2c`/`i2s`; **asignación** a local y a campo; y el `super()` implícito de todo constructor (§8.8.7), sin el cual el `this` queda sin inicializar y el verificador rechaza el `putfield`" },
      { hz: "B", txt: "**✅ Flujo de control** — etiquetas con parcheo de saltos; `if`/`else`, `while`, `do-while`, `for`, `break`, `continue`; comparaciones por categoría (`if_icmp*`, `if_acmp*`, `lcmp`/`fcmpl`/`dcmpl` + `if*`) y `&&`/`||` compilados como **saltos** (cortocircuito real, no un booleano calculado)" },
      { hz: "B", txt: "**✅ Excepciones** — `try`/`catch` con su tabla, `throw`, y el `finally` **duplicado** en la salida normal y en un handler *catch-all* que re-lanza (§14.20.2): la v69 ya no acepta `jsr`/`ret`, así que duplicar es la única vía. (Esto reubicó el *inlining* del `finally`, que teníamos mal anotado como tarea del **desugar**: es del emisor)" },
      { hz: "A", txt: "**✅ `StackMapTable`** (§4.7.4) — el frame de cada destino de salto, como el **merge** de los estados que llegan ahí (un slot que no coincide en todos los caminos pasa a `Top`). Siempre `full_frame`: legal en cualquier posición y evita las formas comprimidas. Los *handlers* llevan `stack = [E]` y hay que **forzarles** el frame, porque no los alcanza ningún salto. La pila de operandos se lleva **tipada**, no como simple altura: es lo que el frame tiene que declarar, y sin eso un destino alcanzado con algo ya empujado (`f(a, b > 0 ? 1 : 2)`) declaraba la pila vacía. Eso incluye el tipo **`uninitialized`** (tag 8), que identifica un objeto recién creado por el **offset de su `new`**; corrido su `<init>`, todas sus apariciones —pila y locales— pasan al tipo definitivo (§4.10.2.4). Validada con el *cross-check* del verificador propio" },
      { hz: "B", txt: "**✅ Barrera de seguridad** — `generate()` devuelve `Result`: una construcción no soportada **falla con posición** en vez de emitir un `.class` a medias. Destapó dos agujeros graves que el propio desugar venía alimentando en silencio —**`instanceof`** (que produce todo *pattern switch*) y la **creación de arrays** (que produce todo *varargs*)—, que a partir de ahí se cerraron. Si un `for-each`/`assert`/`yield` llega al emisor, el mensaje dice que es un **bug del desugar**, no una limitación" },
      { hz: "B", txt: "**✅ Arrays e `instanceof`** — `newarray`/`anewarray`, `arraylength`, las familias `Xaload`/`Xastore` (donde `byte`/`char`/`short` llevan **su propio** opcode aunque compartan la categoría `int`), inicializadores (`new int[]{4,5,6}`) y escritura de elementos. Con esto **compilan y corren** las llamadas **varargs** y los ***pattern switch***, que el desugar ya venía generando" },
      { hz: "B", txt: "**✅ Ternario, `switch`, saltos etiquetados y `synchronized`** — el `? :` con sus dos ramas **promovidas al tipo del todo** (§15.25); el `switch` como `tableswitch` o `lookupswitch` según la **densidad** de las etiquetas (la heurística de javac), con el relleno de alineación y los desplazamientos de 4 bytes, preservando el *fall-through* de la forma de dos puntos y cortándolo en la de flecha; `break`/`continue` **con y sin etiqueta** sobre una pila de sentencias «de las que se puede salir» —un `switch` interpuesto captura un `break` pero **no** un `continue` (§14.16)— más el **bloque etiquetado**; y `synchronized` con `monitorexit` en **las dos** salidas, la normal y un handler *catch-all* que re-lanza, con el monitor copiado a un local por el desugar para no reevaluarlo. De yapa, el **`switch` sobre `String`** pasó a compilar de punta a punta: el desugar ya lo bajaba a dos switches sobre `int`, pero ninguno de los dos se podía emitir" },
      { hz: "B", txt: "**✅ Emisión multi-clase** — `generate()` emite **un `.class` por tipo** (top-level y anidados, recursivo), cada uno con **su** nombre (`Multi`, `Multi$Nested`, `Otra`, el `E$1` del `switch`-enum). Era la última fuga silenciosa: antes se escribía **una sola clase** con el nombre del archivo, y un segundo tipo se perdía sin aviso. De paso, el **`switch` sobre `enum` ahora corre** de punta a punta (su `$SwitchMap` vive en `E$1`, que recién ahora llega al disco)" },
      { hz: "A", txt: "**✅ `invokedynamic`** (lambdas, *method refs* y `record`) — un nodo **`Indy` genérico** (bootstrap + argumentos estáticos **tipados**: `MethodType`/`MethodHandle`/`Class`/`String`) que el desugar produce y el emisor serializa sin recomputar. Las **lambdas** se bajan con un **`LambdaToMethod`** propio: método sintético `lambda$…` con las capturas de cabecera (análisis *effectively-final*, reusando el de las lambdas), `this` capturado cuando el cuerpo lo usa (impl de **instancia**, `REF_invokeSpecial`; si no, estática, `REF_invokeStatic`), y el `invokedynamic` con `LambdaMetafactory.metafactory` de *bootstrap* (`MethodType` borrado/instanciado + el `MethodHandle` de la impl como argumentos estáticos). Las **referencias a método** cubren las **cuatro formas** del §15.13.1 (estático, instancia **ligado**/**no-ligado**, constructor) apuntando al método/constructor **real**, sin sintetizar nada. El `equals`/`hashCode`/`toString` de un **`record`** (§8.10.2) va por `ObjectMethods.bootstrap`, con la `Class`, la cadena de nombres de componentes y un `MethodHandle` `REF_getField` por componente. El pool ganó `MethodHandle`/`MethodType`/`InvokeDynamic` + el atributo **`BootstrapMethods`**, y el **verificador propio** el opcode `0xba`. La **ejecución** del call site vive en la JVM (KajiJDK); acá el oráculo es el verificador estricto" },
      { hz: "A", txt: "**✅ Clases internas, locales y anónimas** (§8.1.3/§14.3/§15.9.5) — captura del entorno **completa**, las tres formas **corren y verifican**. Una **interna de instancia** captura la instancia envolvente en un campo `final Outer this$0` (parámetro de cabecera + asignación en el constructor, `new Inner()` → `new Inner(this)`, y el acceso sin cualificar a un miembro del enclosing reescrito a `this$0.f`/`this$0.m()`, con la resolución por la cadena de `owner` sumada a Attribute). Una **local** (registrada por la pasada mutable `register_local_classes` —un *body-walk* que la renombra a única y le da binary `Outer$1L`—, tipada *in situ* con los locales del método visibles) captura además los locales *effectively-final* en campos `val$x` —parámetros de constructor que `new L(caps)` pasa— reusando el análisis de las lambdas; y combina `this$0`+`val$` en método de instancia. Una **anónima** (`new Base(){…}` o `new Runnable(){…}`) se baja a una **local sintética `Outer$N`** en un pase `hoist_anonymous` entre Enter y Attribute: recorre el mismo camino que la local (registro, tipado, levantado, captura), **extiende una clase o implementa una interfaz** (según el `ty` resuelto) con override y captura `val$`/`this$0` — las dos corren y verifican. **✅ Argumentos de super** (`new ArrayList(10){…}`): la anónima sintetiza en `$N` un constructor que los **reenvía** a `super(...)`. **✅ `Outer.this`** (§15.8.4): baja a la cadena de `this$0` (`this.this$0…`), desambiguando un campo **sombreado** (`Outer.this.f` vs. `this.f` van a clases distintas) y resolviendo el **anidamiento multinivel** subiendo por la cadena de `owner`. **✅ `outer.new Inner()`** (§15.9.2): el **calificador** se empuja como `this$0` en vez de `this` —válido incluso en contexto **estático**, donde no hay `this`—. **✅ Colisión de nombres** (§14.3): dos locales homónimas (`L` en dos bloques del mismo enclosing) ya no colisionan —antes la tabla clavea el tipo por nombre y la 2ª se **descartaba en silencio**—. Una pasada mutable **renombra** cada local a un nombre único por unidad (`L` → `1L`, `2L`…, binarios `Outer$1L`/`Outer$2L`) y **reescribe sus referencias** con **alcance léxico por bloque** (visible de la declaración al fin del bloque), dejando al resto del pipeline ver nombres únicos en un scope plano. Destapó de paso un bug del desugar —`has_this` no se restauraba al procesar el cuerpo de una local, y una segunda local en un método **estático** creía capturar `this$0`—, ya cerrado. **✅ Local-en-local** (§14.3): una local declarada dentro del método de otra local se registra como **tipo anidado suyo** (`Outer$1L1$2L2`) —la pasada de registro recorre el cuerpo de una local tomándola a ella como nuevo enclosing— y el desugar la baja recursivamente. **✅ Multinivel implícito** (§8.1.3): el acceso **sin cualificar** a un miembro de un enclosing lejano (una interna dos o más niveles adentro que lee un campo/llama un método del abuelo) encadena tantos `this$0` como niveles (`this.this$0.this$0.f`), construyendo la cadena de enclosings por `owner`. Con esto las clases internas quedan **sin colas**" },
      { hz: "A", txt: "**✅ Emisión de interfaces propias** — el `.class` de una `interface` sale con `ACC_INTERFACE`, la lista de super-interfaces y sus métodos **abstractos sin `Code`** (§4.6); el emisor ya no sintetiza constructor para ella. Se sumó **`invokeinterface`** (con `InterfaceMethodref`, tag 11, y su `count`) para las llamadas sobre un receptor de interfaz —antes se emitía `invokevirtual`, mal, así que también arregló las llamadas a `List`/`Comparator`—, y el *method ref* a método de interfaz (`REF_invokeInterface`) pasó a `InterfaceMethodref`. Con esto **las anónimas sobre una interfaz** (`new Runnable(){…}`, el caso más común) corren y verifican, y una **lambda contra una interfaz funcional propia** ya tipa y emite" },
      { hz: "A", txt: "**✅ Sueltos de emisión cerrados** — **`RuntimeVisibleAnnotations`** (§4.7.16): se emite el atributo en clase/campo/método para las anotaciones **retenidas en runtime** (los `@interface` del fuente con `@Retention(RUNTIME)` + las conocidas del JDK; `@Override`/`@SuppressWarnings`, que son `SOURCE`, no se emiten), con los `element_value` tagueados (String/primitivo/clase/enum/anidada/arreglo). **Plegado de constantes** (§15.28/§15.29): una `static final int` como etiqueta de `case` —o aritmética constante `case 1 + 4`, o una constante **cualificada** `case K.A` de otra clase que se **inlinea**— se pliega a un literal (mapa `campo → valor` a punto fijo, para constantes que refieren a otras). **Formas comprimidas del `StackMapTable`** (§4.7.4): cada frame se serializa en la forma más compacta según el anterior (`same`/`same_locals_1`/`append`/`chop`, con `full_frame` de respaldo) — puro ahorro de bytes, validado por el verificador" },
    ] },
  { id: "B4", name: "Compilador robusto  ✅ núcleo cerrado", hz: "A",
    exito: "**logrado** — compila programas con sobrecarga, herencia y flujo no trivial. Los siete frentes están cubiertos; quedan **colas menores** documentadas en cada ítem (`@Override` sin override y `throws` en el chequeo, el *target type* en posición de argumento, el `enum` con constantes parametrizadas) — ninguna bloquea el criterio.",
    items: [
      { hz: "A", txt: "**✅ *Overload resolution*** (hecho en B2) **y chequeo de *override*** — pasada **Check** propia (el `Check.java` de javac: bien-formación de las *declaraciones*, no de los usos). Cubre las cuatro reglas de §8.4.8 sobre firmas *override-equivalentes* (mismo nombre y mismos parámetros **borrados**, §8.4.2): no sobrescribir un `final`, no cruzar la frontera `static`/instancia, no **reducir** la visibilidad, y **retorno covariante** —idéntico para primitivos, subtipo para referencias—. Más §8.1.1.1: una clase concreta tiene que implementar todo lo `abstract` que hereda, con las jerarquías que tocan un tipo **externo exentas** (de esos solo conocemos los miembros que aparecieron en alguna firma, así que una ausencia no prueba nada). **✅ Y ahora** `@Override` sin override (§9.6.4.4) —se reporta un `@Override` que no sobrescribe ni implementa nada; el chequeo es **sound**: solo dispara cuando el único ancestro externo es `Object` (cuyo set de métodos conocemos) y el método no es uno de los suyos (`toString`/`equals`/…), y con un supertipo del JDK se calla— y el **`throws` más ancho** (§8.4.8.3) —una excepción **chequeada** que lanza un override tiene que ser subtipo de alguna del método sobrescrito—" },
      { hz: "A", txt: "**✅ Inferencia** desde los argumentos **y desde el *target type*** — la atribución pasó a tener modo *checking* (`attrib_expr_to`), no solo síntesis: el tipo que el contexto espera baja a la expresión y es lo único que puede instanciar una variable que **no aparece** en los argumentos (`Caja<String> c = fabricar();`). Vale también para el **diamante** (§15.9.3), que antes pasaba por indulgente —`new Caja<>()` daba un parametrizado con cero argumentos, que se compara con cualquier cosa sin comparar nada— y ahora infiere de verdad, del target y del constructor. La **incorporación** (§18.3) arbitra entre las dos fuentes: un target que contradice a los argumentos se descarta, para que el error lo reporte el chequeo de asignación y no una inferencia inventada. Contextos cubiertos: los tres de asignación (§5.2 — inicializador, `return`, `=`) y ahora el de **argumento de llamada** por el algoritmo de **dos fases** (§15.12.2.6): la fase 1 resuelve la sobrecarga con los tipos provisionales/lenientes de los argumentos, y la fase 2 —resuelta ya— re-atribuye cada argumento **poly** (lambda, *method ref*, diamante) con el tipo de su parámetro como *target*. Es lo único que tipa el cuerpo de una **lambda-argumento** (`call(x -> x + 1)`) —sin él quedaba `Unresolved` y el emisor no podía bajarla— e instancia un **diamante-argumento** (`take(new Box<>())` infiere `String`); el diamante sin target se deja **crudo** para que la aplicabilidad lo acepte unchecked. **✅ Y la desambiguación de sobrecargas por el argumento poly** (§15.12.2.5): un arg lambda se tipa `Unresolved` (indulgente), así que sin esto sería aplicable a **toda** sobrecarga con un parámetro funcional; se descartan las incompatibles con la **forma** de la lambda —aridad del SAM y valor-vs-`void`—, así `f(x -> x + 1)` elige `Function` sobre `Consumer` (antes daba ambiguo). Cola menor: la misma disambiguación para *method refs* (por aridad)" },
      { hz: "A", txt: "**✅ Análisis de flujo (Flow)** — **asignación definitiva** de locales (Cap. 16, con los flujos *when-true/when-false* de `&&`/`||`/`!`), las reglas de variables **`final`** (conjunto *definitely unassigned*: no reasignar, asignación única) y **alcanzabilidad** (§14.21: sentencias inalcanzables), con `break`/`continue` **etiquetados** (§14.15/§14.16: la pila de contextos lleva la etiqueta a la que salta cada `break`)" },
      { hz: "A", txt: "**✅ Desugar** — sentencias (`for-each` → `for`/`while`+`Iterator`, `try`-recursos → `try/finally`+`close()` con la **excepción del `close()` suprimida** vía `addSuppressed` (§14.20.3.1), `assert` → `if(!$assertionsDisabled && !cond) throw` con su campo sintético y el `<clinit>` que lo calcula de `C.class.desiredAssertionStatus()` (§14.10), **`++`/`--` en descarte** → `x += 1` (§15.14.2), **switch-expresión** en cola → switch-sentencia que asigna (§15.28, con `yield` desde bloques/bucles vía `break` **etiquetado** sobre el switch generado), **`switch` sobre `String`** → dos switches sobre `int` con `hashCode`+`equals` (§14.11), **`switch` sobre `enum`** → switch sobre un `$SwitchMap` sintético (`int[]` indexado por `ordinal()`, alojado en una clase anidada **`C$1`** y poblado en un `<clinit>` con `try/catch NoSuchFieldError`, **fiel a javac** §14.11), **`switch` con *patterns*** (incl. **deconstrucción de records**, que extrae cada componente por su *accessor* y es recursiva) → cadena de `instanceof` en un bloque **etiquetado** (§14.11.1: cada brazo es un `if` **suelto** —no `else if`— para que una **guarda que falla** caiga al `case` siguiente; el binding se materializa con un cast, y un selector `null` sin `case null` lanza **NPE**)) y expresiones (concat de `String` → `StringBuilder`, `x op= y` → `x = (T)(x op y)`, **varargs** → array en el call site). Habilitado por una **síntesis a nivel clase** nueva: `Member::StaticInit` (los bloques `static { }` ya no se descartan), tabla de símbolos **mutable** en el desugar, el `enum extends Enum` implícito y el **binding de variables de patrón** en Attribute/Flow. Además **materializa los miembros implícitos de un `record`** (§8.10.3: campos, constructor canónico y *accessors*, respetando lo declarado a mano) — sus símbolos ya venían de `enter`, pero sin cuerpo en el AST el `.class` los referenciaba **sin declararlos**, y la deconstrucción llamaba a un método inexistente. La síntesis de estos miembros (enum/record/assert) reencuentra el símbolo del tipo por su **FQN con paquete** (§7.1) —el desugar lo armaba sin el paquete, así que un `enum`/`record` en un **paquete nombrado** perdía **toda** su maquinaria (sin constantes/`values()`/`valueOf()`/`$VALUES`): bug #21, ya cerrado con test—. El *inlining* del `finally` resultó ser del **codegen**, no de acá. Queda como asimetría que varias de estas reescrituras producen construcciones que el emisor **todavía no soporta** (`instanceof`, `new int[]`): desde que hay barrera, eso **falla** en vez de salir roto. Aparte (no son desugar, van en su propia pasada): el **boxing** es *TransTypes* (✅, ítem aparte) y las lambdas/`invokedynamic` son LambdaToMethod" },
      { hz: "A", txt: "**✅ TransTypes — boxing/unboxing dirigido por tipo** (§5.1.7/§5.1.8) — el `TransTypes` de javac, acotado al *boxing* (la *erasure* de genéricos ya la resuelve el sistema de tipos por *erased ids*). Dos capas: (1) **Attribute lo acepta** — el contexto de asignación (§5.2) permite `int`→`Integer`/`Number`/`Object` (box + *widening* de referencia) y `Integer`→`int`/`long`/`double` (unbox + *widening* primitivo), sumado a la fase de boxing del *overload*; (2) una **pasada propia** (tras Flow, antes del desugar) que recorre el árbol tipado e **inserta la conversión** en cada sitio: primitivo donde se espera referencia → `Integer.valueOf(v)`, *wrapper* donde se espera primitivo → `v.intValue()`. Contextos: inicializador de `local`/campo, `return`, `=`, **argumentos** (con el tipo de parámetro resuelto — un genérico borrado boxea), **operandos** de aritmética/relacionales/lógicos (`i + 1` → `i.intValue() + 1`), índice de array, ramas del ternario, y el **cuerpo de una lambda** contra el retorno de su SAM. Las inserciones quedan sin decorar y las **materializa la re-atribución**; el emisor saca de ahí `invokestatic valueOf`/`invokevirtual xxxValue`. Cierra el caso insignia de las *poly expressions*: **`Function<Integer,Integer> f = x -> x + 1`** de punta a punta (el `int` del cuerpo se boxea al `Integer` del SAM), validado por el **verificador estricto** —ejecutar `valueOf`/`intValue` es de la JVM (KajiJDK), track aparte—. **✅ Y el *wrapper*→primitivo más ancho** (`long l = anInteger`): tras `intValue()` (int) se inserta un `Cast` al target, que el emisor baja a `i2l`/`i2d`. Cola menor: el `==`/`!=` con unboxing dirigido (se dejó por la identidad de referencia entre dos *wrappers*)" },
      { hz: "A", txt: "**✅ Miembros implícitos del `enum`** (§8.9.3) — constantes como campos, `$VALUES`, el constructor privado `(String, int)` y `values()`/`valueOf()`. Destrabarlo pidió cerrar antes la **invocación explícita de constructor** (`super(...)`/`this(...)`, §8.8.7.1), que **no existía**: el parser la producía, la atribución la rechazaba, y sin ella no se puede heredar de ninguna clase con constructor parametrizado. Dos desvíos deliberados de javac, ninguno semántico: `values()` copia con un **bucle** en vez de `$VALUES.clone()` (lo que importa de `clone()` es devolver un array fresco), y `valueOf` compara contra los nombres literales en vez de delegar en `Enum.valueOf(Class, String)`, que va por reflexión. Se sumó **`java.lang.Enum`** a `bootstrap/`+`boot/`: sin ella el `.class` emitido no se podía ni verificar ni correr. **✅ Y ahora el `enum` con constantes parametrizadas** (`ROJO(\"rojo\")`, §8.9.2): a cada **constructor propio** se le antepone `(String $name, int $ordinal)` tanto en el AST como en el **`Resolved` del símbolo** (la re-atribución no re-resuelve firmas, y sin eso `new E(\"A\", 0, …)` no encontraría el ctor), y se le inyecta `super($name, $ordinal)` de cabecera —o se le **propaga** a un `this(...)` delegado—; cada constante se construye con sus argumentos (`new Color(\"ROJO\", 0, \"rojo\")`), y el ctor `(String, int)` por defecto solo se sintetiza si el usuario no declaró uno. Corre de punta a punta (un `enum Size { SMALL(1), LARGE(3); … }` da `4`) y verifica" },
      { hz: "A", txt: "**✅ Inicializadores de campo** (§8.6/§12.4.2) — `int v = 7;` se vuelve una sentencia, unificada **en orden de fuente** con los bloques `{ }`/`static { }`: las de instancia al frente de cada constructor, las estáticas al `<clinit>`. Hasta ahora **no llegaban al `.class`**: un campo con inicializador compilaba a un campo en cero, y el emisor ni siquiera generaba `<clinit>` —así que el `$VALUES` de un `enum`, el `$SwitchMap` de su `switch` y el guard del `assert` quedaban declarados y vacíos—" },
      { hz: "A", txt: "**✅ Generación de `StackMapTable`** — hecha en B3 (ver ahí): la pila de operandos se lleva **tipada**, no como altura, y el tipo `uninitialized` (tag 8) identifica cada objeto por el offset de su `new`" },
    ] },
  { id: "B5", name: "Cumbre del compilador  ✅ núcleo cerrado", hz: "C",
    exito: "**logrado** — los grandes frentes de **genéricos** y de **Java SE 25** están cerrados: erasure/wildcards/inferencia, captura, sealed+exhaustividad, bridge methods y módulos. La **equivalencia plena** con `javac` (emitir `Signature`, inferencia completa del Cap. 18) es cuestión de **fidelidad**, y se trackea en **B6**.",
    items: [
      { hz: "C", txt: "**✅ Genéricos** — *type erasure*, *wildcards* (containment), subtipado e inferencia por argumentos ya funcionan" },
      { hz: "C", txt: "**✅ Inferencia por *target type*** — hecha en B4 (modo *checking* en la atribución; el diamante incluido). Las ***poly expressions*** ya se tipan y emiten (lambdas y *method refs* contra su interfaz funcional, con `invokedynamic`). El **boxing dirigido por tipo** (*TransTypes*), el target type en posición de **argumento de llamada** (dos fases §15.12.2.6) y la ***capture conversion*** (§5.1.10) ya están **cerrados** (ítems aparte)" },
      { hz: "C", txt: "**✅ `sealed`/`non-sealed` + exhaustividad de `switch`** (§8.1.1.2/§9.1.1.4/§8.1.6/§14.11.1.1) — el parser reconoce las keywords **restringidas** `sealed`/`non-sealed` (con *lookahead* que las distingue de un tipo/campo llamado `sealed`) y la cláusula `permits`; el grafo de símbolos gana la lista de **subtipos autorizados** (`permitted`), con el `permits` **implícito** (§8.1.6) rellenado de los subtipos directos de la unidad, así el resto del pipeline ve el conjunto completo. **Check** valida las reglas de buena formación: un subtipo directo de un `sealed` debe ser `final`/`sealed`/`non-sealed` (o implícitamente final: `enum`/`record`), un `permits` explícito solo puede listar subtipos **directos** y todo subtipo debe estar autorizado, y `non-sealed` exige un super `sealed`. Y la **exhaustividad**: una `switch`-**expresión** (siempre) y una `switch`-**sentencia con *patterns***/`case null` sin `default` deben cubrir **todos** los valores — la jerarquía `sealed` **recursiva** hasta las hojas, **todas** las constantes de un `enum`, y `true`/`false` de un `boolean` (JEP 455) —, y un patrón con **guarda** (`when`) **no** cubre. El `.class` emitido lleva el atributo **`PermittedSubclasses`** (§4.7.31), re-parseado por la JVM propia: el tipo queda **realmente sellado**. Destapó de paso un bug del parser —`case RED -> 1` se leía como la **lambda** `RED -> 1`; una etiqueta constante es una *conditional-expression*, no una *assignment-expression*—, ya cerrado" },
      { hz: "C", txt: "**✅ *Capture conversion*** (§5.1.10) — la *wildcard capture* con **variables de captura frescas**, no una aproximación a cotas. Un `RType::Capture{ id, upper, lower }` lleva sus cotas **inline** (sin tocar la tabla, que Attribute tiene inmutable) y un `id` fresco (contador con mutabilidad interior) que la hace **distinta** de otra captura igual —la *freshness* del §5.1.10—. La conversión (`capture`) transforma `G<…?…>` en `G<…CAPᵢ…>`: `?`/`? super B` → cota superior la del parámetro (`Object`); `? extends B` → `glb(B, Uᵢ)`; y `? super B` guarda además la cota **inferior** `B`. Se aplica en **acceso a miembros** (el receptor se captura **una vez**, así todas las sustituciones de la llamada comparten las mismas variables) y en **argumentos de invocación**. El subtipado ganó las dos reglas: `CAP <: t` por su cota **superior**, y `s <: CAP` **solo** por la **inferior** (`? super`). Efectos observables: `List<?>.get()` → `Object`, `List<? extends Number>.get()` → `Number`, el **lado escritura** de `? super Integer` acepta un `Integer` pero **no** un `Object` (por campo, donde el chequeo es estricto), dos capturas del mismo `?` **no unifican** (`a.val = b.val` se rechaza), y el caso insignia **`swap(List<?>)` → `swapHelper(List<T>)`** ahora **infiere** `T` = la captura (para lo que se hizo aplicable un `<T> m(Box<T>)` a un argumento cuyo tipo lo fija la inferencia, no la invariancia). La *erasure* de una captura es la de su cota superior, así que el emisor y el verificador la ven como su *upper bound*. **✅ Y el lado escritura por llamada** (cerrado en B6): la aplicabilidad **sustituye el receptor** en los params, así `list.add(x)` sobre `List<? super X>` también se chequea estricto —antes solo por campo—" },
      { hz: "C", txt: "**✅ *Bridge methods*** (§8.4.8.3 / §15.12.4.5) — los métodos **puente** sintéticos que hacen que la sobrescritura funcione a nivel de **bytecode** cuando la *erasure* difiere. El emisor, tras las clases reales, detecta por cada método propio concreto de instancia los métodos de un supertipo que **sobrescribe** —los params de aquél **sustituidos** (`T := Integer`) y borrados coinciden con los suyos (§8.4.2)— cuya *erasure* **sin sustituir** difiere, y sintetiza uno con el **descriptor borrado del supertipo** (`ACC_BRIDGE`+`ACC_SYNTHETIC`) que reenvía al real: `this` + los argumentos con un `checkcast` al tipo angosto + `invokevirtual` al método real + el `return` (el subtipo del retorno es asignable). Cubre las dos causas: el **parámetro genérico** borrado (`class INode extends Node<Integer>` con `set(Integer)` → puente `set(Object)`, y el clásico `Foo implements Comparable<Foo>` → `compareTo(Object)`) y el **retorno covariante** (`A.get():Object` / `B.get():String` → puente `Object get()`). Se dedup por descriptor y no pisa un método real ya presente. Verificado de punta a punta: el `.class` lleva el puente con sus flags, **verifica** (checkcast + invokevirtual), y el **despacho** funciona —`a.pick()` visto como `A.pick():Object` sobre un `B` corre el puente que reenvía a `B.pick():String`—. Acotado a **clases** (en una interfaz el puente sería un `default`, aparte)" },
      { hz: "C", txt: "**✅ Módulos** (§7.7 / §4.7.25) — el sistema de módulos (JPMS) del lado del **compilador**: parseo de un `module-info.java` y emisión de su `module-info.class`. El parser reconoce las keywords **restringidas** (`module`, `open`, `requires`, `exports`, `opens`, `uses`, `provides`, `to`, `with`, `transitive` — todas identificadores salvo `static`) y arma la `[open] module nombre { … }` con las **cinco directivas**: `requires [transitive] [static]`, `exports [to]`, `opens [to]`, `uses`, `provides … with` (con la sutileza de `requires transitive;`, donde `transitive` seguido de `;` es el **nombre** del módulo, no el modificador). El **emisor** produce un `module-info.class` fiel: `ACC_MODULE`, `this_class = module-info`, sin super/campos/métodos, y el atributo **`Module`** (§4.7.25) con los flags (`ACC_OPEN`/`ACC_TRANSITIVE`/`ACC_STATIC_PHASE`) y el `requires java.base` **mandated** implícito; el pool ganó `CONSTANT_Module`/`CONSTANT_Package` (nombres de módulo con **puntos**, de paquete/servicio en forma **interna** con `/`). **Check** valida la buena formación (§7.7): directivas **duplicadas** (mismo módulo/paquete/servicio), implementación repetida en un `provides`, y auto-`requires`. Verificado re-parseando el `module-info.class` con la JVM propia (que ya leía los tags 19/20). La **resolución del grafo** de módulos —legibilidad, accesibilidad entre módulos— es de tiempo de ejecución (JPMS), track de la JVM aparte" },
    ] },
  { id: "B6", name: "Fidelidad de `javac`  🚧 esquinas cerradas + tooling + diferencial de emisión", hz: "C",
    exito: "el `.class` emitido y los diagnósticos son **indistinguibles** de los de `javac` para el núcleo del lenguaje: se emiten **todos** los atributos, la inferencia cubre el Cap. 18, y los errores se **recuperan** en vez de cortar en el primero. Los cinco frentes grandes de Java SE 25 ya están (B5); esto cierra la brecha de **fidelidad** que queda.",
    items: [
      { hz: "C", txt: "**✅ Atributos de class-file** — se emiten `Code`/`SourceFile`/`StackMapTable`/`BootstrapMethods`/`RuntimeVisibleAnnotations`/`RuntimeVisibleParameterAnnotations`/`AnnotationDefault`/`PermittedSubclasses`/`Module` y **✅ `Signature`**. **✅ `Signature`** (§4.7.9) —el de mayor valor, ya **cerrado**: la firma **genérica** que la erasure borra del descriptor, emitida en **clase** (`<T:Ljava/lang/Object;>Ljava/lang/Object;`), **método** (`<T:…>(TT;)TT;`) y **campo** (`Ljava/util/List<Ljava/lang/String;>;`, `TT;`), con las cotas (interfaz tras un `:` extra), los *wildcards* (`+`/`-`/`*`) y solo cuando el elemento **usa genéricos**; sin él una clase/método/campo genérico perdía sus genéricos en el `.class` y la reflexión + la compilación separada los veían borrados. **✅ `InnerClasses`** (§4.7.6) —también **cerrado**: cada `.class` lista su **cadena de enclosing** (él mismo si es anidado + sus ancestros) y las clases que **contiene**, clasificando **miembro** (con dueño y nombre) / **local** (`Outer$1L`: sin dueño, con nombre) / **anónima** (`Outer$1`: sin dueño ni nombre) por el sufijo del binary, con los flags de la declaración; es lo que la reflexión necesita para `getEnclosingClass`/`getDeclaringClass`/`isAnonymousClass`—. **✅ `EnclosingMethod`** (§4.7.7) —el **complemento** de InnerClasses para local/anónimas, también **cerrado**: el desugar registra, al **levantar** la clase, el `(nombre, descriptor)` del método/constructor envolvente (o `None` en un inicializador → `method_index` 0), y el emisor pone `class_index` = la clase envolvente y `method_index` = ese `NameAndType`; con esto `getEnclosingMethod`/`getEnclosingConstructor` andan—. **✅ `Record`** (§4.7.30) —también **cerrado**, fiel: un `record` ahora extiende **`java.lang.Record`** (agregada a `boot/`, extraída del JDK), lleva **`ACC_FINAL`** y el atributo **`Record`** con sus componentes (nombre + descriptor, + `Signature` por componente genérico), lo que la reflexión exige para `isRecord()`/`getRecordComponents()`; de paso destapó y cerró dos bugs latentes —el parser no tomaba un **record genérico** (`record Box<T>(…)`) y el descriptor de una **variable de tipo** salía `LT;` en vez de la *erasure* `Ljava/lang/Object;` (no verificaba)—. **✅ `NestHost`/`NestMembers`** (§4.7.28/§4.7.29) —también **cerrado**: el *nest* (Java 11) que da acceso privado entre anidadas sin puentes sintéticos; cada clase **anidada** lleva `NestHost` a su **top-level**, y la top-level un `NestMembers` con **todas** sus anidadas (miembro/local/anónima, transitivo) — un nest **plano** por árbol de anidamiento—. **✅ `MethodParameters`** (§4.7.24) —también **cerrado**: los **nombres** de los parámetros formales (+ `ACC_FINAL` si se declaró `final`, y `ACC_SYNTHETIC` para las capturas `this$0`/`val$x`), lo que hace que la reflexión (`Method.getParameters()`) devuelva los nombres reales en vez de `arg0`/`arg1`; incluso un `record` conserva los nombres de sus componentes en el ctor canónico—. **✅ `Exceptions`** (§4.7.5) —también **cerrado**: las clases de la cláusula `throws` de cada método (los índices `Class` de las excepciones **chequeadas** que declara lanzar), lo que hace que `Method.getExceptionTypes()` funcione y que la compilación separada vea las checked exceptions; va tanto en un método concreto (junto al `Code`) como en uno **abstracto/native** (sin `Code`); verificado diferencialmente contra el `javap` de Temurin (lee el atributo con las clases correctas y en orden). **✅ `LineNumberTable`** (§4.7.12) —también **cerrado**: anidado en el `Code`, mapea offset de bytecode → línea del fuente (el emisor marca la línea al entrar a cada sentencia, deduplicando por pc y por línea; el `super()` implícito de un ctor se mapea a la línea de su declaración), lo que da los **números de línea en los stack traces** y habilita breakpoints por línea en un debugger; verificado contra `javap -l` de Temurin. **✅ `LocalVariableTable`** (§4.7.13) —también **cerrado**: anidado en el `Code`, da los **nombres** (+ descriptor + slot + rango de vida) de `this`, los parámetros y los locales del cuerpo, lo que hace que un debugger muestre los nombres reales. El emisor mantiene una **pila de scopes** que espeja la de la pasada 2 (que reusa slots al salir de un bloque): así dos locales que **comparten slot** en bloques disjuntos dan rangos **no solapados** (verificado contra `javap -l`, con `a`/`b` en el mismo slot y rangos disjuntos, igual que javac); los locales **muertos** (rango de largo 0) se descartan, también como javac. Cubre método/`Block`/`for`/`switch`/`synchronized`/`try`-`catch`-`finally`; quedan afuera los *pattern bindings* (instanceof/`switch`) —incompletos pero **nunca** solapados—. **✅ `ConstantValue`** (§4.7.2) —también **cerrado**: un `static final` con inicializador de **expresión constante** (§15.29) lleva el atributo (`Integer`/`Long`/`Float`/`Double`/`String`, coercionado al tipo del campo), que la JVM asigna al **preparar** la clase; el desugar lo deja **sin bajar** al `<clinit>` (no se inicializa dos veces, igual que javac) y así las constantes se pueden *inlinear* en compilación separada (§13.4.9); un evaluador acotado pliega literales con `+`/`-` unario (las expresiones aritméticas complejas caen al `<clinit>`, correcto pero sin inlinear); verificado contra `javap` de Temurin. **✅ `RuntimeVisibleTypeAnnotations`** (§4.7.20, JSR 308) —el último atributo, **cerrado**: **✅ Fase 1** las anotaciones sobre **parámetros de tipo** (`class C<@Foo T>` target `0x00`, `<@Foo T> void m()` `0x01`); **✅ Fase 2** las anotaciones **líder** de declaración ruteadas por su `@Target` (`@NonNull String` sobre **campo** `0x13`, **retorno** `0x14`, **parámetro** `0x16`, con exclusión del `RuntimeVisibleAnnotations` para las `TYPE_USE`-only); **✅ Fase 3** los usos **anidados** con su `type_path` reconstruido por el parser —argumentos de tipo (`List<@A String>` → `[TYPE_ARGUMENT(0)]`), *wildcards* (`? extends @A T` → `[…, WILDCARD]`) y arrays con la **dirección** correcta del path (`int @A []` → path vacío en la dimensión externa, `List<@A X>[]` → `[ARRAY, TYPE_ARGUMENT(0)]`)—, `extends`/`implements` (`0x10`, `supertype_index` `0xFFFF`/índice de interfaz), **cotas** de parámetro de tipo (`0x11` clase / `0x12` método, con el `bound_index` que arranca en 1 cuando la primera cota es una interfaz —el `Object` implícito ocupa el 0—, como javac), `throws` (`0x17`) y los **targets de `Code`** —cast (`0x47`, offset del `checkcast` + `type_argument_index`), `instanceof` (`0x43`), `new` (`0x44`, objeto **y** array, con el offset del opcode / del inicio de la creación) y **variable local** (`0x40`, con la **tabla de rangos de vida** `{start_pc, length, index}`, el mismo rango que el `LocalVariableTable`, y filtradas por `@Target(TYPE_USE)`)—, estos cuatro en un `RuntimeVisibleTypeAnnotations` **anidado en el `Code`**. Para retener la anotación por posición de tipo el parser acumula un buffer `pending_type_annos` (el `Type` del AST no tiene slot) que las declaraciones/expresiones drenan con su `type_path`, y el emisor la rutea al target + offset correcto. **Todo verificado byte-fiel** contra el `javap` de Temurin (parámetros de tipo, cotas, extends/implements, throws, field/return/param, y los cuatro targets de `Code`). Con esto **RVTA queda cerrado** y no quedan atributos de fidelidad **reflexiva** pendientes —solo variantes menores de bajo valor (`RuntimeInvisible{,Type,Parameter}Annotations`, `LocalVariableTypeTable`)—" },
      { hz: "C", txt: "**✅ Inferencia completa (Cap. 18)** — cerrada, con **barrido diferencial contra `javac` real** como evidencia. **✅ Lado escritura de la captura por llamada** — **cerrado**: la aplicabilidad ahora **sustituye el receptor** en los parámetros (§15.12.2.2), así una variable de tipo de **clase** pasa a su argumento (incluida la variable de **captura** de un `? super B`), y `list.add(x)` sobre `List<? super X>` se chequea **estricto** (`add(X)` vale, `add(Object)` no); las variables de tipo de **método** las deja la sustitución como están, para que la inferencia las resuelva. **✅ `lub` con intersección** (§4.10.4) — **cerrado**: un `RType::Intersection(A & B)` cableado por el sistema (subtipado `A&B <: t` sii algún miembro; `s <: A&B` sii todos; *erasure* = primer miembro, la **clase**), y el `lub` se computa por **supertipos borrados comunes mínimos** (así `Comparable` no se pierde por invariancia) recuperando la forma parametrizada cuando hay uno solo; ahora `lub(Integer, Long)` = `Number & Comparable`, y el ternario/inferencia se puede usar como cualquiera de los dos. **✅ Y el *solver* transitivo** (§18.3/§18.4) — **cerrado**: se **siembran las cotas declaradas** de cada variable (`<C extends List<T>>` ⇒ `C <: List<T>`), la **incorporación** cierra los bounds a punto fijo cruzando `S <: α` con `α <: U` (y reduciendo lo derivado, que puede fijar otra variable), y la **resolución** instancia en **orden de dependencia** sustituyendo lo ya resuelto (un ciclo se fuerza junto). Así una variable **sin cota directa** se deduce fluyendo por la de otra: `<T, C extends List<T>> T first(C c)` sobre un `List<String>` infiere `T = String` (antes caía a `Object`). De paso destapó un bug de `enter`: las cotas de un parámetro de tipo de **método** se resolvían en `global` en vez del scope propio del método, así que un `<C extends List<T>>` no veía ni al hermano `T` ni a las clases del paquete. **✅ Y el containment de `? super X`** (§18.2.3) — el caso `Super` de `reduce_arg`, que se caía: `List<? super X>` como parámetro **acota `X` por arriba** (`sup(aListOfAnimal)` con `<T> T sup(List<? super T>)` infiere `T = Animal`). **✅ Y la detección de `false`** (§18.5.1) en su caso claro — dos **igualdades incompatibles** sobre una misma variable (`m(List<Foo>, List<Bar>)` sobre `<T> void m(List<T>, List<T>)`) hacen **fallar** la inferencia y se reporta el error (en métodos **propios**; sobre un externo se mantiene la indulgencia, que no modela toda firma del JDK). **✅ Y el argumento fuera de la cota declarada** (§4.5.1/§18.3): `S <: α` con `S` fuera de la cota de `<α extends B>` (`m(\"x\")` sobre `<T extends Number>`) ahora hace **fallar** la inferencia —la aplicabilidad, indulgente con la variable de método, lo dejaba pasar—. Con esto el `false` de §18 queda cubierto en lo que importa. Las contradicciones cota-inferior/superior **derivadas del target** (`Integer n = id(\"hola\")`, donde el contexto impone `T <: Integer` y el argumento `String <: T`) se dejan **a propósito** al chequeo de asignación, que da mejor mensaje; reportarlas también en la inferencia las duplicaría. **✅ Y las variables de inferencia frescas** (§18.1) **con solving combinado de anidadas** (§18.5.2.1): cada invocación genérica introduce variables **frescas** (`RType::InferVar`, no el propio parámetro de tipo), y una **llamada genérica anidada** como argumento (`pick(empty(), strs())`) ya **no** se resuelve aislada —lo que daba `empty() → Box<Object>` y rechazaba la llamada—: sus variables entran en el **mismo** conjunto de constraints que las de la externa, así `empty()` toma su `E` del contexto (`E = String`) y el nodo se re-tipa. **✅ Y la aplicabilidad por inferencia** (§18.5.1) **con desempate de sobrecargas** (§18.5.4): un overload genérico cuya inferencia de argumentos es **insatisfacible** (`m(T, List<T>)` sobre `(List<String>, List<String>)`: el 2º arg fija `T = String`, el 1º pide `List<String> <: T`) ya **no** es aplicable —lo detecta un `hard_false` sobre los bounds **sin podar**, gateado solo para **desambiguar** entre varios candidatos—, así que el desempate elige el mismo método que javac. Con esto, y completando la tabla de containment del §18.2.3, un **barrido de 13 casos borde** (desempate, capturas, wildcards, functional-interfaces, lub/glb) matchea a javac uno a uno. **✅ Y las tres esquinas que faltaban —que se creían no-op por capturar en el sitio de uso, y resultaron ser tres divergencias observables reales, las tres de indulgencia (aceptábamos programas que javac rechaza, o rechazábamos uno que acepta)—, cerradas y verificadas contra Temurin 21:** **(a) §18.3 capture-in-bounds** — un método que devuelve `G<? extends T>` toma su `T` del *target* (`Box<? extends Number> b = make()` ⇒ `T = Number`); antes no se miraba dentro del wildcard del retorno, `T` caía a `Object` y se rechazaba una asignación **válida** (nuevo `reduce_target_arg`: `? extends inner` contra `? extends X` ⇒ `inner <: X`, y `? super` análogo). **(b) §18.4** — la variable de una llamada **anidada** que vive solo bajo un wildcard del retorno (`take(make())`) se resuelve por **sus** cotas (⇒ su declarada `Object`), **no** heredando el *target* externo; la clausura transitiva ya no le pasa cota superior a una variable de inferencia **pelada**, así `take(make())` con target `Number` **falla** como javac (antes lo aceptábamos). **(c) §5.1.10** — pasar un argumento `G<? extends X>` a un parámetro **invariante** `G<α>` **captura** (`α = CAP` fresca); si el *target* además fija `α = T` concreto son igualdades incompatibles y la llamada se **rechaza** (`reduce_arg` gana las arms de captura contra param invariante, con cota **concreta** para que el conflicto sea *proper*). Un **barrido diferencial de 9 casos** anidados (capture/wildcard/nido) contra Temurin 21 matchea uno a uno. El único resto del formalismo —el *second attempt* literal del §18.4, la variable de tipo `Y` de rescate— es un **no-op observable verificado**: la *capture conversion* (§5.1.10) en el sitio de uso ya produce la `Y` equivalente, así que no hay ningún programa que rescate (implementarlo pediría mantener bounds `G<α> = capture` en el conjunto, reescritura arquitectónica para cero cambio observable). **La inferencia del Cap. 18 queda cerrada, con barrido diferencial contra `javac` real como evidencia** —ya no por argumento de equivalencia—" },
      { hz: "C", txt: "**✅ Diagnósticos y recuperación de errores** (**cerrado**) — **✅ recuperación del parser**: en vez de abortar en el primer error de sintaxis, el parser entra en **panic-mode** y **resincroniza** —a nivel **unidad** (salta al comienzo de la próxima declaración de tipo, rastreando llaves), **miembro** y **sentencia** (salta al próximo `;`/`}` del cuerpo)—, acumula **todos** los errores en un `Vec<Error>` y **sigue** construyendo el AST con las partes buenas; así una unidad con tres declaraciones rotas reporta los **tres** errores en una corrida y aún parsea los miembros/tipos válidos de alrededor. La **semántica** ya reportaba varios (Enter/Attribute/Check/Flow acumulan), y `--check` ahora lista los sintácticos **primero** y luego los semánticos. **✅ Frame de render tipo `javac`** (**cerrado**): el CLI ya no imprime un escueto `línea:col: error: msg` sino el marco de `javac` —`archivo:línea: error: mensaje` (la columna se transmite con el **caret**, no en el texto), la **línea del fuente**, una línea con el `^` bajo la columna (preservando los tabs del prefijo para alinear), las **notas** indentadas, y un resumen `N error[s]` al final—; el tipo `Error` ganó un campo `notes: Vec<String>` que el render imprime. **✅ Mensajes ricos** (**cerrados** los tres más comunes): el ***cannot find symbol*** lleva las sub-líneas `símbolo:`/`ubicación:` (`método fro(int)` / `clase String`, con las etiquetas alineadas) en los cuatro sitios —método, campo, nombre pelado, *method ref*—; los **candidatos de sobrecarga descartados** (§15.12.2) se listan cuando el nombre existe pero ninguna sobrecarga aplica, cada uno con su firma (receptor sustituido) y el **motivo** (aridad distinta, o el primer argumento que no convierte, espejando `applicable`); y el ***did-you-mean*** sugiere el nombre más cercano en scope por **distancia de edición** (Levenshtein, umbral `long/3`) —método/campo del receptor, o locales + campos de la envolvente para un nombre pelado—. **✅ Nodos de error en el AST** (**cerrado**): la recuperación a nivel **expresión**, no solo estructural. Una expresión que no parsea se vuelve un nodo `ExprKind::Error` y la **estructura de alrededor sobrevive** —cableado en el inicializador de un `local` (la variable **igual queda declarada**) y en los **argumentos** de una llamada (`f(bad, ok)` no pierde `ok`)—; el parser registra el error, sincroniza a un límite de expresión (`,`/`;`/`)`/`]`/`}` de profundidad 0) y sigue, y la atribución tipa el nodo `Unresolved` **sin** un error nuevo. Así `int x = @@@; int z = x + 1;` reporta **solo** el error de sintaxis: `x` queda declarado y `x + 1` **no cascadea** «no se encuentra `x`». Con esto el ítem queda **cerrado**; el único resto es puramente cosmético (los **mensajes ricos** de `javac` que faltan —candidatos de constructor, notas de *unchecked*—, de bajo valor)" },
      { hz: "C", txt: "**✅ Esquinas de lenguaje (A/B/C/D)** — cerradas las cuatro, verificadas contra `javac` real: **(A)** evaluación **completa** de expresiones constantes (§15.28/§15.29) a `ConstantValue` — referencias entre `static final`, concatenación de `String` (§15.18.1) y condiciones booleanas de *dead-code* (§14.21); **(B)** *bridges* en **interfaz** (métodos `default` puente §9.4.1.3 — `invokeinterface` + `InterfaceMethodref`, con la vtable plegando los `default` de superinterfaces §5.4.3.3); **(C)** exhaustividad con **record patterns anidados** (JEP 440/441, reducción columna a columna); **(D)** asignación definitiva de un `switch` **exhaustivo sin `default`** (§16.2.9, el `default` implícito), ya integrada con Flow" },
      { hz: "C", txt: "**🚧 Nivel herramienta** (el `javac` **como herramienta**, fuera del núcleo del lenguaje) — **✅ `-Xlint`** (§9.6.4.5): un pase de lint con **nueve categorías** (`empty`, `cast`, `fallthrough`, `deprecation`, `rawtypes`, `unchecked`, `finally`, `serial`, `this-escape`), con **severidad** en el diagnóstico, `@SuppressWarnings` (§9.6.4.6) y render estilo `javac`. **✅ APT — fase 1**: la API de `javax.annotation.processing` / `javax.lang.model`, y la resolución de **tipos anidados de clases externas** —leyendo el atributo `InnerClasses`— como `Diagnostic.Kind`. **⬜ Falta:** el *round loop* del procesamiento y `javadoc`" },
      { hz: "C", txt: "**✅ Diferencial de emisión** — un **arnés** (`tools/emitdiff`) que vuelve **medible** la fidelidad: compila cada `.java` con **nuestro** `javac` y con el **real**, desensambla ambos con el mismo `javap -p -c` y compara el resultado **normalizado** —sin los índices del constant pool (difieren por el orden de emisión), con las líneas **ordenadas** para que el orden de los miembros no genere ruido—. En la primera pasada destapó y **cerró** un lote de divergencias: el `default` de interfaz salía **sin `ACC_PUBLIC`** (§9.4); `x++`/`x += c` no usaban **`iinc`**; las comparaciones contra **0** iban por `iconst_0; if_icmp…` en vez de `ifgt`/`ifle`/…, y un `-literal` por `push; neg` en vez de `iconst_m1`; una clase **anidada** emitía `ACC_STATIC` en los access_flags **de clase** (bug de correctitud: la JVM rechaza ese `.class`, §4.1) y su ctor por defecto salía siempre `public` en vez de heredar el acceso (§8.8.9); un `enum` no emitía el `Signature` de su supertipo `Enum<E>` (§8.9) y su `values()` copiaba con un **bucle** en vez de `(E[]) $VALUES.clone()` —lo que exigió soportar **`array.clone()`** de punta a punta (§10.7)—; y una switch-**expresión** sobre `String` en cola no compilaba. Lo que queda son **desvíos deliberados** semánticamente equivalentes (`valueOf()`/`$values()`, el `finally` duplicado, la switch-expresión materializada en un temporal)" },
    ] },
];

// ---- Componentes del compilador: las pasadas y las estructuras intermedias ----
const TBL_PASADAS = [
  ["**Lexer**", "texto `.java` → tokens", "B0", "✅"],
  ["**Parser**", "tokens → **AST** (cada nodo con su posición de fuente)", "B1", "✅"],
  ["**1 · Enter**", "declaraciones → **tabla + grafo tipado** (class finder real, resolución)", "B2", "✅"],
  ["**2 · Attribute**", "resuelve nombres + tipa + **decora** el AST (overload, genéricos, inferencia)", "B2", "✅"],
  ["**Check**", "bien-formación de **declaraciones** (override §8.4.8, abstractos §8.1.1.1) + **usos** (acceso §6.6, excepciones chequeadas §11.2)", "B4", "✅"],
  ["**3 · Flow**", "asignación definitiva (+ `final`) + alcanzabilidad", "B4", "✅"],
  ["**4 · Desugar**", "baja azúcar (sentencias, concat, compuesta, varargs, `++`/`--`, switch-expr→sentencia, `switch` sobre `String`/`enum`/*patterns*, guard del `assert`, records, miembros del `enum`, inicializadores de campo), **LambdaToMethod** (lambdas/*method refs* → `invokedynamic`), `record` por **`ObjectMethods`**, y la **captura del entorno** de las clases internas/locales/anónimas (`this$0`/`val$`, con `hoist_anonymous`); el **boxing** va en *TransTypes*, su pasada aparte", "B4", "✅"],
  ["**5 · Codegen**", "AST decorado → **bytecode** → `.class` (v69): objetos, tipos anchos, flujo de control completo (`switch`, ternario, saltos etiquetados), excepciones, `synchronized`, **StackMapTable** e **`invokedynamic`** (lambdas, *method refs*, `record`)", "B3", "✅"],
];

const TBL_ESTRUCTURAS = [
  ["Tabla de símbolos", "catálogo de todo lo declarado, con sus tipos", "persistente"],
  ["Arena de tipos (*interned*)", "tipos resueltos, comparables por `TypeId` en O(1)", "persistente"],
  ["Interner de nombres", "identificadores → `NameId` (comparación e identidad baratas)", "persistente"],
  ["**Log de errores**", "acumula errores/warnings — **no aborta** al primero", "persistente"],
  ["Decoración del AST", "cada nodo lleva su `tipo` y su `binding` **in situ** (salida de Attribute, estilo `JCTree.type`/`sym`)", "persistente"],
  ["Álgebra de tipos (`types`)", "subtipado, erasure, sustitución, `lub`/`glb` — el `Types` de javac", "persistente"],
  ["Motor de inferencia (`infer`)", "variables de inferencia + *bounds* + resolución (Cap. 18)", "transitorio"],
  ["Tabla de constantes", "valores de expresiones constantes (JLS §15.29)", "persistente"],
  ["*Class finder*", "carga los **tipos externos** (`.class` del classpath)", "persistente"],
  ["**Env** (contexto)", "*scope* / clase / método / tipo esperado actuales", "transitorio"],
  ["Estado de *Flow*", "*bitsets* de asignación definitiva y alcanzabilidad", "transitorio"],
  ["Estado de inferencia", "variables de inferencia + restricciones (genéricos/lambdas)", "transitorio"],
];

const PASADAS_ASCII = [
  "  .java ─[lexer]─▶ tokens ─[parser]─▶ AST",
  "                                      │   (un solo árbol, reusado por las 5 pasadas)",
  "                                      ├─ 1 Enter      ─▶ tabla de símbolos",
  "                                      ├─ 2 Attribute  ─▶ decora el AST (tipos + bindings)",
  "                                      ├─ 3 Flow       ─▶ asignación definitiva + alcanzabilidad",
  "                                      ├─ 4 Desugar    ─▶ baja la azúcar sintáctica",
  "                                      └─ 5 Codegen    ─▶ bytecode ─[write]─▶ .class",
].join("\n");

const FASE_C = [
  { id: "C0", name: "Núcleo de java.lang", hz: "B",
    exito: "un programa que usa `String` y `System.out` corre en tu JVM.",
    items: [{ hz: "B", txt: "`Object`, `String`, `System`, wrappers, `Math`, `StringBuilder`" }] },
  { id: "C1", name: "Excepciones", hz: "B",
    exito: "lanzar y atrapar excepciones de la biblioteca propia.",
    items: [{ hz: "B", txt: "Jerarquía `Throwable`/`Exception`/`RuntimeException`" }] },
  { id: "C2", name: "Colecciones e IO", hz: "A",
    exito: "un programa que usa `ArrayList`/`HashMap` corre.",
    items: [
      { hz: "A", txt: "`java.util`: `List`, `ArrayList`, `Map`, `HashMap`" },
      { hz: "A", txt: "`java.io`: `InputStream`/`OutputStream`/`PrintStream`" },
    ] },
  { id: "C3", name: "Cumbre de la biblioteca", hz: "C",
    exito: "cubrir lo necesario de `java.base` para programas reales.",
    items: [{ hz: "C", txt: "Resto de `java.base` (net, nio, time, reflexión completa…)" }] },
];

const FASE_F = [
  { id: "F-bench", name: "Infraestructura de medición  ✅ hecha", hz: "A",
    exito: "**hecho** — sin números honestos antes/después, todo el track es fe. `bench_baseline` (`#[ignore]`, `cargo test --release --lib bench_baseline -- --ignored --nocapture`) mide 5 workloads que aíslan una dimensión cada uno; los conteos de opcodes son exactos y reproducibles, así que **ns/opcode** compara entre workloads aunque los tiempos absolutos tengan ruido.",
    items: [
      { hz: "A", txt: "**Workloads** (`java/Bm*.java`): `BmLoop` (aritmética frame-local = el piso), `BmArray`, `BmInvoke` (invokestatic), `BmVirtual` (invokevirtual polimórfico), `BmField` (`new` + get/putfield). Valores esperados contrastados contra el `java` real del JDK 25, y un test **no-ignorado** los verifica para que los benchmarks no se pudran en silencio" },
      { hz: "A", txt: "**Protocolo, aprendido a los golpes**: el ruido de *code layout* de esta máquina es **±3-12%, mayor que el efecto de casi cualquier cambio**. Demostrado con un experimento nulo: agregar un campo `HashMap` **sin usar** movió un control +3.4%. Por eso: workloads de **control de efecto-cero** (verificados *contando* la operación, no razonando), **cuadrado latino** con los binarios pre-compilados, medianas y mínimos, y la regla \"si la dispersión intra-rama es mayor que el efecto que declarás, todavía no lo mediste\"" },
      { hz: "A", txt: "**Medir antes de optimizar** — dos cuellos \"obvios\" resultaron falsos: el GC parecía el culpable de que `BmField` fuera 8.6× el piso (era **≤5%**), y el scan lineal O(#clases) de `class_name_at_mirror` parecía la gran palanca de `BmVirtual` (valía ~3%: hay **5** mirrors, no cientos)" },
    ] },
  { id: "F0", name: "Quickening  ✅ hecho", hz: "A",
    exito: "**logrado, y con creces**: un método que invoca en bucle ya no re-resuelve nada — pero la medición mostró que el mismo patrón estaba en **tres** opcodes, no solo en los invokes. Resultado: `getfield`/`putfield` **−86%**, `invokevirtual` **−45%**, `invokestatic` **−40%** (acumulando el pool de frames), sin cambiar una sola regla de la spec.",
    items: [
      { hz: "A", txt: "**El diagnóstico**: las tres optimizaciones tenían la **misma forma** — *un cache keyeado por `&str` alcanzado alocando un `String`, recomputado por ejecución en vez de por call site*. Y esas alocaciones no eran necesidad semántica sino **impuesto del borrow checker** (`class_of()`/`name()` ya devuelven `&str`; se clonaban solo para poder llamar métodos `&mut self` después). Por eso se pudo sacar el 86% sin tocar semántica: no se hizo el trabajo más rápido, **se dejó de hacerlo dos veces**" },
      { hz: "A", txt: "**Campos** (`field_sites`): `field_offset` **reconstruía el layout entero de la clase** —un `Vec<(String,String)>` fresco, recorriendo la cadena de superclases— **en cada acceso**. Cache por `(MethodId, pc)`: una celda `AtomicU64` por byte de código, sin hash ni strings. De paso cerró un peligro real: `layout_fields_ref` truncaba en silencio ante una superclase no cargada, devolviendo offsets **demasiado chicos**; sin cache eso solo causaba una escalación, **con** cache habría inmortalizado un offset incorrecto — ahora solo se cachea si la cadena se recorrió entera" },
      { hz: "A", txt: "**Llamadas** (`call_sites`): callee resuelto, `vtable_slot` del tipo estático, anchos de slots (antes un `Vec` fresco por llamada), y la cadena de **~7 comparaciones de string** contra nombres de intrínsecos reemplazada por un enum `Intrinsic` **en el `MethodBody` del callee** —no en el sitio— porque por vtable el sitio no puede saber la respuesta: depende del receptor. El bit de \"ya inicializada\" solo se pone con la clase en `Done`, así que `Erroneous` sigue tirando `NoClassDefFoundError` en cada uso" },
      { hz: "A", txt: "**Pool de frames**: reusar `Frame`s (y la capacidad de sus `Vec`) en vez de dropearlos → **−11/12%** más en llamadas, ~72 ns/llamada recuperados. El riesgo era de GC —un frame retirado retiene referencias stale— y se blindó con **una sola puerta de entrada** (`recycle`, que hace `scrub` incondicional), `debug_assert!(is_scrubbed())` en cada reuso **con la suite entera corrida en modo debug**, y la verificación de que el pool no está en ningún camino de raíces del colector" },
    ] },
  { id: "F1", name: "Superinstrucciones / fusión  ⏭️ salteado a propósito", hz: "A",
    exito: "**decisión, no omisión**: fusionar `iload,iload,iadd` atacaría el piso de despacho (~28-30 ns/opcode) con una mejora estimada de 10-20% — **por debajo del ruido de layout de esta máquina (±3-12%)**. O sea: no podríamos *demostrar* que funciona. El esfuerzo se redirigió a F3, donde el efecto (2 órdenes de magnitud) es medible muy por encima del ruido.",
    items: [{ hz: "A", txt: "Queda disponible si en algún momento el piso de despacho vuelve a ser el cuello y hay instrumentación capaz de resolverlo" }] },
  { id: "F2", name: "Inline caching + stack caching  ✅ hecho", hz: "C",
    exito: "**hecho (2026-08-21)** — con una corrección de rumbo: este hito es de la **caja de herramientas del intérprete**, no del compilador (la fase arranca diciendo que *«un optimizador no necesita JIT»*). Las dos ideas ya existían dentro del JIT, pero eso dejaba al intérprete igual que antes; se implementaron donde correspondía.",
    items: [
      { hz: "C", txt: "**✅ Inline cache monomórfico** — guarda de clase exacta + cuerpo inlineado + **deopt en miss**. La clase se elige por **observación del propio intérprete**: `invokevirtual`/`invokeinterface` ya cargaban el header del receptor para despachar, así que registrarlo cuesta **un store `Relaxed` en un camino que ya tenía el valor en la mano**, y al llegar al umbral hay 32 observaciones gratis. Adivinar el tipo estático sería peor: una apuesta sobre una clase que el sitio puede no ver nunca, y errarla cuesta un deopt **por llamada** en vez de nada" },
      { hz: "C", txt: "**✅ Stack caching en el JIT** — registros para la pila de operandos dentro del bloque básico, derramando en los bordes; `JVM_JIT_REGS=0` lo desactiva, de modo que las dos ramas son **el mismo binario** y el ruido de layout queda estructuralmente ausente de la comparación" },
      { hz: "C", txt: "**✅ Inline cache en el INTÉRPRETE** — el dato ya se estaba pagando: la celda que solo alimentaba al compilador ahora guarda `[mirror | método]` en **una sola palabra atómica**, y en un hit se saltea el lookup de vtable entero (en `invokeinterface`, un **escaneo lineal comparando dos `String` por entrada**). La palabra única es requisito de **corrección**: dos celdas `Relaxed` paralelas se pueden leer desgarradas, y un torn read de este cache no es una llamada lenta sino **una llamada al método equivocado**. **Sin invalidación**, con un argumento más fuerte que «el lookup está bien»: *el cache no puede discrepar del lookup que reemplaza* — la clave es estable porque los mirrors están pinneados, y el valor porque `vtables` es insert-only" },
      { hz: "C", txt: "**La medición, aislada del ruido**: los controles se movieron **−8% por puro relayout**, así que en vez de pelear contra eso se midió la razón **monomórfico/megamórfico dentro del mismo binario** — mismo programa opcode por opcode, mismas direcciones, difiriendo solo en el receptor: **1.0037 → 0.9385**, rangos sin solaparse. Antes de F2 un sitio monomórfico costaba **lo mismo** que uno megamórfico; ahora cuesta **6.5% menos**, y el megamórfico no se degrada" },
      { hz: "C", txt: "**Un sabotaje que no atrapó nadie**: un guard que compara solo los **16 bits bajos** del mirror pasaba la suite entera en verde. Que dos clases coincidan en media palabra es suerte, y un guard de clave parcial es silencioso hasta el día que no lo es. Van asertos de los 32 bits completos, y un test permanente que **falla si un workload de control empieza a despachar** — la deuda exacta que hizo que `BmArray`/`BmField` dejaran de ser controles sin que nadie se enterara" },
      { hz: "C", txt: "**⏭️ Stack caching en el intérprete: evaluado y descartado, con datos** — segunda vez que un hito de esta fase se cierra decidiendo *no* hacerlo (la primera fue F1). Costo: 193 `push`, 64 `pop`, 9 escapes a `operands_mut()` y **tres recorridos de raíces del GC** que leen la pila, cada uno con flush/reload. Beneficio: techo **~5%**, porque antes de cualquier handler `run_one` ya paga safepoint, dos indexados al scheduler con bounds-check y `sync_code_cache` — inmedible sobre un instrumento que se mueve −8% por relayout. **Dónde sí está la plata, medido:** `BmInvoke` 51.9 ns/opcode contra 36.6 de `BmLoop`, con el call site ya cacheado ⇒ lo que queda es **armado y desarmado de frames**" },
    ] },
  { id: "F3", name: "JIT (bytecode → nativo)  ✅🟡 primer tier corriendo", hz: "C",
    exito: "**la cumbre, alcanzada en su primer tier**: hay un compilador propio que emite x86-64 y lo ejecuta. `BmLoop` pasó de **475 ms a 4.1 ms — 116×** (medido de nuevo tras ensanchar el subconjunto: **125×**) — y el número que lo explica no es el tiempo sino el conteo de opcodes: **17.325.011 → 620**. El bucle dejó de existir para el intérprete. Los cuatro workloads de control quedaron planos.",
    items: [
      { hz: "C", txt: "**Emisor x86-64 propio** (`src/burst/x64.rs`), sin dependencias — las funciones de Windows se declaran a mano con `extern \"system\"`, igual que en su día se escribió el cursor de bytes en vez de usar `nom`. ALU, `idiv`, `jcc` con labels y parcheo de desplazamientos, ABI **Microsoft x64** (args en RCX/RDX/R8/R9, 32 bytes de shadow space, RSP alineado a 16). Verificado **ejecutando**: bucles, factorial, y un test de código generado **llamando a código generado**, que cubre de una sola vez las tres reglas del ABI que solo muerden en un `call`" },
      { hz: "C", txt: "**Memoria W^X como garantía del tipo** (`exec_mem.rs`): `CodeBuf` (escribible) → `.make_executable()` **consume `self`** → `ExecMem` (ejecutable). Después de la transición no queda ningún handle escribible — no es que \"recordamos no escribir\", es que el tipo lo hace imposible" },
      { hz: "C", txt: "**Compilador** (`compile.rs`) del subconjunto **frame-local de enteros** — el mismo que ya corría lock-free (W1), o sea sin heap, sin llamadas, sin referencias: un método así es una **función pura de sus locales**. Cualquier otro opcode ⇒ no se compila. La profundidad de la pila se recalcula **del CFG**, sin confiar en el `StackMapTable`: un desacuerdo entre ramas se rechaza en vez de generar código sobre una suposición" },
      { hz: "C", txt: "**Las tres trampas de semántica**, con prueba: (1) *normalización* — todo `int` en registro es la sign-extensión de sus 32 bits; se emite `movsxd` tras la aritmética pero **no** tras `iand/ior/ixor`, con la demostración de por qué (los 33 bits altos de un sign-extendido son copias del bit 31); (2) *shifts* — Java enmascara a 5 bits, x86 a 6 en ops de 64, y `iushr` es lógico sobre 32 (`-1>>>1` debe dar `MAX_VALUE`); (3) *división* — `INT_MIN/-1` trunca como manda el JLS §15.17.2 en vez de fallar con `#DE`. Los 11 operadores binarios contrastados contra un modelo en Rust sobre **361 pares de valores de borde**" },
      { hz: "C", txt: "**Deopt por pureza**: como el subconjunto no tiene efectos secundarios, ante cualquier situación que el nativo no pueda manejar (divisor cero) el código **abandona y el intérprete ejecuta el método desde cero** — re-ejecutar es indistinguible. Eso evita emitir manejo de excepciones en nativo, que es donde estos proyectos se vuelven inmanejables" },
      { hz: "C", txt: "**OSR + poll de safepoint**, ambos restringidos a back-edges con **pila de operandos vacía** (un `goto` de retorno de `while`/`for` la tiene vacía → cubre los bucles reales), con lo que el estado a transferir se reduce a *locales + pc* en las dos direcciones. Sin OSR, un método con un bucle largo entrado **una sola vez** nunca calentaba: es exactamente el caso de `BmLoop`. El poll **sale hacia el intérprete** en vez de manejar el GC desde el nativo" },
      { hz: "C", txt: "**El differential testing sale gratis**: con el JIT activo en `green`/`os-gil` y apagado en `os-parallel`, el oráculo `green≡os-gil≡os` que el proyecto usa hace meses **se convierte en la comparación JIT-vs-intérprete** sobre el corpus entero. La suite pasa igual con `JVM_JIT=0` y sin él. Además, un `static` estable para la palabra de poll: el `gc_pending` del driver es un `Arc` construido **fresco en cada corrida** (e inexistente en green), así que hornear su dirección habría sido corrupción silenciosa" },
      { hz: "C", txt: "**✅ Subconjunto ensanchado** — prefijo `wide` (hoy un `+= 256` ya no descalifica un método), los siete opcodes de pila que faltaban (`dup_x1`…`dup2_x2`, `pop2`, `nop` — que en su mayoría **no emiten código**: la pila se simula en compilación y son permutaciones de slots), `tableswitch`/`lookupswitch` (cadena de comparaciones; con el padding de alineación a 4 bytes, el *default* como target real, y los arms que saltan hacia atrás registrados como cabeceras de bucle para OSR), y **`getstatic` de `int`**. Guarda linda: `wide iinc` mide **6 bytes**, y un decodificador que lo tratara como 4 se re-sincronizaría sobre el byte bajo del delta —que en `0x0102` es `0x02`, un `iconst_m1` perfectamente decodificable, o sea corrupción que sigue \"ejecutando\"— así que hay un test clavado para eso" },
      { hz: "C", txt: "**Dos rechazos razonados, que valen tanto como lo agregado.** `putstatic`: la regla \"compilar solo si el método no tiene sitios de deopt-por-reinicio\" era sólida, pero aceptarla convertiría el invariante *\"el código compilado no escribe nada observable\"* en *\"…salvo cuando no puede deoptar\"*, y **cada guarda futura** tendría que re-derivar su interacción con esa excepción; el censo dice que bloquea 6 de 682 métodos, así que se paga poco por mantenerlo limpio. `long`: **no es esfuerzo, es un límite estructural** — `lreturn` no entra en el protocolo de retorno (`RAX = (status<<32) | value` deja 32 bits, suficientes para un `int`, imposibles para un `long`), así que exige mover la frontera misma y con ella el marshalling, el write-back de OSR y cada `unsafe` que la cruza" },
      { hz: "C", txt: "**El techo, medido** (censo `subset_census` sobre el corpus): **77 de 710 métodos** compilan — 31% del techo de 246, ya que `ireturn` es la única salida posible. Los bloqueantes, en orden: **`aload_0` 354** (todo método de instancia y constructor: `this` es una referencia), `new` 96, `invokestatic` 41, statics no-`int` 26, `ldc2_w` 18. Nota: los 18 métodos `void` que aparecen bloqueados **no valen nada** — un método puro y frame-local que devuelve `void` no puede tener ningún efecto observable, es literalmente un no-op. O sea que ensanchar más el subconjunto de *opcodes* ya tiene rendimiento decreciente: **lo que sigue son referencias y llamadas**" },
      { hz: "C", txt: "**✅ Referencias** — el premio: `aload_0` **desapareció** de la lista de bloqueantes. Entraron `aconst_null`, `aload`/`astore` (+`wide`), `ifnull`/`ifnonnull`, `if_acmp*`, **`getfield` de `int`**, `arraylength`, `iaload` y **`areturn`** (que sube el techo del censo, porque `ireturn` dejó de ser la única salida). Y **no hicieron falta stack maps para el colector**: como el nativo no aloca, corre solo en green/os-gil y en los polls *sale*, **ningún GC puede observar un frame compilado** — el objeto graph está congelado mientras hay nativo en la pila. Lo único necesario es un **mapa de tipos** (int/ref por local y por posición de pila, por interpretación abstracta sobre el mismo punto fijo que ya calculaba la profundidad) en la frontera de marshalling" },
      { hz: "C", txt: "**Dos hallazgos que el diseño no anticipaba.** (1) **El heap son DOS buffers**: Eden vive en un `AtomicRegion` aparte y survivors/Old en `memory`, así que `base + offset` es una función de **dos ramas** — asumir un solo buffer habría hecho que todo acceso a un objeto en Eden leyera memoria ajena. (2) **Los exception handlers son aristas que el recorrido hacia adelante no ve**, así que el intérprete puede llegar a un loop header habiendo ejecutado código que el mapa nunca analizó (alcanzable de verdad: un deopt lo manda a re-ejecutar, y *esa* pasada puede lanzar y ser atrapada) — por eso un método con tabla de excepciones compila pero **no recibe entradas por OSR ni sitios de poll**" },
      { hz: "C", txt: "**El test del peor modo de falla, verificado como detector.** Devolver una referencia como `Value::Int` en el write-back haría que el GC tratara un entero como puntero (o al revés) y no fallaría donde está el bug. No alcanzó con testearlo: se **rompió el código a propósito** para confirmar que el test efectivamente falla, y recién después se restauró. Un test que nunca viste fallar no es un test. En la misma línea, los tests machine-level usan un `FakeHeap` con **dos** buffers, porque uno solo dejaría pasar a un compilador que ignorara la partición de Eden" },
      { hz: "C", txt: "**Censo: 77 → 96 métodos** (32% de un techo que subió de 246 a **299** gracias a `areturn`). `BmVirtual` dejó de ser un control —sus overrides son `aload_0; getfield; ireturn`— aunque gana solo 1.15×: los cuerpos son diminutos y domina el cruce de frontera. Rechazado a propósito: los campos **`volatile`**, aunque una lectura volátil *es* un `mov` común en x86-64 — apoyarse en el modelo de memoria de una arquitectura dentro de código generado merece su propio paso" },
      { hz: "C", txt: "**✅ Deopt de verdad** — el deopt dejó de ser *por reinicio* (abandonar y re-ejecutar el método, válido solo mientras todo fuera puro) y pasa a **reconstruir el estado del intérprete en un pc**: locales, **pila de operandos** y posición. La pieza que faltaba ya existía: el mapa de tipos del paso anterior sabe, en cada pc, cuántos operandos hay y de qué tipo. Con eso entraron las **escrituras** (`putfield`, `iastore`, `putstatic` de `int`). Efecto lateral elegante: polls de safepoint y deopts **se unificaron** en una sola tabla de `resume_sites` — un header de bucle resultó ser un sitio de resume con pila vacía" },
      { hz: "C", txt: "**La regla que hace correctas las escrituras**: *toda guarda de deopt se emite **antes** del primer efecto observable de su instrucción, y nada después del efecto puede deoptar*. O sea: cada instrucción tiene una fase de guardas (sin efectos) y una de efectos (sin deopts). De ahí sale que el pc devuelto siempre nombra una instrucción cuyo efecto **no** se aplicó, y el par «intento nativo + resume interpretado» escribe **exactamente una vez**. Validado rompiéndolo: reponer el viejo reinicio da 832497 en vez de 832289 — 208 de diferencia, que son las escrituras duplicadas" },
      { hz: "C", txt: "**✅ Alocación (`new`) sin romper el invariante** — alocar puede disparar un GC, y todo el diseño depende de que eso no pase con nativo en la pila. Solución de los JIT reales: emitir **solo el camino rápido** (bump de Eden con `lock xadd` sobre *la misma palabra* que usa el intérprete, así una reserva nativa nunca se solapa con una suya) y **deoptar** cuando no entra — si el bump entró, no hubo colección. El bookkeeping del GC (el índice de objetos jóvenes, que es un `Mutex`) resultó **diferible**: se drena en un único lugar de toda la VM, la entrada al GC, así que la ventana entre el bump y el registro no contiene ninguna colección *por construcción*; el código nativo anota `(offset, size)` en un array plano y el trampolín lo replica al volver" },
      { hz: "C", txt: "**✅ Llamadas por inlining** — el último bloqueante: `invokespecial` refusaba 379 métodos. Se eligió inlining sobre llamada nativa (sin ABI, sin unwind info); el costo es el deopt, resuelto con **virtual frames**. La clave fue que **el intérprete ya tenía la convención necesaria**: en un invoke el pc del caller queda *apuntando al invoke* y es el `return` del callee el que lo avanza — así que un deopt desde código inlineado se reconstruye como «caller en el pc del invoke con los argumentos ya popeados + callee en el pc del deopt», y lo termina la maquinaria existente **sin ningún caso especial**. La recursión se corta por **identidad de método**, no por profundidad (atrapa `f→g→f`, que ningún límite de profundidad reconocería), y un callee con rama hacia atrás se rechaza de plano: solo los headers de la raíz tienen poll, así que un bucle inlineado sería código del que no se puede salir" },
      { hz: "C", txt: "**✅ Merge del mapa de tipos** — un slot que llegaba `Int` por una arista y `Reference` por otra (la forma corriente de javac cuando el slot está *muerto* ahí) descalificaba el método entero. Ahora el retículo tiene un elemento de **conflicto**: no descalifica, y lo que prueba que el slot está muerto **no es una suposición sobre javac sino el propio código** — toda lectura pide `Int` o `Reference`, así que un slot en conflicto **falla toda lectura**; el rechazo *es* la prueba. En un resume ese slot simplemente **no se escribe de vuelta**: el frame conserva su `Value` anterior, que es seguro para el GC porque no puede producir ninguna de las dos corrupciones (un offset marcado `Int`, un int marcado `Reference`). Costo documentado: una referencia vieja puede quedar viva de más — una fuga menor, nunca corrupción. Y donde no podía *probar* la deadness declinó asumirla: un cuerpo con exception handlers no recibe conflictos, porque las aristas hacia un handler son justamente las que el recorrido no sigue" },
      { hz: "C", txt: "**Con eso `BmField` compila: 98.7 ms → 1.8 ms = 55×** — el patrón `new X(…)` en bucle con campos, o sea código Java completamente corriente, y el último de los cinco workloads que el JIT nunca había podido tocar. Se promovió `BmArray` a control pinneado para que el harness conserve dos brazos de efecto-cero contados. En el mismo paso, el riesgo que el anterior había dejado anotado —un `resume_state` que devolviera `None` tras haber corrido código nativo **reiniciaría el método en silencio**, re-aplicando escrituras— pasó de *mitigado con un `debug_assert`* a **estructuralmente imposible**: una compilación cuyos sitios de resume no sean reconstruibles **no se instala**" },
      { hz: "C", txt: "**Censo: 21% → ~57%** (152 → **444 de 785 métodos**), con los rechazos por `invokespecial` cayendo de **379 a 3**. Rendimiento sobre bucles: **~120×** (`BmLoop` 637 ms → 5.3 ms), y ~98× / ~70× en workloads de array y de campos. La disciplina de *romper el código a propósito para verificar que el test detecta* se volvió rutina: en el paso de inlining se sabotearon cinco cosas distintas y **el primer juego de tests no atrapaba ninguna** — dos tests nuevos y uno reformado existen exactamente por eso" },
      { hz: "C", txt: "**✅ Subconjunto ensanchado (2026-08-21)** — cinco frentes: **tipos anchos** (`long`, `float`/`double`; obligó a rediseñar el protocolo de retorno, que gastaba la mitad alta de RAX en el status), **arrays** en el fast path de Eden, **referencias de lectura** (`getfield` de ref, `volatile`, `checkcast` por clase exacta o deopt), **llamadas** (inline caching + polls en bucles inlineados) y **control excepcional** (`athrow`/monitores como deopt: lo que carga el peso es el `decode`, porque ambos son `Flow::Return` y el escaneo se detiene). Censo **11% → 68%** (697 de 1029 métodos)" },
      { hz: "C", txt: "**Una brecha ajena al JIT, hallada por auditoría**: `gc::compact` **no se ejecutaba ni una vez en toda la suite** (0 invocaciones en 1074 tests), así que el pinning de los mirrors —del que dependen **todos** los inmediatos horneados: `checkcast`, `instanceof`, `ldc Foo.class`, la dirección de `getstatic`— no tenía ninguna prueba. Borrar el conjunto `pinned` entero era invisible. Va el test que faltaba" },
      { hz: "C", txt: "**⬜ Lo que falta, medido**: **write barrier** (~76 métodos: `ReferenceWrite` 60 + `aastore` 11 + `aaload` 5 — el bloque más grande, y su forma probablemente ya existe: el *log diferido* de alocaciones es la misma figura; costo a aceptar: hoy el `DANGLING` se atribuye al heisenbug ajeno *porque* el nativo no puede escribir un puntero); **`ldc` de String** (46, bloqueado por una carencia de la VM — no hay tabla de interning, y por eso `\"a\" == \"a\"` da `false`, no-conformidad con JLS §3.10.5 que existe con o sin JIT); **accesos de array por ancho** (~20, mecánico); **cola larga de análisis** (~22, incluidas las conversiones saturantes que la JLS hace NaN-aware); **llamadas reales** compilado→compilado (lo irreducible: recursión — el más caro y el que menos rinde, va último); e **`invokedynamic`** (25, **estructuralmente cerrado**: acá un indy no es una llamada, el intérprete re-corre el bootstrap en cada ejecución, así que no hay `MethodId` ligado que darle al JIT)" },
      { hz: "C", txt: "**⬜ Deuda de medición**: no se mide nada desde los tipos anchos — el costo del inline caching, los `long` y los arrays es **desconocido** y una regresión pasaría inadvertida. Y el instrumento se degradó solo: `BmArray`/`BmField` **dejaron de ser controles** al volverse compilables, así que hay que **rediseñar el set de workloads** antes de que los números vuelvan a significar algo" },
    ] },
];

const FASE_G = [
  { id: "G0", name: "plain_data plano en el heap", hz: "C",
    exito: "un value type sin header conviviendo con los objetos normales.",
    items: [
      { hz: "C", txt: "Tipo sin header: `[field0 | field1]` vs objeto normal `[class_ptr | fields]`" },
      { hz: "C", txt: "El compilador (Fase B) lo marca; la VM lo guarda inline" },
    ] },
  { id: "G1", name: "Arrays flatteados + semántica de valor", hz: "C",
    exito: "comparar (medible) memoria/layout de `plain_data[]` vs un array de objetos.",
    items: [
      { hz: "C", txt: "`plain_data[]` contiguo: sin los N punteros, ni los N headers, ni N alocaciones" },
      { hz: "C", txt: "Igualdad por valor (comparar bytes), sin `null`, sin GC para ellos" },
    ] },
  { id: "G2", name: "Escape analysis lite", hz: "C",
    exito: "aplanar en *load time* cuando se prueba que un objeto no escapa.",
    items: [{ hz: "C", txt: "Versión declarativa/estática (el automático completo es del JIT, Hito F3)" }] },
];

const FASE_H = [
  { id: "H1", name: "KajiLibrary — la biblioteca estándar propia  ✅ tiers 0–5 cerrados", hz: "C",
    exito: "un programa real —con `StringBuilder`, colecciones y lambdas sobre sus interfaces funcionales— se **compila con el `javac` propio** y **corre en KajiJDK**, sin tocar el JDK de Temurin. La biblioteca se escribe a mano en `KajiLibrary/` (nuevo directorio en la raíz), en *tiers* por dependencia: **🟢** Java puro (autocontenido, se dogfoodea entero) · **🔌** necesita **intrínsecos del VM** (avanza con el track KajiJDK/runtime).",
    items: [
      { hz: "B", txt: "**✅ Tier 0 — Núcleo / raíces** (mayormente 🔌 nativo) — `Object`, `Class`, `String`, `System`, la jerarquía `Throwable`→`Exception`/`RuntimeException`/`Error` + subclases, `Enum`, `Integer`, `Math`, `Thread`, `java.lang.ref/*`, `java.io.PrintStream`. Escrito **de cero** (el `bootstrap/` es solo **referencia**). **Hecho:** compila y pasa el gate; `String` implementa `Comparable`+`CharSequence`; #6 (Object `super_class`) arreglado → `#0`. Correr pende del runtime" },
      { hz: "B", txt: "**✅ Tier 1 — Interfaces fundamentales** (🟢 Java puro) — `Comparable`, `Comparator`, `CharSequence`, `Iterable`, `Iterator`, `Runnable`, `AutoCloseable`, `Cloneable`, `Appendable`. Triviales y **base de casi todo lo demás**. **Cerrado**" },
      { hz: "B", txt: "**✅ Tier 2 — Wrappers + texto** (🟢) — **`StringBuilder`** (implementa `CharSequence`); `Number` + los 8 wrappers (`Integer`/`Long`/`Short`/`Byte`/`Character`/`Boolean`/`Float`/`Double`, todos `Comparable` con **bridge `compareTo(Object)`**). **Hecho** (`Void` incluido; correr el boxing/unboxing pende del runtime)" },
      { hz: "A", txt: "**✅ Tier 3 — Interfaces funcionales** `java.util.function` (🟢) — `Function`/`Consumer`/`Supplier`/`Predicate` **con** sus default de composición (`andThen`/`compose`/`identity`, `and`/`or`/`negate`). **Confirmado que el `javac` propio emite lambdas** (`invokedynamic` + `LambdaMetafactory`). **`java.util.function` completo**: + `BiFunction`/`BiConsumer`/`UnaryOperator`(extends Function)/`BinaryOperator`(extends BiFunction)" },
      { hz: "A", txt: "**✅ Tier 4 — Colecciones `java.util`** (🟢, gran superficie) — interfaces `Collection`/`List`/`Set`/`Queue`/`Map`(+`Map.Entry`) e implementaciones **`ArrayList implements List`/`HashMap implements Map`/`HashSet implements Set`** con `iterator()` real (retrofit hecho: #9/#10 arreglados; el iterador va en clase same-file por #13). Las utilidades `Objects`/`Optional`/`Arrays`/`Collections` pasaron a **H2**; los contenedores que faltaban (`Deque`/`LinkedList`/`ArrayDeque`/`TreeMap`/`TreeSet`/`Hashtable`/`LinkedHashMap`/…) se cerraron en **H11**" },
      { hz: "C", txt: "**✅ núcleo Tier 5 — I/O básica** (🔌 en los bordes) — framework `InputStream`/`OutputStream`/`Reader`/`Writer` + concretas Java-puro `ByteArray*`/`String*`/`BufferedReader` (dogfoodean `StringBuilder`). `PrintStream` real y `FileInputStream`/`OutputStream` arrastran intrínsecos del VM → track **KajiJDK** (runtime). La **capa de decoradores completa** (14 clases) llegó en **H11**" },
    ] },
  { id: "H2", name: "Utilidades y conveniencia (`java.util`)  🚧 en curso", hz: "C",
    exito: "el esqueleto de tiers 0–5 se vuelve una biblioteca **usable**: las clases-pegamento que aparecen en casi todo el código real (utilidades estáticas y contenedores), todas **🟢 Java puro** y **desbloqueadas ya** (no dependen de los gaps del compilador #4–#10).",
    items: [
      { hz: "A", txt: "**✅ `java.util.Objects`** — utilidad estática (`requireNonNull`, `equals`, `hashCode`, `isNull`/`nonNull`, `toString`). Hecho (8 miembros). Pero cablear su `requireNonNull` en los `default` de Tier 3 **espera el fix de #11**: el javac no resuelve llamadas estáticas a `java.util` (ni `Objects`/`Arrays`/`Collections`)" },
      { hz: "A", txt: "**✅ `java.util.Optional<T>`** — contenedor sin-`null` (`of`/`empty`/`ofNullable`, `get`/`isPresent`/`isEmpty`/`orElse`/`orElseGet`, `map`/`filter`/`ifPresent`). Dogfoodeó genéricos + inferencia + lambdas sobre las interfaces funcionales. Sumó `NoSuchElementException`" },
      { hz: "A", txt: "**✅ `java.util.Arrays`** — 20 miembros: `toString`/`equals`/`fill`/`copyOf`/`hashCode` sobre `int`/`char`/`boolean`/`Object[]` + `sort` (insertion). Escrita; **consumirla** desde otras clases espera #11" },
      { hz: "C", txt: "**✅ `java.util.Collections`** — `swap`/`reverse`/`fill`/`sort`×2 (natural con type param acotado + por `Comparator`), sobre acceso indexado de `List`. Escrita; **consumo** espera #11 y que los concretos sean `List` (#9). Faltan `emptyList`/`unmodifiable*`/`binarySearch` (necesitan backing concreto o iteración)" },
      { hz: "A", txt: "**✅ Stragglers de tiers previos** — `Void` (T2), `Appendable` (T1) y `BiFunction`/`BiConsumer`/`UnaryOperator`/`BinaryOperator` (T3). `java.util.function` quedó **43/43** en H11" },
    ] },
  { id: "H3", name: "java.util.stream — pipelines funcionales  ✅ núcleo + Collectors (4 a 37 factories, contrato 81%)", hz: "C",
    exito: "el **pago** de Tier 3 (interfaces funcionales) + Tier 4 (colecciones): `coleccion.stream().filter(...).map(...).collect(...)`. Dogfoodea el compilador como nada — lambdas encadenadas, genéricos, method refs. **Eager primero** (cada op intermedia materializa en un backing interno; la terminal consume): correcto para streams finitos y mucho más simple que el lazy; el lazy real (`Spliterator` + pipeline diferido) es un tier avanzado. Se apoyó en los **retrofits de #9** (ya hechos: `ArrayList`/`HashMap`/`HashSet` son `List`/`Map`/`Set` reales con `iterator()`), así que `toList()` devuelve un `ArrayList` de verdad. Núcleo T1–T5 hecho; solo `collect()` + `Collection.stream()` esperan **#17** (override de variable de tipo a nivel método).",
    items: [
      { hz: "A", txt: "**✅ T1 — Substrato funcional** — las 6 especializaciones primitivas de `java.util.function` (`IntFunction`/`ToIntFunction`/`IntPredicate`/`IntConsumer`/`IntUnaryOperator`/`IntBinaryOperator`) + la interfaz `Collector<T,A,R>`" },
      { hz: "A", txt: "**✅ T2 — `Stream<T>` core (eager)** — intermedias `filter`/`map`/`flatMap`/`distinct`/`sorted`/`limit`/`skip`/`peek` + `mapToInt`/`mapToLong`/`mapToDouble`; terminales `forEach`/`reduce`/`count`/`toList`/`toArray`/`anyMatch`/`allMatch`/`noneMatch`/`findFirst`/`min`/`max`; factories `of`/`empty`. **`toList()` devuelve un `ArrayList` real.** `collect()` + `Collection.stream()` esperan **#17**" },
      { hz: "A", txt: "**✅ núcleo T3 — `Collectors`** — `toList`/`toSet`/`joining`/`counting` (+ el `Collector` concreto `CollectorImpl`), gate-clean. El `collect()` que los consume espera **#17** (override de variable de tipo a nivel método). Faltan `groupingBy`/`mapping`" },
      { hz: "A", txt: "**✅ T4 — Streams primitivos** — `IntStream`/`LongStream`/`DoubleStream` eager (`range`/`of`/`sum`/`average`/`map`/`filter`/`reduce`/`sorted`/`limit`/`skip`/… + `boxed()`) con `OptionalInt`/`OptionalLong`/`OptionalDouble`. `mapToObj` diferido (#7 / array genérico)" },
      { hz: "C", txt: "**✅ núcleo T5 — Avanzado** — `flatMap` (eager) + bridges primitivo↔objeto (`Stream.mapToInt`/`mapToLong`/`mapToDouble`, `*Stream.boxed()`). **Fuera de alcance del modelo eager** (piden rediseño lazy): `Spliterator`/lazy real, `iterate`/`generate` infinitos, paralelismo verdadero" },
    ] },
  { id: "H4", name: "java.time — fecha y hora como value types  ✅ COMPLETO (value types + offsets + formato + chrono + 2 calendarios)", hz: "C",
    exito: "el dogfood de las **clases inmutables**: cada tipo es `final` con campos `final`, y `plus`/`minus`/`with` devuelven instancias nuevas. Casi todo es aritmética pura — el único seam 🔌 es el reloj (`now()` → un `System.currentTimeMillis()` native). **Ahora COMPLETO más allá del núcleo ISO:** value types + **offsets** (`ZoneOffset`/`OffsetDateTime`/`OffsetTime`/`ZonedDateTime` con offset fijo, `ZoneId` abstracto) + **formato por patrón** (`DateTimeFormatter.ofPattern` + `format`) + **chrono** (`IsoChronology` + los calendarios **Minguo** y **ThaiBuddhist** —vía refactor de `ChronoLocalDate` a *default methods*, para que los `Date` hereden en vez de declarar—) + `InstantSource`. **Paquete principal `java.time` 19/19; gate 721 match, 0 mismatch, 0 extra, PASS**. **Y ahora los subpaquetes también cierran: `chrono` 21/21, `temporal` 16/16, `format` 8/8** —las tres interfaces `Chrono{LocalDateTime,ZonedDateTime,Period}`, los calendarios **Japanese** e **Hijrah**, el `DateTimeFormatterBuilder`, `IsoFields` y `WeekFields`—, y **`zone` 5/5**, con lo que **H4 queda cerrado entero**. El **round-trip ISO** cierra (`parse` en los 9 value types), y todo lo demás está validado con **decenas de miles de checks** contra el JDK como oráculo. Findings del compilador destapados y resueltos: **#22** (resolución de métodos heredados —super/interfaz—, que habilitó el refactor chrono) y bridges covariantes verificados.",
    items: [
      { hz: "C", txt: "**✅ T1 — Abstracción `java.time.temporal`** — 7 interfaces (`TemporalAccessor`/`Temporal`/`TemporalField`/`TemporalUnit`/`TemporalAmount`/`TemporalQuery`/`TemporalAdjuster`) + enums `ChronoField`/`ChronoUnit`. El substrato sobre el que se montan los value types" },
      { hz: "C", txt: "**✅ T2 — Duración e instante** — `Duration` (seg+nanos, `TemporalAmount`) e `Instant` (epoch seg+nano, `Temporal`, `now()` 🔌 = `System.currentTimeMillis` native). Normalización de nanos con floor manual" },
      { hz: "C", txt: "**✅ T3 — Fecha local** — `LocalDate`: la conversión **epoch-day ↔ (año,mes,día)** del JDK (bisiestos, largos de mes) compilada y andando; `plusDays`/`plusMonths`/`getDayOfWeek`. + enums `Month`/`DayOfWeek`" },
      { hz: "C", txt: "**✅ T4 — Hora y fecha-hora** — `LocalTime` (h/m/s/nano, wraparound de día) y `LocalDateTime` (compone T3+T4, la aritmética de horas carga días en la fecha)" },
      { hz: "C", txt: "**✅ núcleo T5 — Períodos y parciales** — `Period` (`TemporalAmount`), `Year`/`YearMonth` (`Temporal`), `MonthDay` (`TemporalAccessor`)" },
      { hz: "C", txt: "**✅ JT1b — Conveniences + `parse`** — `get`/`plus`/`minus`/`with` genéricos y **`parse(CharSequence)` en los 9 value types** (round-trip ISO con `toString`; `Instant.parse` reusa `LocalDate`/`LocalTime`, `Duration.parse` con signo/fracción). `InstantSource` (Java 17) + `Clock implements InstantSource` → paquete principal 19/19" },
      { hz: "C", txt: "**✅ JT2 — Offsets** — `ZoneOffset`, `OffsetTime`, `OffsetDateTime`, `Clock`; validado `toEpochSecond`/`toInstant` vs JDK" },
      { hz: "C", txt: "**✅ JT3 — `DateTimeFormatter`** — `ofPattern` + `format(TemporalAccessor)`, motor de patrones (`u`/`y`·`M`[num/nombre]·`d`·`H`·`h`·`m`·`s`·`S`·`a`·`E`, literales/comillas) leyendo campos por `getLong(ChronoField)`; **48/48** vs JDK. + `LocalDate/Time/DateTime.format(fmt)`" },
      { hz: "C", txt: "**✅ JT4 — Zonas** — `ZoneId` (abstracto; `of()` de formas offset), `ZoneOffset extends ZoneId`, `ZonedDateTime`; **238/0** vs JDK. **Y el muro de tzdb cayó:** `java.time.zone` cierra **5/5** (`ZoneRules`, `ZoneOffsetTransition`, `ZoneOffsetTransitionRule`, `ZoneRulesProvider`, `ZoneRulesException`) apoyado en una tabla de datos propia (`TzData`) en vez de los ~600 KB de la IANA. Gate PASS; **lo que cubre esa tabla frente a la tzdb completa es lo que queda por medir**" },
      { hz: "C", txt: "**✅ JT5 — Chrono + calendarios** — `Chronology`/`AbstractChronology`/`IsoChronology`/`Era`/`IsoEra`/`ChronoLocalDate`; **`ThaiBuddhistDate`** (+543, **25504/0**) y **`MinguoDate`** (−1911, **21004/0**) vía refactor de `ChronoLocalDate` a *default methods*. **✅ Backlog cerrado:** las interfaces `Chrono{LocalDateTime,ZonedDateTime,Period}` y los calendarios **`Japanese`** (con sus eras Meiji→Reiwa) e **`Hijrah`** (tabla lunar) — `chrono` queda **21/21**" },
    ] },
  { id: "H5", name: "java.util.regex — motor de expresiones regulares  ✅ paridad exacta de API publica, y el motor honra los flags", hz: "C",
    exito: "un motor de regex de verdad, 100% Java puro — el dogfood más algorítmico de la fase: un **parser** de la sintaxis regex + una **máquina de matching por backtracking recursivo** sobre una **jerarquía de nodos con `match` polimórfico** (cada nodo hace `match(m, pos, seq)` y, al tener éxito, llama al nodo siguiente — *continuation-passing*, lo que habilita backtrackear en los cuantificadores). NFA con backtracking (no DFA), char-based. **Núcleo cerrado y VALIDADO**: como el track de biblioteca no ejecuta, se portó el mismo algoritmo a Java puro y se corrió con el JDK contra `java.util.regex` como oráculo → **138/138 checks** (matches + find-all con spans por grupo + reemplazos). Cero findings de correctitud del compilador. 18 clases (Pattern/Matcher/PatternSyntaxException + jerarquía de nodos + parser).",
    items: [
      { hz: "C", txt: "**✅ T1 — API pública** — `Pattern` (`compile`/`matcher`/`pattern`/`quote`/`matches`) + `Matcher` (`matches`/`find`/`lookingAt`/`group`/`start`/`end`/`groupCount`/`reset`) + `PatternSyntaxException`. La superficie sobre la que se apoya todo" },
      { hz: "C", txt: "**✅ T2 — Parser** — regex → árbol de nodos: literales, `.`, clases `[...]` (rangos, negación), cuantificadores greedy `*`/`+`/`?`/`{n,m}`, grupos `(...)`, alternación `\|`, anclas `^`/`$`, escapes `\\d`/`\\w`/`\\s` y sus negaciones" },
      { hz: "C", txt: "**✅ T3 — Motor de matching** — backtracking recursivo continuation-passing; `matches()`/`find()`/`lookingAt()`; `Quantifier` greedy con loop-back + `Prolog` (reset de contador para cuantificadores anidados) y guard de zero-width" },
      { hz: "C", txt: "**✅ T4 — Grupos y reemplazo** — grupos capturadores → `group(n)`/`start(n)`/`end(n)` (con guardar/restaurar en backtrack); `replaceAll`/`replaceFirst`/`quoteReplacement` con referencias `$n`" },
      { hz: "C", txt: "**✅ T5 (parcial) — Avanzado** — HECHO y validado: reluctant `*?`/`+?`/`{}?`, grupos non-capturing `(?:...)`, lookahead `(?=...)`/`(?!...)`, backreferences `\\1`–`\\9`. **⬜ Backlog:** possessive `*+`, lookbehind `(?<=...)`, grupos con nombre `(?<n>...)`, clases Unicode/POSIX `\\p{...}`, flags DOTALL/MULTILINE" },
    ] },
  { id: "H6", name: "java.util.Formatter / String.format — formato printf  ✅ COMPLETO (T1–T5)", hz: "C",
    exito: "el formato de texto estilo printf, 100% Java puro — mismo espíritu que regex: un **parser** de un mini-lenguaje (el format string `%[arg$][flags][width][.prec]conv`) + un **motor de conversión** que formatea cada argumento según su especificador. `Formatter` envuelve un `Appendable` y `String.format` es el atajo. **Cerrado T1–T5 (incl. el avanzado): 322 casos validados** contra `java.util.Formatter` del JDK. T5 completo: `%a`/hex-float + `Formattable`, `Double`/`Float.toString`, fecha/hora `%t`, `Locale`, e integración con `java.text` (`DecimalFormat`/`NumberFormat`/`MessageFormat`).",
    items: [
      { hz: "C", txt: "**✅ T1 — API + parser + conversiones base** — `java.util.Formatter` (`format`/`toString`/`out`/`flush`/`close`, `Closeable`/`Flushable`) + `String.format`; parser del format string → chunks literales + specifiers; conversiones `%s`/`%S`, `%%`, `%n`, `%b`, `%c`" },
      { hz: "C", txt: "**✅ T2 — Enteros** — `%d`/`%x`/`%X`/`%o` (radix vía `Integer`/`Long`), con flags `-` (left-justify) / `0` (zero-pad) / `+` / espacio / `(` (negativos) / `,` (grouping) y width" },
      { hz: "C", txt: "**✅ T3 — Punto flotante** — `%f`/`%e`/`%E`/`%g`/`%G` con precisión (default 6), redondeo, width y flags. La parte algorítmica dura: `double` → string decimal" },
      { hz: "C", txt: "**✅ T4 — Flags/width/precision + índice de argumento** — pulido transversal: `#` (alt-form), precisión en `%s`, índice `%n$` y relativo `%<`, edge cases de width/precision por conversión" },
      { hz: "C", txt: "**✅ T5 — Avanzado (hecho)** — `%a` (hex-float) + `Formattable`, `Double`/`Float.toString` (shortest-decimal), fecha/hora `%t`/`%T`, `Locale` (separadores), e integración con `java.text` (`DecimalFormat`/`NumberFormat`/`MessageFormat`)" },
    ] },
  { id: "H7", name: "Fuera de alcance por ahora  ⬜ backlog", hz: "C",
    exito: "lo que queda para **acotar** el scope, todo **acoplado al runtime** (espera el track KajiJDK): I/O de bajo nivel, concurrencia y reflexión. `java.text` (formato por patrón) es autocontenido y vive como el T5 diferido de H6.",
    items: [
      { hz: "C", txt: "**⬜ `java.nio`** — I/O de canales y buffers (🔌 descriptores de archivo → runtime)" },
      { hz: "C", txt: "**✅ `java.util.concurrent`** — salió del backlog y es hoy el hito **H9**: se pudo hacer **en Java puro** sobre los monitores que la VM ya tenía, con sólo dos natives nuevos" },
      { hz: "C", txt: "**🚧 reflexión** — la metadata la tiene el runtime (metaspace: field/method tables + anotaciones ya parseadas); faltaban los **natives que la puenteen a Java**. **✅ Hecho (natives + tests):** `Class.getName`/`getModifiers`/`getSuperclass`/`isInterface`/`isAssignableFrom` (leen el metaspace) + **`ldc` de class literals** (`X.class` → mirror) en el intérprete; de paso se destapó y arregló un bug latente de `load_class` (dedupeaba por *id* en vez de por *mirror*). **✅ `Class.getDeclaredFields()`** — **aloca un `java.lang.reflect.Field` por campo** desde el metaspace (clazz/name/type/modifiers/slot) y arma el `Field[]` (GC-safe: `malloc` no dispara GC), **`Field.get`** de campos de referencia. API `java.lang.reflect` completa a nivel de clase: `Modifier`/`Member`/`AccessibleObject`/`Field`/`Method`/`Constructor` (todas gate ok; `Method.invoke`/`Constructor.newInstance` + `getDeclaredMethods`/`Constructors` = natives pendientes). Tests: `class_reflection…`=11022, `field_reflection…`=206. **✅ Lectura de anotaciones en runtime** — el bloqueante del motor de validation, resuelto sin proxies dinámicos: natives `Reflect.hasConstraint/constraintLong/constraintString(Field, tipo, elemento)` que leen los `RuntimeVisibleAnnotations` **ya parseados** por el metaspace (accessors nuevos en `annotations.rs`). Test `reads_constraint_annotations_off_fields`=102101 (`@NotNull` + `@Size(min=2,max=10)`). **⬜ Falta (🔌 runtime):** `Field.set`, boxing de primitivos en `Field.get`, `Method.invoke`/`Constructor.newInstance`. El motor de Bean Validation (H8) ya no espera al runtime: lo bloquea el **#102** del compilador" },
    ] },
  { id: "H8", name: "jakarta.validation (JSR 303/380 — Bean Validation)  ✅ superficie COMPLETA (88/88)", hz: "C",
    exito: "una expansión **más allá de `java.base`**: la spec de Bean Validation (`jakarta.validation`, sucesor moderno de JSR 303/380). Como no está en el JDK, el **oráculo del gate es el jar** `jakarta.validation-api-3.1.1` (javap por classpath). Es todo **anotaciones + interfaces + reflexión** (98 clases). Prerequisito resuelto: **`java.lang.annotation.*` propio** — el `javac` propio compila el **bootstrap circular** de meta-anotaciones auto-referenciales (`Retention` anotado con `@Retention`) sin cambios. Findings del compilador destapados: **#100** (variable de tipo **acotada** erasa a `Object` en vez de su bound, JLS §4.6) y **#101** (referencia calificada a tipo anidado `Outer.Nested` no resuelve; con import cross-file emite `Object[]` — codegen silencioso incorrecto).",
    items: [
      { hz: "C", txt: "**✅ `java.lang.annotation.*`** — `Annotation`/`ElementType`/`RetentionPolicy`/`Retention`/`Target`/`Documented`/`Inherited`/`Repeatable`; **gate 26 match vs JDK**. Llena el hueco JLS §4" },
      { hz: "C", txt: "**✅ Base** — `Payload`, `Constraint`, `ConstraintValidator<A,T>`, `ConstraintValidatorContext`; anotaciones **runtime reales** (`RuntimeVisibleAnnotations` + `AnnotationDefault` emitidos)" },
      { hz: "C", txt: "**✅ 23 constraints** — `@NotNull`/`@Size`/`@Min`/`@Max`/`@Pattern`/`@Email`/`@NotBlank`/`@Digits`/`@DecimalMin`/… cada una con `message`/`groups`/`payload` + params + `@interface List` (repeatable). `Pattern` con enum anidado `Flag`. **Gate 111 match, 0 mismatch vs jar**" },
      { hz: "C", txt: "**✅ Core** — `Validator`/`ValidatorFactory`/`ValidatorContext`/`Validation`/`Configuration`/`ConstraintViolation`/`Path`(+`Node`+8 subnodos)/`MessageInterpolator`/`ClockProvider`/`TraversableResolver` + la jerarquía de excepciones completa. Truco clave: los genéricos **auto-referenciales** (`Configuration<T extends Configuration<T>>`, `ProviderSpecificBootstrap`, `spi.ValidationProvider`) se modelan **raw** con los métodos devolviendo el bound — así el descriptor calza con la erasure del JDK **sin allowlist**, esquivando el #100" },
      { hz: "C", txt: "**✅ Metadata (17/17)** + spi/bootstrap/executable/valueextraction/groups/constraintvalidation. **Superficie total: 88/88 clases, gate 357 match / 0 mismatch / 0 extra**, 6 divergencias allowlisteadas (4×#100 + 2×#101). **⬜ El motor** (un `Validator` que reflexione sobre el bean y produzca `ConstraintViolation`) queda pendiente: la lectura de anotaciones en runtime **ya está** (natives), pero lo bloquea el **#102** API" },
    ] },
  { id: "H9", name: "java.util.concurrent (JSR 166)  ✅ COMPLETO (C1–C5) y validado ejecutando", hz: "C",
    exito: "el paquete de concurrencia **entero en Java puro**, sin tocar el runtime salvo dos natives mínimos. La clave conceptual: en una VM cuyos hilos se intercalan **entre opcodes**, un atómico hecho con `synchronized` es *indistinguible* de uno lock-free con CAS — así que todo se construye sobre los monitores que la VM ya tiene (`synchronized`/`wait`/`notify`). Gate limpio y —lo que de verdad importa— **24 tests de comportamiento corriendo la biblioteca sobre el intérprete real** (`tests/concurrency_locks.rs`), no sólo forma de API. **Es el único paquete validado end-to-end ejecutando**, y por eso es el que destapa los bugs que el gate no ve: de los 62 tipos públicos del JDK están los que no piden `Unsafe`.",
    items: [
      { hz: "C", txt: "**✅ C1 — `atomic` (13) + `TimeUnit`** — `AtomicInteger`/`Long`/`Boolean`/`Reference`, los arrays, `Markable`/`Stamped` y los adders/accumulators. Lectura y escritura simples son *un* opcode (atómicas de por sí); sólo los read-modify-write toman el monitor. Los adders extienden `Number` directo (el JDK pasa por `Striped64`, que sin contención no aporta). `TimeUnit` usa el `cvt` con saturación del JDK. **La lección más cara del hito vive acá:** ese `cvt` estaba **validado 112/112 contra el JDK** y sin embargo **toda** conversión grueso→fino trapeaba en nuestra VM —leía `Long.MAX_VALUE` con `getfield` (#110)—; sólo `src == dst` esquivaba el bug, que es justo la forma que cubrió la validación. **Validar en Java real no prueba nada sobre nuestra VM**" },
      { hz: "C", txt: "**✅ C2 — `locks`** — `Lock`/`Condition`/`ReadWriteLock` + `ReentrantLock` y `ReentrantReadWriteLock` (con las clases anidadas `ReadLock`/`WriteLock` reales). Identidad de dueño por `Thread.currentThread()`; el `await` de las condiciones es **lost-wakeup-free** (entra al monitor de la condición *antes* de soltar el lock). El RW lock soporta reentrancia y el **downgrade** write→read. **Dos natives agregados al runtime**: `Thread.currentThread()` y `Object.wait(long)`" },
      { hz: "C", txt: "**✅ C3 — sincronizadores** — `CountDownLatch`, `Semaphore`, `CyclicBarrier`, `Exchanger`. La barrera modela las generaciones como un **contador**, con `brokenGen` marcando la que rompió un `reset`: así ven la excepción exactamente las partes que estaban esperando, y la barrera queda reusable" },
      { hz: "C", txt: "**✅ C4 — executors** — `Executor`/`Callable`/`Future`/`ExecutorService`, `FutureTask`, `ThreadPoolExecutor` (workers lazy hasta el core size; el último en salir marca *terminated*) y las factories de `Executors`. Es la pieza más didáctica: los hilos son caros, así que se crean una vez y se **reusan**, y la cola absorbe las ráfagas" },
      { hz: "C", txt: "**✅ C5 — colecciones concurrentes** — `ConcurrentHashMap`, `CopyOnWriteArrayList` (el array se reemplaza en cada escritura; los lectores no toman lock y el iterador ve un *snapshot*), `BlockingQueue` + `ArrayBlockingQueue`/`LinkedBlockingQueue`. El productor/consumidor sobre una cola **acotada** —donde ambos lados bloquean de verdad— es el test insignia" },
      { hz: "C", txt: "**✅ C6 — el resto de la cola** — `Phaser` (partes **dinámicas**: se registra una tercera *entre fases*, que es lo que un `CyclicBarrier` no puede; la terminación va en el bit de signo), `SynchronousQueue` (capacidad **cero** — no es un contenedor sino un punto de encuentro: `size()` siempre 0), `PriorityBlockingQueue`/`DelayQueue` (delegan el heap a nuestro `PriorityQueue` y sólo agregan monitor y espera), `ConcurrentLinkedQueue`/`Deque`, `CopyOnWriteArraySet`, `ThreadLocalRandom` y `ExecutorCompletionService` (entrega por orden de **terminación**, no de envío)" },
      { hz: "C", txt: "**⬜ Cola** — los `*FieldUpdater` (piden `Field.getInt/setInt` como native) y `LockSupport`/AQS (piden `park`/`unpark`: hay que coordinar con el track del runtime). **🔴 Bug del runtime, latente en código ya entregado:** un `wait(ms)` que **expira** queda en el *wait-set* del monitor —`expire_timed_block` sólo lo saca si el estado es `Waiting`, y un wait temporizado está en `TimedWaiting`—; después un `notify` drena esa entrada rancia y **bloquea al hilo que corre**. Afecta a `ArrayBlockingQueue.poll(timeout)`, `LinkedBlockingQueue` y `SynchronousQueue.poll(timeout)`. Fix de **una línea**, del track runtime; hay repro y un test `#[ignore]` que pasa a verde cuando se arregle" },
    ] },
  { id: "H10", name: "jakarta.persistence (JSR 338 — JPA 3.2)  ✅ COMPLETA — 203/203 clases (P1–P6)", hz: "C",
    exito: "la segunda expansión fuera de `java.base`, con el mismo molde que H8: **API pura, sin motor** (un proveedor JPA real es ORM + JDBC + una base de datos, inequívocamente fuera de alcance). Oráculo del gate: el jar `jakarta.persistence-api 3.2.0`. **Cerrada: 203/203 clases** (root 152 · criteria 26 · metamodel 17 · spi 8), gate **PASS**. El grueso dio **990 match / 0 mismatch / 0 extra** con 5 allowed; las 7 últimas entraron después, con un costo que vale la pena mirar de frente (abajo).",
    items: [
      { hz: "C", txt: "**✅ P1 — enums, excepciones y markers** — 20 enums (`AccessType`/`CascadeType`/`FetchType`/`LockModeType`…), la jerarquía de `PersistenceException` (11) y las interfaces de opción nuevas de 3.2 (`FindOption`/`LockOption`/`RefreshOption`) + `Timeout`" },
      { hz: "C", txt: "**✅ P2 — las 93 anotaciones de mapeo** (`@Entity`/`@Table`/`@Column`/`@Id`/`@OneToMany`/…) **con sus defaults reales**. Primero se compilaron las **fuentes** del `-sources.jar`, que era la única forma de tener los `default` exactos —`javap` a secas no los muestra, y una anotación cuyos defaults difieren no es la misma anotación—. Eso dejaba **165 archivos de Eclipse/Oracle** dentro del repositorio, con su cabecera `EPL-2.0 OR BSD-3-Clause`. **Después se regeneraron 126 desde el BINARIO**: los miembros salen de los descriptores y los `default` del atributo **`AnnotationDefault`**, que los tiene exactos — la misma relación que este proyecto ya tiene con el JDK en todos lados, leer la forma pública de un `.class` y reimplementarla. **Quedan 39 copiados**, con su licencia y atribución intactas: nueve son interfaces con métodos `default` y seis son clases con cuerpo, y **un cuerpo es bytecode, no fuente** — reconstruirlo pediría decompilar, y una decompilación aproximada reemplazaría comportamiento por una suposición. Los 24 restantes esperan fixes del compilador. **Todo declarado en `THIRD-PARTY.md`**, en la raíz del repositorio" },
      { hz: "C", txt: "**Lo que el gate no podía ver, y por eso se midió aparte.** Un `default` **no está en el descriptor**, así que la comparación de firmas da verde aunque los valores desaparezcan — y en la primera pasada de la regeneración **se perdieron 22 en silencio**. `tools/check_jpa_defaults.py` los compara uno por uno contra el jar: **197 idénticos, 0 distintos**. De paso destapó el finding **#127**: un `default` **negativo** o con **valor de anotación** no se emite, así que `int secondPrecision() default -1;` compila sin una queja y el `.class` sale sin el atributo" },
      { hz: "C", txt: "**✅ P3/P4 — core y metamodel** — `EntityManager` completo (64 métodos), `EntityManagerFactory` (21), `Query`/`TypedQuery`, `Persistence`, `Cache`, `EntityGraph`, `Tuple`… y el paquete `metamodel` (17). Los mismatches iniciales resultaron **artefactos del orden de compilación**, no bugs: recompilando con todas las dependencias presentes dan PASS limpio" },
      { hz: "C", txt: "**✅ P5/P6 — criteria y spi** — destrabados por la técnica **`cppjar-minus-self`**: extraer el jar a un **directorio** (el finder lee dirs, no jars) y compilar cada fuente con ese classpath **habiendo borrado su propio `.class`** —lo segundo evita la trampa de perf del #19—. Así los ciclos `metamodel`↔`criteria`↔`EntityManager` resuelven contra las clases reales **sin ordenar nada**. La lección general: un mismatch `→Object` suele ser un **artefacto de orden**, no un bug; siempre hacer un pase final con todas las deps antes de juzgar" },
      { hz: "C", txt: "**✅ Las 7 últimas — cerradas sin tocar el compilador.** Lo que las destrabó fue una observación sobre el gate, no un fix: **exige subconjunto, no igualdad**, así que omitiendo *el miembro* que no compila entra *la clase* entera, y el hueco queda **declarado** en vez de escondido. `SetJoin`/`CollectionJoin`/`ListJoin`/`MapJoin` omiten su `getModel()` covariante (#123) —que igual **heredan** de `PluralJoin`: la única pérdida real es el retorno covariante, que obliga a castear— y `MapJoin` también `entry()` (#101). `CriteriaBuilder` omite 5 métodos por dependencias de **otros módulos** (`java.sql`, `java.math`). `PersistenceProviderResolverHolder` es la única con **cuerpo propio**: la de referencia usa `ServiceLoader` + `WeakReference` por class loader + `java.util.logging`, nada de lo cual existe; como tampoco hay proveedor que descubrir, el resolver por defecto devuelve **lista vacía** —honesto en vez de simulado— y `setPersistenceProviderResolver` sigue funcionando, que es el punto de la indirección" },
      { hz: "C", txt: "**⚠ La deuda que el verde tapa** — `CriteriaBuilder` gatea con **14 entradas de allowlist, todas del #100**: una variable de tipo **acotada** erasa a `Object` en vez de a su cota (`N extends Number` → `Number`, `Y extends Comparable` → `Comparable`, `C extends Collection`, `M extends Map`). La fuente declara las cotas bien; lo que sale mal es el **descriptor emitido**, así que son 14 métodos que **no linkearían** contra un llamador real. Hoy no molesta —JPA es *gate-only*, nunca ejecuta— y por eso mismo conviene tenerlo escrito: es exactamente el tipo de deuda que un gate en verde disimula" },
    ] },
  { id: "H11", name: "Cierre de superficie de `java.base` — ~200 clases en paralelo  ✅ lote grande", hz: "C",
    exito: "el hito **menos conceptual y más de kilometraje**: llenar los huecos que quedaban en los paquetes ya abiertos, hechos por una **flota de agentes en paralelo** (6 + 4 relanzados), cada uno dueño de un paquete. **La biblioteca entera pasa el gate: 3074 match / 0 mismatch / 0 extra, 47 allowed, PASS** sobre **681 clases públicas** (396 de `java.*` + 285 de `jakarta.*`) — la cobertura de los paquetes que tocamos subió de ~38% a ~68%. Lo interesante no fue el volumen sino **qué hizo falta para que el paralelismo rindiera**.",
    items: [
      { hz: "C", txt: "**✅ `java.util` (78/110)** — los **6 esqueletos abstractos** (`AbstractCollection`/`List`/`Set`/`Queue`/`SequentialList`/`Map`, que son justo los que generaban nuestras entradas de allowlist: al extenderlos, los métodos pasan a **heredarse** en vez de declararse, como en el JDK), las 10 interfaces del orden (`Sorted`/`Navigable`/`Sequenced`), y los contenedores que faltaban: `TreeMap`/`TreeSet` (**árbol rojo-negro de verdad**, con un `checkInvariants()` que devuelve la altura negra o −1), `LinkedList`/`ArrayDeque`, `PriorityQueue` (heap binario), `Hashtable` (**encadenamiento separado**, a propósito como contraste contra el **direccionamiento abierto** de nuestro `HashMap` — dos respuestas a la misma pregunta), `LinkedHashMap` (la tabla **más** una lista doblemente enlazada que pasa por los **mismos** objetos entry, con `accessOrder`+`removeEldestEntry`: el par que lo vuelve una caché LRU), `IdentityHashMap`, `WeakHashMap`, `EnumMap`/`EnumSet`, `BitSet`, y los codecs `Base64`/`HexFormat`/`UUID`" },
      { hz: "C", txt: "**✅ `java.io` (51/81)** — la jerarquía de excepciones, `DataInput`/`DataOutput` y **los 14 decoradores**, que es donde vive el algoritmo: el `fill()` real de `BufferedInputStream`, `PushbackReader`, `LineNumberReader`, `StreamTokenizer`… El patrón decorador es el contenido del paquete; lo que falta pide **descriptores de archivo** (🔌 runtime)" },
      { hz: "C", txt: "**✅ `java.util.function` 43/43 — COMPLETO**, y `java.lang` (82/107) con `StringBuffer`, `ThreadLocal`, un `StrictMath` acotado y la jerarquía entera de `Error`/`Exception`" },
      { hz: "C", txt: "**Lo que hizo funcionar la flota**, que es el aprendizaje transferible: (1) pasarle a cada agente el **manual de minas completo** —los workarounds numerados de los findings abiertos—, porque sin eso pisan todas; (2) **exigir validación de comportamiento**, no sólo gate: el agente de codecs corrió **86 773 comparaciones** contra el JDK y encontró un bug propio, el de `java.io` corrió 105/105 y encontró otro; (3) prohibirles tocar `src/`, commitear y editar el archivo de findings —que reporten y consolide uno solo—; (4) exigir que el algoritmo se porte **mecánicamente** al JDK para validar, así se prueba el código que se entrega y no una reescritura" },
      { hz: "C", txt: "**✅ `java.lang.constant` 11/11 — el paquete de los descriptores nominales.** `ClassDesc`, `MethodTypeDesc`, la familia `MethodHandleDesc`/`DirectMethodHandleDesc` (con su enum `Kind`), `DynamicConstantDesc`, `DynamicCallSiteDesc` y los ~50 `CD_*` de `ConstantDescs`. Lo que define al paquete es lo que se le **quitó**: cada tipo declara un `resolveConstantDesc(MethodHandles.Lookup)` —el método que convierte la *descripción* en la cosa viva— y **todos están omitidos**, porque resolver es `java.lang.invoke`, o sea maquinaria de la VM y no biblioteca (`ConstantDesc` queda como interfaz **vacía**). Sobrevive la mitad útil: construir, comparar e imprimir un descriptor **sin resolver nada**, que es exactamente lo que lo hace usable en tiempo de compilación. Contenido algorítmico real: el parser de descriptores —que son **auto-delimitados**, y por eso una lista de parámetros no lleva separadores— y `invocationType()`, donde el descriptor de *lookup* y el tipo de *invocación* se separan: un método de instancia recibe el receptor como primer argumento, un constructor **devuelve** la clase que construye, y un accessor de campo no tiene descriptor propio sino uno sintetizado. **Cuidado que casi se pierde:** el campo `name` de `ConstantClassDesc` **lo lee el intérprete por nombre** para matchear labels de `switch` sobre enum, así que `descriptorString()` computa el descriptor **encima** del nombre binario en vez de reemplazarlo — el contrato quedó escrito en el archivo" },
      { hz: "C", txt: "**El patrón que destapó el lote, ya innegable.** Seis findings (**#108, #111, #114, #118, #119, #120**) comparten una sola raíz: **cuando la resolución falla, el compilador emite silencio en vez de un error** — la llamada desaparece y deja operandos colgados o la pila vacía. Y cuatro son puntos ciegos del **lector de classpath** (#104 `Exceptions`, #110 `ACC_STATIC`, #118 `ACC_VARARGS`, #120 la tabla de métodos del supertipo): lo que *escribimos* está bien, lo que *leemos* se interpreta mal. **Si el track del compilador arregla una sola cosa, que sea la guarda** — una llamada que no resuelve tiene que ser error de compilación: eso convierte seis bugs mudos en seis mensajes. Caso testigo: `WeakHashMap` **gatea limpio y no puede correr** (#119), sin workaround posible desde el fuente" },
    ] },
  { id: "H12", name: "java.util.zip — compresión de cero  ✅ COMPLETO (21/21) y validado ejecutando", hz: "C",
    exito: "el paquete que mejor separa **lo que el gate puede decir de lo que no**. Cierra 21/21 con **207 match / 0 mismatch / 0 extra y CERO entradas de allowlist** —raro para un paquete de este tamaño—, pero lo que de verdad lo sostiene es que se validó **ejecutando contra el JDK**: **747 checks** del inflater, **30** del deflater y **7** del contenedor. Esa validación encontró **dos bugs propios que el gate jamás habría visto**, que es exactamente el argumento de esta fase.",
    items: [
      { hz: "C", txt: "**✅ Checksums** — `Adler32` y `CRC32`/`CRC32C` son **dos respuestas distintas a la misma pregunta**, como `Hashtable` frente a nuestro `HashMap`. Adler son dos sumas corridas donde la segunda acumula la primera, y ese detalle es todo: una suma simple de bytes no ve un reordenamiento, pero sumar el total corrido hace que cada byte dependa de su **posición**. CRC es división polinómica, con una tabla que precomputa ocho pasos de bit por vez. Adler es mucho más barato y bastante más débil; zlib eligió ese trade a propósito" },
      { hz: "C", txt: "**✅ `Inflater` — DEFLATE (RFC 1951) en Java puro.** Tres cosas que cuesta acertar y fallan calladas: el stream se lee **LSB-first** pero un código Huffman se empaqueta **MSB-first**, y los dos órdenes conviven en el mismo stream; una *back-reference* puede **solapar** los bytes que todavía está produciendo, así que hay que copiar byte a byte —y eso es lo que hace que mil bytes iguales cuesten tres—; y como el input llega en pedazos, el decodificador es **resumible por checkpoint**: el cursor se guarda tras cada símbolo completo y en *underflow* rebobina ahí" },
      { hz: "C", txt: "**⚠ `Deflater` — lo que NO hace, escrito arriba del archivo.** Emite sólo bloques **STORED**: es DEFLATE válido y cualquier inflater del mundo lo lee, pero **no comprime**. Las dos mitades no son el mismo problema — decodificar es mecánico, el formato te dice qué hacer; **codificar es donde vive el algoritmo** (el buscador de coincidencias sobre 32 KB, elegir entre literal y back-reference, armar tablas de Huffman que le ganen a las fijas lo suficiente como para pagar transmitirlas). Con el encoder de bloques stored andando, los streams y el escritor de zip funcionan de punta a punta, y el *match finder* entra después detrás de la misma superficie" },
      { hz: "C", txt: "**✅ El contenedor ZIP, que no es DEFLATE.** Se conflacionan porque una extensión cubre las dos, pero DEFLATE es un stream de bytes sin noción de archivos y ZIP es el formato que carga muchos streams más un índice. Cada entrada lleva un **header local** delante y el archivo termina con un **directorio central** que repite toda la metadata: esa duplicación es el diseño —permite listar sin leer, y permite escribir sin poder hacer *seek*—, y es también por qué un zip truncado suele ser parcialmente recuperable. Las tres capas del sobre (DEFLATE · zlib · gzip) quedan separadas: `GZIPOutputStream` no comprime *más* que un `DeflaterOutputStream`, son los mismos bytes en otro envoltorio" },
      { hz: "C", txt: "**🔴 Los dos bugs que encontró la ejecución.** (1) `needsInput()` **mentía**: con bytes que no alcanzan para el símbolo siguiente devolvía `false`, el consumidor no alimentaba más y `inflate` devolvía 0 para siempre — el self-test se colgó. «Se quedó sin bits a mitad de símbolo» es un estado **distinto** de «no queda input». (2) El tamaño comprimido del descriptor salía corto **por 5 bytes exactos**, el bloque final de `def.finish()`, porque `deflate()` escribe directo a `out` sin pasar por el contador de posición. El lector del JDK lo cazó al instante (*«invalid entry compressed size, expected 15 but got 20»*); **el gate, nunca**" },
      { hz: "C", txt: "**⬜ `ZipFile` es un caso especial, y está escrito así.** Su razón de existir es el **acceso aleatorio** —saltar al final, leer el directorio, abrir sólo la entrada que se quiere— y no hay `java.io.File` ni descriptores de archivo. El constructor **falla a propósito** en vez de fingir que sostiene un archivo, mismo criterio que `Validation.buildDefaultValidatorFactory`. El parseo que necesitaría no falta: vive en `ZipInputStream`, donde sí puede correr" },
    ] },
  { id: "H13", name: "java.lang.invoke — donde la biblioteca se topa con la VM  ✅ 19/19 clases, 97.6% de miembros", hz: "C",
    exito: "**el hito cuyo número más engaña, y conviene decirlo de frente.** Cierra 19/19 con 172 match / 0 mismatch / 0 extra, pero **la mitad que importa no puede ser Java**: `MethodHandle.invoke` es *signature-polymorphic* y `Lookup` captura a su llamador caminando la pila. Lo que sí quedó real es el **tipo** de un handle y toda la familia de descriptores; el resto son declaraciones honestas que lanzan.",
    items: [
      { hz: "C", txt: "**✅ `MethodType` (33 miembros, cero divergencias) — el trabajo de verdad.** El tipo de un handle con todas sus derivaciones (`insert`/`drop`/`change`, `erase`, `generic`, `wrap`/`unwrap`), y su **`describeConstable()`**, que lo convierte en el `MethodTypeDesc` de **H11**: el mismo dato, una vez con clases cargadas y otra con nombres. Salió gratis un truco: el descriptor de una `Class` se deriva de **`getName()` sola** —el JDK ya devuelve `int` para un primitivo y `[Ljava.lang.String;` para un array, así que los tres casos caen del primer carácter—, lo que evitó necesitar `isPrimitive`/`isArray`, que nuestra `Class` no tiene" },
      { hz: "C", txt: "**⚠ Por qué `invoke` NO puede escribirse en Java.** Es **signature-polymorphic** (JLS §15.12.3): se declara una vez como `(Object...)Object`, pero el *call site* **no adapta sus argumentos** — el compilador emite un `invokevirtual` con el descriptor **del call site** y la VM lo linkea contra esa única declaración. Una llamada como `mh.invokeExact(1, \"x\")` compila a un método que no existe en ningún lado. La regla de resolución vive en la VM; por eso van `native`, igual que en el JDK" },
      { hz: "C", txt: "**✅ Lo que sí quedó completo** — `SerializedLambda` (la *receta* de un lambda: como su clase se genera en runtime no puede serializarse como sí mismo, así que viaja la receta), `MethodHandleInfo` (el gemelo cargado del enum `Kind` de H11), `SwitchPoint` y la familia `CallSite`, que son las tres respuestas a «¿puede cambiar el destino, y quién tiene que verlo?»: nunca, sí sin promesa de orden, o sí visible ya para todos" },
      { hz: "C", txt: "**✅ El caso feliz: `TypeDescriptor` sin allowlist.** Sus dos interfaces anidadas son *self-bounded* genéricas y daban **9 mismatches** por el #100 (variable acotada erasa a `Object`). Modelándolas **raw con los métodos devolviendo la cota** —el mismo truco que usó `jakarta.validation` con `Configuration<T extends Configuration<T>>`— el descriptor emitido queda **idéntico** al del JDK: nueve divergencias eliminadas, cero entradas de allowlist" },
      { hz: "C", txt: "**Verificado antes de escribir: publicar estos nombres no rompe los lambdas.** `LambdaMetafactory`, `StringConcatFactory` y `ConstantBootstraps` son declaraciones que la VM **nunca carga** — nuestro `invokedynamic` los reconoce **por nombre** y sintetiza el comportamiento en Rust (el de lambdas incluso *spinea* la clase implementadora). Se comprobó en el intérprete antes de crear los archivos" },
    ] },
  { id: "H14", name: "java.nio \u2014 buffers  \u2705 14/14 clases publicas, 82% de miembros", hz: "C",
    exito: "el paquete donde el conteo de clases mas enga\u00f1a: figuraba 21/83, y de las 62 faltantes **ninguna es publica** \u2014 son los `HeapByteBuffer`/`DirectIntBufferRS` internos de HotSpot. Medido por lo que importa, la API publica esta **completa** y el trabajo real fue de miembros: 34% a 82%.",
    items: [
      { hz: "B", txt: "**\u2705 `ByteBuffer` vuelve a ser abstracta.** Declaraba **concretos** 18 metodos que el JDK declara `abstract` (`putLong(int,long)`, `putInt`, los `getX`), mas `compact()`/`isDirect()` en los otros seis buffers: una subclase no estaba obligada a implementarlos y heredaba un cuerpo que no le corresponde. Se cerro moviendo las implementaciones a los `Heap*Buffer` que ya existian \u2014 el unico subtipo concreto \u2014, **sin introducir ninguna clase nueva**." },
      { hz: "B", txt: "**\u2705 `float`/`double` con aritmetica pura.** `Double.doubleToLongBits` es `native` y la VM no lo tiene ligado, asi que la conversion IEEE-754 se hace escalando por potencias de dos, que es exacto hasta los subnormales. Validado valor por valor contra el JDK: 20/20, incluidos los ceros con signo, los infinitos, `NaN` y subnormales de `float` y `double`." },
      { hz: "C", txt: "**\u2705 Un bug real que la superficie no mostraba:** `Float`/`DoubleBuffer.compareTo` ordenaba `NaN` **antes que todo**. Ahora es el orden total de `Float.compare`." },
      { hz: "C", txt: "**\u26a0 No se puede ejercitar en la VM.** El interprete no despacha `invokevirtual` a un metodo resuelto `abstract`, asi que `IntBuffer.wrap(a).get(1)` panica \u2014 y `get(int)` ya era abstracto antes de este trabajo. Llamando por el tipo concreto anda. Es el bloqueante n\u00ba 1 del paquete y es de la VM, no de la biblioteca." },
    ] },
  { id: "H15", name: "java.text \u2014 formato y colacion  \u2705 21/38 clases, colador y BreakIterator validados", hz: "C",
    exito: "el paquete que **si** se pudo validar ejecutando, y por eso el que mas confianza da: reglas de corte de texto y orden de colacion comprobadas contra su definicion, no contra una firma.",
    items: [
      { hz: "B", txt: "**\u2705 Colacion completa** \u2014 `Collator`, `RuleBasedCollator`, `CollationKey` y `CollationElementIterator`, con los tres niveles de fuerza y el parseo de reglas. Verificado en la VM: acentos Latin-1 como diferencia secundaria, caracteres no mapeados, y **las 64 combinaciones de `CollationKey` coinciden con `compare`**." },
      { hz: "B", txt: "**\u2705 `BreakIterator`** con las cuatro reglas (caracter, palabra, oracion, linea) y `previous`/`next(n)`/`following`/`isBoundary`/`preceding`, mas `ChoiceFormat` y `Annotation`. Como `java.lang.Character` no tiene `isLetter`/`isDigit`, el paquete trae su propia clasificacion por rangos, documentada." },
      { hz: "C", txt: "**\u26a0 `DateFormat`/`SimpleDateFormat` bloqueadas** \u2014 toda su API se apoya en `java.util.Date`, `Calendar` y `TimeZone`, que no existen: solo hay `java.time`. Es hueco de biblioteca, no de compilador, y define el proximo paso del paquete." },
    ] },
  { id: "H16", name: "java.lang.reflect \u2014 el modelo de reflexion  \u2705 29/33 clases publicas", hz: "C",
    exito: "cierra la jerarquia de tipos que estaba a medias: `Executable` pasa a ser la superclase real de `Method`/`Constructor`, y las interfaces de `Type` dejan de colgar en el aire.",
    items: [
      { hz: "B", txt: "**\u2705 13 clases nuevas** \u2014 `Array` (22/22 miembros, **identica al JDK** salvo el varargs), `Executable`, `AnnotatedElement`, `TypeVariable`, `GenericDeclaration`, `Parameter`, `RecordComponent`, `InvocationHandler` y los cuatro `Annotated*Type`. `Method` y `Constructor` se reparentaron a `Executable` sin mover su layout de campos." },
      { hz: "C", txt: "**\u26a0 `Proxy` queda afuera**: necesita generar clases en runtime y `java.lang.ClassLoader`, que no existe en la biblioteca. `InvocationHandler` si entra, porque como tipo ya es util." },
      { hz: "C", txt: "**\u26a0 El techo no es el compilador sino la VM**: casi toda la reflexion util necesita intrinsecos que no estan (`getDeclaredMethods`, los 20 de `Array`, el parseo de `Signature`), y `java.lang.Class` no declara `implements Type`, que es lo que sostiene todo el modelo generico." },
    ] },
  { id: "H17", name: "javax.lang.model \u2014 el modelo de APT  \u2705 42/42 clases publicas (3 + 18 + 21)", hz: "C",
    exito: "la mitad que le faltaba a APT. El hito B6 daba la fase 1 por hecha, pero el **modelo de lenguaje** sobre el que se apoya estaba casi vacio: `javax.lang.model.type` no existia y `element` tenia 4 de 21, con las cuatro que habia en estado de esqueleto.",
    items: [
      { hz: "B", txt: "**\u2705 `javax.lang.model.type` de cero: 18/18** \u2014 `TypeMirror`, `TypeKind`, `TypeVisitor` y las 15 subinterfaces. Es la dependencia dura de `element` (`Element.asType()` devuelve `TypeMirror`), asi que se escribio primero." },
      { hz: "B", txt: "**\u2705 `element` 4/21 a 21/21** \u2014 `ExecutableElement`, `VariableElement`, `PackageElement`, `ModuleElement` con sus 8 anidados, los visitors y los enums. Las cuatro preexistentes eran esqueletos: `TypeElement` tenia 2 de 11 miembros y `ElementKind` 13 de 21 constantes. **24 de 29 tipos con el conjunto de miembros identico al JDK.**" },
      { hz: "C", txt: "**Nota de construccion:** `TypeMirror` y `TypeVisitor` son mutuamente dependientes y el javac no resuelve cruzado en una sola invocacion, asi que el paquete necesita un bootstrap en dos fases \u2014 compilar `TypeMirror` sin `accept()`, despues las subinterfaces, y recompilarla completa. Queda anotado para quien lo rehaga desde cero." },
    ] },
  { id: "H18", name: "javax.tools \u2014 la API de herramientas  \u2705 16/17 clases publicas", hz: "C",
    exito: "el paquete que mejor muestra **cuanto depende la superficie de lo que hay debajo**: casi todo lo que quedo afuera lo bloqueo un tipo ausente de otro paquete, no un defecto propio.",
    items: [
      { hz: "B", txt: "**\u2705 14 clases nuevas** \u2014 `FileObject`, `JavaFileManager`, `StandardJavaFileManager`, `StandardLocation`, `JavaCompiler`, `DiagnosticListener`/`Collector`, los tres `Forwarding*`, `Tool`, `OptionChecker`, `ToolProvider` y `DocumentationTool`. `Diagnostic` y `JavaFileObject`, que eran cascaras de una linea, quedaron completas." },
      { hz: "C", txt: "**\u26a0 El inventario de lo que falta debajo**, que es el dato util: `java.net.URI` (se lleva `toUri` y `SimpleJavaFileObject` entera), `java.io.File` (4 metodos), `java.nio.file.Path` (8 + una anidada), `Charset` (2), `ClassLoader` (3) y `ServiceLoader` (2). Ninguno es del paquete." },
      { hz: "C", txt: "**Criterio que vale como precedente:** `SimpleJavaFileObject` se omitio **entera** en vez de dejar que javac le sintetizara un constructor sin argumentos que la API real no tiene. El gate acepta un subconjunto, pero da por buena una firma inventada \u2014 la ausencia es honesta, la firma inventada no." },
    ] },
];

const FASE_I = [
  { id: "I0", name: "JVMTI mínimo — los ganchos en la VM  ✅ logrado", hz: "A",
    exito: "**logrado** — un agente pide un breakpoint y recibe el evento en medio de la ejecución, sin que la VM pague nada cuando no hay agente atacheado. Módulo `jvmti.rs` (`JvmtiAgent`/`JvmtiEnv`/`BreakpointTable`/`Capabilities`) + `attach_agent`/`fire_debug_events` en la `JVM`, con el gancho al tope de `step()`; 3 tests (incl. uno de integración: un agente pone un breakpoint, corre `add(3,4)` en la JVM propia, recibe el evento y lee el local 0).",
    items: [
      { hz: "A", txt: "**`trait JvmtiAgent`** (callbacks *push*: `breakpoint`/`single_step`/…) + **`JvmtiEnv<'a>`** — el handle **acotado** que el callback usa para volver a entrar a la VM (introspección + control). La lectura casi ya está: `frames()`/`pc()`/`locals()`/`thread_views()` del intérprete mapean 1:1 a las funciones de introspección de JVMTI" },
      { hz: "A", txt: "**Ganarle al borrow checker sin `unsafe`**: la VM **posee** al agente, pero al disparar un evento **destructura `self` en campos disjuntos** (`agent` ⊥ los campos de `env`). Acotar el `env` **es** lo que lo hace posible — si fuera `&mut JVM` entero no compilaría" },
      { hz: "A", txt: "Hooks de **`breakpoint` + `single_step`** en `step()`, una `BreakpointTable`, y `Capabilities` con **fast-path** (`if !caps.any()` al frente → cero overhead: «pagás por lo que activás», el modelo de JVMTI)" },
    ] },
  { id: "I1", name: "`jdb` propio — front-end interactivo  ✅ logrado", hz: "A",
    exito: "**logrado** — un binario `jdb` (in-process, sobre JVMTI de I0) frena la ejecución y abre un prompt: `step`/`cont`, `break`/`clear <pc>`, `where`, `locals`, `print <slot>`, `help`, `quit`. Verificado de punta a punta: `jdb Add.class add 3 4` frena en `add@0`, `where` muestra el nombre del método, `locals` da los args, `step` avanza, `cont` completa.",
    items: [
      { hz: "A", txt: "**✅** Depurador CLI **in-process** (el prompt corre *dentro* del callback del agente, inspeccionando/controlando por el `JvmtiEnv` y devolviendo el control con `step`/`cont`). `command()` puro/testeable. El `JvmtiEnv` ganó lectura del *Method Area* (`method_name`/`code`) para mostrar nombres. Cola: `stop at C.m` por nombre, `list`/disassembly, `print` por nombre de variable (con la `LocalVariableTable`)" },
    ] },
  { id: "I2", name: "Más eventos JVMTI  ✅ logrado (incl. field watchpoints)", hz: "A",
    exito: "trazar entradas/salidas de método, atrapar excepciones y **vigilar campos**.",
    items: [
      { hz: "A", txt: "**✅ `method_entry`/`method_exit`/`exception`** — enganchados en los *chokepoints* centrales (`push_frame_locked` / `pop_frame` / `unwind_with`), con el mismo destructure disjunto y su *capability* que gatea el overhead; entry no dispara para el método de entrada ni `<clinit>`, exit salta sintéticos, exception cubre el `throw` explícito y las implícitas del VM. 2 tests de integración (trace `outer→inner`; `ArithmeticException` atrapada)" },
      { hz: "A", txt: "**✅ Field watchpoints** (`field_access`/`field_modification`) — el gancho `fire_field_watchpoint` al tope de `step()` decodifica el opcode de campo (`getfield`/`putfield`/`getstatic`/`putstatic`) y resuelve el *fieldref* **read-only** (`cf.fieldref_target`), leyendo el objeto/valor de la pila —**sin tocar los handlers de opcode**, que otra sesión edita—. `FieldWatchTable` en el `env`. 2 tests de integración (un agente vigila `Watched.value`, corre `run()` y recibe la modificación con el nuevo valor y el acceso; sin watch, cero eventos). Cableado por JDWP en I5 (validado con `jdb watch`)" },
    ] },
  { id: "I3", name: "JDWP — el protocolo de cable  ✅ logrado", hz: "C",
    exito: "**logrado** — un cliente JDWP **externo** (otro proceso) habla con nuestra VM por el protocolo estándar sobre un socket TCP. Verificado end-to-end: un cliente en Python attachea a `jvm-jdwp java/Add.class add 3 4`, hace el handshake, pide un `EventRequest` de single-step, y recibe los **4 Composite events** (uno por opcode de `add`) frenando y reanudando por el cable, hasta `Some(Int(7))`. 12 tests del módulo `jdwp` + 4 del `bridge` + 3 del binario.",
    items: [
      { hz: "C", txt: "**✅ Codec** (`jdwp.rs`, puro/sin I/O): handshake de 14 bytes, framing de paquetes (header de 11), y una serialización tipada (`Writer`/`Reader`: byte/int/long/id de 8 bytes/string big-endian) — se testea de punta a punta sin socket" },
      { hz: "C", txt: "**✅ Command handlers** (`JdwpSession`): `VirtualMachine.Version`/`IDSizes`/`Suspend`/`Resume` y `EventRequest.Set`/`Clear` (parsea `eventKind`/`suspendPolicy`/modificadores; del `LocationOnly` saca la posición y aloca el `requestID`). Puro: registra la intención, sin tocar la VM" },
      { hz: "C", txt: "**✅ Transporte**: `handshake`/`serve`/`listen` genéricos sobre `Read`+`Write` — la VM como servidor `dt_socket` (server=y). El core no sabe de sockets → se testea con un stream en memoria" },
      { hz: "C", txt: "**✅ Bridge JVMTI↔JDWP** (`bridge.rs`): un `JvmtiAgent` que **empuja** los eventos de la VM como *Composite packets* (↑) y **aplica** los `EventRequest` del cliente a la VM real por el `JvmtiEnv` (↓: un breakpoint pedido por cable **frena de verdad**). El mismo `serve_until_resume` corre en `vm_init` y tras cada evento; el truco del borrow otra vez (`session`/`stream` campos, `env` parámetro → disjuntos). Genérico sobre el transporte → 4 tests con un `MockStream` dúplex" },
      { hz: "C", txt: "**✅ `jvm-jdwp`**: el binario servidor — `bind`→`accept`→`handshake`→`attach_agent(JdwpBridge)`. Equivale a `-agentlib:jdwp=transport=dt_socket,server=y,suspend=y,address=<port>`. La VM arranca suspendida hasta el primer `Resume`. **⬜ Cola** (rumbo a I5): breakpoints por *ubicación* necesitan que el cliente resuelva el `methodID` real vía `ReferenceType.Methods`/`ClassesBySignature`, todavía no implementados — por eso el smoke test usó single-step" },
    ] },
  { id: "I4", name: "JDI — la API cliente  ✅ logrado", hz: "C",
    exito: "**logrado** — el front-end habla con la VM por *mirrors* tipados (`Vm`/`Location`/`Event`) en vez de bytes de protocolo, y un cliente propio (`jdi-attach`) maneja la ejecución por socket. Verificado **dos procesos por el cable, todo nuestro** (reemplaza al cliente de Python): `jdi-attach` ↔ `jvm-jdwp` sobre `Add.add(3,4)` → Version, `requestID`, los 4 eventos single-step y `Some(Int(7))`. 4 tests del módulo `jdi` + 3 del binario.",
    items: [
      { hz: "C", txt: "**✅ Módulo `jdi.rs`** — la API cliente de alto nivel, contraparte del *bridge* del lado del debugger. Depende **solo** del codec `jdwp` (ni VM ni JVMTI), así que un cliente y el `JdwpSession` servidor se prueban de punta a punta **en memoria** (un `Loopback` que es un servidor síncrono real). *Mirrors*: `Vm` (la conexión), `Location`, `EventRequest`, `Event` (ya decodificado). API: `attach` (handshake cliente), `version`, `resume`/`suspend`, `set_breakpoint`/`set_step_request`/`clear`, `next_event` (drena una cola: los eventos que llegan mientras se espera una respuesta **no se pierden**)" },
      { hz: "C", txt: "**✅** Queda explícito que el **jdb pragmático** (I1, in-process) y el **jdb fiel** (I3+I4, por protocolo) son **dos front-ends sobre el mismo JVMTI**. **⬜ Cola:** empaquetar un `jdb` interactivo completo sobre la JDI (hoy `jdi-attach` es el front-end de demostración)" },
    ] },
  { id: "I5", name: "Fidelidad del debugger  ✅ logrado (validado vs jdb real)", hz: "C",
    exito: "**logrado** — el **`jdb` de referencia de Temurin** (el mismo `com.sun.jdi` que usa IntelliJ Platform) attachea a nuestra VM por `dt_socket` **sin saber que es propia**, pone un breakpoint **por línea de fuente** y lo **frena**. Salida real de esa sesión: `VM Started: Add.add(), line=3 bci=0` · `Breakpoint hit: Add.add(), line=3 bci=0` · `where → [1] Add.add (Add.java:3)`. Es *differential testing* contra el debugger real como oráculo, el espejo de la «Fidelidad de `javac`» (B6) para el depurador.",
    items: [
      { hz: "C", txt: "**✅ I5a — inspección de pila y valores**: `VirtualMachine.AllThreads`, `ThreadReference.Name/Frames/FrameCount`, `StackFrame.GetValues`. Un cliente parado ve la **call stack** (frames como `frameID`=profundidad + `location`) y las **variables** (locales tageados; `float`/`double` por sus bits). Los sirve el *bridge* leyendo el `JvmtiEnv` que ya existía —sin tocar la inmutabilidad del metaspace—; un dispatcher `reply_for` los intercepta y delega el resto a la sesión pura" },
      { hz: "C", txt: "**✅ I5b — breakpoints por línea**: `AllClasses`/`ClassesBySignature`, `ReferenceType.Signature/Methods/SourceFile`, `Method.LineTable`. El cliente traduce `Add.java:3` a `(methodID, índice)`. **El muro y su salida**: listar métodos con su `MethodId` real exige resolverlos (`&mut metaspace`), pero en un callback el metaspace es inmutable → un **snapshot** capturado al attachear; como `resolve_method` **cachea**, el `methodID` del snapshot es *el mismo* que la VM usa al correr, por eso el breakpoint **frena de verdad**" },
      { hz: "C", txt: "**✅ Fidelidad para un JDI real** (lo que destrabó el attach de `jdb`, hallado iterando contra él): las variantes **`WithGeneric`** que emite el JDI de Java 5+ (`AllClassesWithGeneric`/`MethodsWithGeneric`/`SignatureWithGeneric`); la **negociación** (`Capabilities`/`CapabilitiesNew`, `ClassPaths`, thread groups, `Dispose`); el evento automático **`VMStart`** (para que fije su «hilo actual»); y los **ids no-cero** —JDWP reserva el `0` para *null*—: classID, threadID y **methodID = `MethodId + 1`** (sin esto, `<obsolete>`/`line=-1`/`Invalid frame location`)" },
      { hz: "C", txt: "**✅ `locals`** (`Method.VariableTable`) — con la VM parada, `jdb locals` muestra los argumentos y las variables **por nombre y valor** (`a = 3`, `sum = 7`). Lee la `LocalVariableTable` del `.class` (solo con `javac -g`) capturada en el snapshot; de paso destapó un bug real: `StackFrame.GetValues` lleva `threadID` **y** `frameID`, y solo leíamos el frame" },
      { hz: "C", txt: "**✅ Field watchpoints + inspección de objetos** — `jdb watch Watched.value` pone el watch por protocolo (`ReferenceType.Fields`, modificador `FieldOnly`, caps `canWatchField*`) y muestra el evento **completo**: `Field (Watched.value) is 0, will be 7`. El *«is 0»* (valor **actual**) sale de leer el objeto del heap: el `JvmtiEnv` ganó `heap` read-only, con `ObjectReference.ReferenceType/GetValues` (clase real del objeto por el mirror del header; valor del campo por su offset precomputado). Cada gap se halló iterando contra `jdb` (Superclass, Interfaces, las caps, el threadID de GetValues)" },
      { hz: "C", txt: "**✅ Sesión de `jdb` 100% limpia**: `threads` (`Group main: 1 main running`), `classes`, `stop at`/`cont`, `where`, `locals`, `watch` — todos sin errores. Lo único fuera de alcance es correr el IntelliJ **gráfico** (no headless acá); `jdb` es el mismo `com.sun.jdi` que hay debajo. **El hito I queda completo.** **838 tests** en verde" },
    ] },
];

const FASE_J = [
  { id: "J0", name: "`ModuleDescriptor` — leer el módulo  ✅ logrado", hz: "B",
    exito: "**logrado** — `jlink --describe-module` sobre el `module-info.class` real de `java.base` da **233 líneas idénticas** a `java --describe-module java.base` (el descriptor más complejo que existe: 100+ `exports`, cualificados, `uses`, `provides`). Normalizando sólo CRLF y el orden de las listas `to`, que el tool real emite en orden de iteración de un `Set` — no es orden especificado.",
    items: [
      { hz: "B", txt: "**`ModuleDescriptor`** en `parser/attributes/module.rs`: `Requires{transitive, static_phase, mandated}`, `Exports{package, to}`, `Provides{service, with}`, más `ModulePackages`/`ModuleMainClass`. Mismo corte que en `annotations.rs`: los `print` siguen sirviendo a `javap`, y al lado viven los **accessors** que las herramientas usan para *preguntarle* cosas al descriptor en vez de imprimirlo." },
      { hz: "B", txt: "**El círculo propio cierra**: `module-info.java` de nuestro `java.base` → compilado por `bin/javac-frozen.exe` → descrito por nuestro `jlink`." },
      { hz: "B", txt: "Fixture `java/kaji.sample/` — un módulo *exploded* de verdad (el **nombre del directorio es el nombre del módulo**) que ejercita **todas** las directivas: `open module`, `requires transitive`/`static`, `exports`/`opens` cualificados y no, `uses`, `provides … with` múltiple. Confirmado bit a bit contra `javap -v` (`ACC_OPEN`, `ACC_TRANSITIVE`, `ACC_STATIC_PHASE`)." }
    ] },
  { id: "J1", name: "Resolución del grafo (JPMS)  ✅ logrado", hz: "A",
    exito: "**logrado** — el conjunto resuelto coincide con el del `jlink` **real** en **68 de 68 raíces comparables** (las 2 restantes, `jdk.jlink`/`jdk.jpackage`, el tool de referencia las rechaza por venir sin `jmods`, no por diferencia de resolución) más 3 casos multi-raíz. Módulo `jvm/modules.rs`.",
    items: [
      { hz: "A", txt: "**Alcanzabilidad**: cierre transitivo sobre `requires` desde las raíces, con el módulo faltante nombrando **quién lo requería**. `requires static` **no se resuelve** — es una dependencia de compilación, y que falte en link/run es legal." },
      { hz: "A", txt: "**Legibilidad implícita**, que es lo jugoso: la relación *reads* es **más grande** que las aristas `requires` de las que sale. `requires transitive N` propaga — quien me lee, lee `N`. Verificado en el JDK real: `java.sql.rowset` **no requiere** `java.xml` ni `java.transaction.xa` pero **los lee**, porque `java.sql` los requiere `transitive`." },
      { hz: "A", txt: "**Split packages** como error de link: dos módulos resueltos con el mismo paquete harían ambigua la carga. Se chequea sobre el conjunto **resuelto** — dos módulos pueden estar bien por separado y no juntos en una imagen." },
      { hz: "A", txt: "`Configuration::reads(from,to)` y `exports_to(to,pkg,from)` — la base de J5: **legibilidad no es acceso**, el dueño además tiene que haber exportado el paquete." }
    ] },
  { id: "J2", name: "Imagen *exploded*  ✅ logrado", hz: "A",
    exito: "**logrado** — `jlink --module-path … --add-modules java.base --output img/` produce una imagen de **427 clases** y **un programa corre desde ella**: `ImgSmoke` (que usa `ArrayList`, `StringBuilder` y `String`) devuelve 49, y `Add.add(3,4)` devuelve 7, con el bootclasspath apuntando a `img/modules/java.base`. Ese `java.base` es **el nuestro**: una imagen de runtime producida por nuestro toolchain, con nuestra biblioteca, ejecutando nuestro bytecode.",
    items: [
      { hz: "A", txt: "Layout `<out>/modules/<módulo>/…` — la forma *exploded* del propio JDK — más el `release` en la raíz. El tool real mete el mismo contenido en el contenedor `lib/modules`: **mismo contenido, distinto contenedor**, que es exactamente la costura donde entran J3 y J4." },
      { hz: "A", txt: "**`release` verificado contra el real**: linkeando las mismas raíces del JDK, nuestra línea `MODULES=\"java.base java.logging\"` sale **idéntica** a la del `jlink` de Temurin. Decisión propia: **se omite `JAVA_VERSION`** cuando los módulos no traen versión, porque un `JAVA_VERSION=\"\"` *afirma* una versión vacía en vez de admitir que no hay." },
      { hz: "A", txt: "El descriptor de nuestro `java.base` vive ahora en `KajiLibrary/module-info.java`, junto a la biblioteca que describe; `run-headless --boot <dir>` es lo que permite comprobar que la imagen sirve de runtime." },
      { hz: "A", txt: "*Gotcha del camino*: un module path copiado a medias (236 de 427 clases) se manifestó como `invokeinterface: could not resolve the receiver's class` — parecía un bug de la VM y era `ArrayList.class` que sencillamente no estaba en la imagen. Un recordatorio de que en una imagen **lo que falta no da «no encontrado», da errores raros río abajo**." }
    ] },
  { id: "J3", name: "`jimage` — el lector  ✅ logrado", hz: "C",
    exito: "**logrado** — los tres comandos verificados contra el `jimage` real sobre la imagen de 145 MB: `info` idéntico (10 campos), `list` idéntico (28256 líneas · 70 módulos · 28115 recursos), `list --verbose` idéntico (offset/size/compressed de las 28115), y `extract` con **55 archivos byte a byte idénticos** en un módulo completo más **28115 extraídos con 0 faltantes y 0 de tamaño distinto**. Módulo `jvm/jimage.rs`, binario `bin/jimage.rs`.",
    items: [
      { hz: "C", txt: "**Dos endianness en un mismo archivo**: la cabecera son 7 `u32` **little-endian** (a diferencia del `.class`, que es big-endian), pero los valores de atributo dentro de `locations` son **big-endian**. Leer una mitad con el criterio de la otra produce basura que *parece* plausible — el tipo de error que no se cae, se desvía." },
      { hz: "C", txt: "**`locations`**: cada atributo es un tag `(kind << 3) | (len-1)` seguido de `len` bytes. **`strings`**: cadenas NUL-terminadas, y los nombres se guardan **partidos en cuatro** (módulo · parent · base · extensión). Compartir esos pedazos es lo que hace que 30473 nombres entren en 680 KB." },
      { hz: "C", txt: "**El orden de los módulos no es por nombre sino por directorio.** La referencia pone `java.management.rmi` antes que `java.management`; son seis pares y todos del mismo tipo. La clave es `nombre/`: comparando `java.management/` con `java.management.rmi/`, en esa posición hay `/` (47) contra `.` (46), y `.` es menor — así que el módulo más largo va primero. Ordenar por el nombre pelado da exactamente el orden inverso en esos seis." },
      { hz: "C", txt: "**Entradas *meta***: la imagen guarda 2358 entradas (70 módulos + ~2288 paquetes) que se describen a sí misma y que `list` no muestra. Por eso `resource_count` (30473) no es la cantidad de recursos (28115)." },
      { hz: "C", txt: "**Lo que NO está**: la compresión (todo recurso de un JDK de fábrica trae `compressed == 0`; una imagen con `jlink --compress` necesitaría el descompresor de J6 — el código *reporta* la entrada comprimida en vez de escribirla mal), y el **hash perfecto**, que está parseado pero todavía sin usar: `list`/`extract` recorren la tabla entera. La búsqueda O(1) por nombre es lo que J4 tiene que **construir**, y por eso el lector va primero." }
    ] },
  { id: "J4", name: "`jimage` — el escritor  ✅ logrado", hz: "C",
    exito: "**logrado** — `jlink --jimage` escribe un `lib/modules` de verdad y **el `jimage` de Temurin lo lee**: `info` da los 10 campos idénticos, `list` las 428 entradas, y `extract` devuelve los 428 archivos **byte a byte idénticos al origen**.",
    items: [
      { hz: "C", txt: "**Primero descubrir la función de hash, no asumirla**: FNV-1a sobre los bytes del nombre con el bit de signo limpiado, y `0x01000193` de multiplicador *y* de semilla inicial. La validación fue implementar la búsqueda y correrla contra la tabla ya construida de la imagen real: **30473 de 30473** entradas se encuentran a sí mismas, y un nombre inventado da `None` — confirmar el hit es lo que evita devolver el recurso de otro." },
      { hz: "C", txt: "Las dos primeras fallas fueron las reveladoras: `/modules` y `/packages`, las entradas **raíz**, no tienen módulo y guardan el nombre completo en `base`, así que componérselo duplicaba los separadores." },
      { hz: "C", txt: "**Construirlo es el problema inverso**: los buckets de un solo nombre se resuelven directo (índice negado) y los que colisionan necesitan una **semilla** que los desparrame a slots libres distintos; se resuelven los grandes primero, mientras queda espacio." },
      { hz: "C", txt: "**El bug de verdad del hito: la búsqueda de semilla no terminaba.** Con factor de carga 1.0 los últimos buckets casi no tienen slots libres, y para algunos conjuntos **ninguna** semilla los ubica — un test consumió **1181 segundos de CPU** antes de que se notara. El arreglo es acotar los intentos y **agrandar la tabla** si no cierra, que es exactamente para lo que `table_length` y `resource_count` son campos separados en la cabecera. El test que colgaba ahora resuelve en 0.01 s." }
    ] },
  { id: "J5", name: "Imagen ejecutable — JPMS en runtime  ✅ logrado", hz: "C",
    exito: "**La VM bootea desde un `jimage`.** Verificado con los directorios de boot **vacíos**, para que no queden dudas del origen: `ImgSmoke` (que usa `ArrayList`, `StringBuilder` y `String`) da **49** cargando sólo de nuestra imagen de 427 clases, y `Add.add(3,4)` da **7** cargando sólo de `lib/modules` **de Temurin** — 27084 clases, o sea nuestra VM leyendo la imagen real de un JDK de 145 MB con el lector de J3.",
    items: [
      { hz: "C", txt: "`ClassFile::from_bytes` separa el parseo de la lectura del archivo — el requisito de fondo: una clase que **no viene de un archivo**." },
      { hz: "C", txt: "`BootImage` indexa la imagen por **nombre binario**: la imagen se indexa por nombre completo (`/módulo/ruta`) pero un loader pide `java/lang/Object` y **no sabe en qué módulo está**, así que el mapa se arma una vez al abrir y cargar cuesta un lookup." },
      { hz: "C", txt: "La imagen es parte del loader **bootstrap**, consultada *después* de sus directorios (uno todavía puede sombrearla). Que la defina el mismo loader mantiene la identidad de runtime `(nombre, loader)` igual, se haya booteado de directorios o de imagen." },
      { hz: "C", txt: "**La accesibilidad, aplicada de verdad.** La regla pide **dos** condiciones y hacen falta las dos —el par que se confunde—: el módulo que llama tiene que **leer** al otro **y** el otro tiene que **exportar** el paquete. Leer no alcanza: un módulo puede leer a otro y seguir sin poder tocar sus paquetes internos." },
      { hz: "C", txt: "Para que sea chequeable la imagen **recuerda el dueño de cada clase**; sin eso una clase es sólo un nombre sin dueño. Los `module-info` van en un mapa aparte porque **todos comparten el mismo nombre binario** y en el de clases se pisaban entre sí — con dos módulos sobrevivía uno solo (bug encontrado con un test)." },
      { hz: "C", txt: "El chequeo se aplica en los **9 sitios de resolución**: `new`, `getstatic`/`putstatic`, los cuatro accesos a campos de instancia, los tres `invoke*` y el literal de clase de `ldc`. Aplicarlo en uno solo se leería como encapsulación dejando el resto abierto. En `invokevirtual`/`invokeinterface` se chequea la **referencia simbólica** —la clase que nombra el pool—, no la del receptor en runtime: es la *resolución*, no el despacho." },
      { hz: "C", txt: "**Verificado ejecutando**, con dos clases en la misma imagen y `kaji.app` requiriendo a `kaji.api` en ambos casos: `UsesApi` da **7** y `UsesSecret` muere con **`IllegalAccessError`**. La única diferencia es que un paquete está exportado y el otro no — y **las dos compilan sin queja**, porque nuestro javac no aplica JPMS: la regla es de runtime." },
      { hz: "C", txt: "Sin grafo de módulos **todo se permite**, con test propio: una VM booteada de directorios no tiene módulos, y negar accesos ahí rompería todo el uso normal." }
    ] },
  { id: "J6", name: "Plugins del pipeline  ✅ logrado", hz: "C",
    exito: "**logrado** — los cuatro plugins. `--strip-debug` baja la imagen de **828588 a 693048 bytes** (−16 %), y `--compress` escribe recursos comprimidos que **el `jimage` de Temurin descomprime**, devolviendo los 428 archivos byte a byte idénticos al origen.",
    items: [
      { hz: "C", txt: "**`--strip-debug`** es el que tiene sustancia: cirugía a nivel de bytes que saca cuatro atributos (`LineNumberTable`, `LocalVariableTable`, `LocalVariableTypeTable`, `SourceFile`) recalculando **cada longitud envolvente, incluida la de `Code`**, porque los de debug van anidados adentro. Verificado con el `javap` real sobre una clase extraída de la imagen: **0** atributos de debug, `Code` y `StackMapTable` intactos, y `ImgSmoke` **sigue dando 49** — quitar debug no cambia la semántica." },
      { hz: "C", txt: "Decisión documentada: **el constant pool se copia tal cual**, así que los `Utf8` de los atributos borrados quedan sin referencias (`#16 = Utf8 LineNumberTable`). Renumerar el pool costaría mucho más que los bytes que ahorra, y el tool real hace lo mismo; hay un test que lo fija como intencional." },
      { hz: "C", txt: "**`--compress`**: el formato del *recurso comprimido* se descifró contra la imagen real —`0xCAFEFAFA`, `content_size`, `uncompressed_size`, offset al nombre (`\"zip\"`), flags, `is_terminal`, y después un stream **zlib**—; la cuenta cerraba sola, 29 + 1032 = 1061, el `compressed` que reporta el índice. Se emite zlib con bloques DEFLATE ***stored***, que es exactamente el `zip-0` que el tool real documenta como «No compression»: un stream válido que cualquier inflater acepta, y que simplemente no encoge. El crecimiento da la cuenta exacta: +18410 = 428 × 43." },
      { hz: "C", txt: "Y cierra la limitación que J3 había anotado: `BootImage` **descomprime al servir una clase**, que es lo que hace booteable una imagen comprimida — sin eso la VM fallaba con `invokeinterface: could not resolve the receiver's class`, el mismo síntoma engañoso de un module path incompleto." },
      { hz: "C", txt: "**Y se escribió el inflate completo** (RFC 1951 + el envoltorio zlib): bloques *stored*, Huffman **fijo** y Huffman **dinámico**, con las tablas canónicas y las referencias LZ77. Con eso la VM lee imágenes comprimidas **de verdad**: extraer la imagen `zip-6` real del JDK da **7440 de 7440** recursos, 7438 byte a byte idénticos a la referencia. Los 2 restantes no son un fallo — `SystemModules$all` y `SystemModulesMap` son clases que **jlink genera por imagen** para codificar su grafo, y la nuestra tiene 2 módulos donde la de referencia tiene 70." },
      { hz: "C", txt: "Dos cosas del formato se equivocan fácil y fallan **en silencio**: el stream se lee **LSB-first** pero un código Huffman va empaquetado **MSB-first** —los dos órdenes conviven—, y una *back-reference* puede **solaparse** con lo que todavía está escribiendo (distancia menor que longitud), así que hay que copiar byte a byte; copiar el rango de una vez da otra cosa. Es justo lo que hace que una tirada `aaaa…` cueste tres bytes." },
      { hz: "C", txt: "**La consecuencia cruzada se cumplió**: `--strip-debug` borra justo lo que lee el `locals` de nuestro `jdb` (Fase I). Un hito que rompe a otro, igual que en el JDK real." }
    ] },
];

const TBL_HERRAMIENTAS = [
  ["`javap` (desensamblador)", "Se logra con el Hito A0", "Base"],
  ["`java` (lanzador de la JVM)", "Se logra durante la Fase A", "Base"],
  ["`javac` (compilador)", "Es toda la Fase B", "Base"],
  ["`jar` (empaquetador de `.class`)", "Tras la Fase B", "Avanzado"],
  ["`javadoc`, `keytool`", "Largo plazo", "Cumbre"],
];

// ---- Concurrencia: el "rascacielos" (lo que falta para multithread pro) ----
const TBL_CONCURRENCIA = [
  ["6 · `java.util.concurrent`", "✅🟡 núcleo + utils (H6)", "**Hecho:** **AQS** (`AbstractQueuedSynchronizer`) en **modo exclusivo y compartido** —base de casi todo—, `ReentrantLock` + `Condition`, `ReentrantReadWriteLock`, `Semaphore`, `CountDownLatch`, `CyclicBarrier`, `ArrayBlockingQueue`/`LinkedBlockingQueue`/`PriorityBlockingQueue`/`DelayQueue` (una/dos locks / min-heap / delay), el **framework `Executor`** (`ThreadPoolExecutor` + `submit`/`Future`/`FutureTask`/`Callable`, y un `ScheduledThreadPoolExecutor` con min-heap por deadline), un **`ConcurrentHashMap`** (encadenamiento + *lock striping*) y un **`CompletableFuture`** (`supplyAsync`/`thenApply`/`thenCompose`/`get`, `exceptionally` y `thenCombine`), todo **en Java** sobre AQS (o el monitor) y validado por el oráculo (`green≡os-gil≡os`). Motivaron agregar `Object.equals`, `java.util.function`/`Objects`, `Comparator`/`Comparable`/`Delayed` y el intrínseco `System.nanoTime`. **Falta (volumen):** deques, *resize*/lecturas lock-free del `ConcurrentHashMap`."],
  ["5 · Atómicos / CAS", "✅ **hecho (H5)**", "**Hecho:** `compareAndSwap` (la instrucción atómica raíz: `cas_u32`/`cas_u64` sobre el `AtomicRegion`, `AcqRel`) y `AtomicInteger`/`AtomicLong`/`AtomicReference` —la CAS de referencia pasa por el write barrier—. Verificado: `AtomicLong` 64-bit + `AtomicReference` identity-CAS → 202."],
  ["4 · Modelo de Memoria (JMM)", "✅🟡 relajado", "**Hecho (H4):** campos no-volatile **lock-free** (`Relaxed`), `volatile` = `Acquire`/`Release`, no-*tearing* de `long`/`double` (`AtomicU64`), Eden atómico **Miri-verificado**; el cache de condy es raíz de GC. *happens-before* vía los átomicos + el `RwLock`. Falta: semántica de `final`, `VarHandle`/CAS."],
  ["3 · API de `Thread`", "✅ **hecho (H1)**", "**Hecho:** `start`/`run`/`join`/`sleep`, `currentThread`, `yield`, nombre/id, `isAlive`, `getState()` (los seis estados; `ThreadStatus` pasó a cinco + `NEW` derivado, con `sleep_until`/`joining_on` plegados dentro), `Thread(Runnable)`, `start` dos veces → `IllegalThreadStateException`, e `interrupt`/`InterruptedException` que despierta `sleep`/`join`/`wait` (re-adquiere el monitor antes de lanzar en el `wait`; la carrera notify/interrupt la resuelve el GIL). **Periféricos hechos:** `ThreadLocal` (aislamiento por-hilo verificado), prioridad (rango `[1,10]`), daemon (regla de ciclo de vida). **Falta:** `UncaughtExceptionHandler`."],
  ["2 · Monitor intrínseco", "🟢 casi", "**Hecho:** `monitorenter`/`exit`, `synchronized` (bloques **y** métodos), reentrancia, `wait`/`notify`/`notifyAll`, `IllegalMonitorStateException`, `wait(timeout)` y monitor ***GC-safe*** (las claves se remapean por el *forward* del GC en minor/compact, así que se puede compactar con monitores tomados). **Falta:** interrupción del `wait` (llega con H1), biased/thin locks, detección de deadlock."],
  ["1 · Substrato de ejecución", "✅🟡 gran avance", "**Hecho (E1+E2):** green threads (default y visor) **y** hilos de SO reales (`std::thread` por `Thread.start()`); `park`/`unpark` reales. **Hecho (E3 — GIL removido):** en `JVM_THREADS=os` los opcodes **frame-local** (aritmética int/stack/ramas) corren **lock-free en paralelo** sobre un `RunningCtx` por-hilo con code cache; solo `SharedVm` se lockea; GC con **safepoint stop-the-world cooperativo**. El `Arc<Mutex<JVM>>` de toda-la-VM desapareció; `os-gil` queda como oráculo. **Ensanchado:** frame-local completo (W1), lecturas concurrentes bajo `RwLock` (W3), y **asignación lock-free** con Eden en un arena `UnsafeCell` **Miri-verificado** (W2). **JMM (H4) hecho:** modelo relajado — campos lock-free `Relaxed`, `volatile` `Acquire`/`Release`, Eden atómico. **Falta:** locks por-estructura finos, preempción."],
];

// Rascacielos en ASCII, con padding calculado en JS para que el borde derecho cierre.
const TOWER_W = 78;
const cBar = (l, r) => l + "─".repeat(TOWER_W - 2) + r;
function cRow(text) {
  const inner = TOWER_W - 4;
  const t = text.length > inner ? text.slice(0, inner) : text;
  return "│ " + t + " ".repeat(inner - t.length) + " │";
}
const TOWER_ASCII = [
  "        RASCACIELOS DE LA CONCURRENCIA   ·   construir hacia arriba ↑",
  "",
  cBar("┌", "┐"),
  cRow("[6]  java.util.concurrent   ·  núcleo + utils · falta vol.  HECHO"),
  cRow("     [x] AQS (excl + shared) · ReentrantLock/RWLock · Condition"),
  cRow("     [x] Semaphore · CountDownLatch · CyclicBarrier · ArrayBlockingQueue"),
  cRow("     [x] ThreadPoolExecutor · ScheduledThreadPoolExecutor · Future"),
  cRow("     [x] ConcurrentHashMap · Linked/Priority/DelayQueue · Completabl"),
  cRow("     [ ] deques · resize/lecturas lock-free CHM · locks finos"),
  cBar("├", "┤"),
  cRow("[5]  Atómicos / CAS                   ·  raíz lock-free        HECHO"),
  cRow("     [x] compareAndSwap · AtomicInteger / AtomicLong / Reference"),
  cBar("├", "┤"),
  cRow("[4]  Modelo de Memoria (JMM)          ·  relajado (H4)       ✅🟡"),
  cRow("     [x] volatile=Acq/Rel · no-volatile Relaxed · no-tearing · Eden atómico"),
  cBar("├", "┤"),
  cRow("[3]  API de Thread                    ·  control de hilos    HECHO"),
  cRow("     [x] start · run · join · sleep · currentThread · yield"),
  cRow("     [x] getState (6 estados) · isAlive · name/id · start x2"),
  cRow("     [x] interrupt despierta sleep/join/wait · daemon/prio/TLocal"),
  cBar("├", "┤"),
  cRow("[2]  Monitor intrínseco               ·  reentrante          PARCIAL"),
  cRow("     [x] monitorenter/exit · synchronized (bloques Y métodos)"),
  cRow("     [x] reentrancia · wait / notify / notifyAll · wait(timeout)"),
  cRow("     [ ] IllegalMonitorStateException · GC-safe · thin locks"),
  cBar("├", "┤"),
  cRow("[1]  Substrato de ejecución           ·  paralelismo real   ✅🟡"),
  cRow("     [x] green threads (cooperativo, default + visor)"),
  cRow("     [x] hilos de SO reales · park/unpark · os-gil (referencia)"),
  cRow("     [x] GIL removido: frame-local + lecturas + alloc lock-free"),
  cRow("     [x] W2 alloc: Eden en arena UnsafeCell (Miri-verificado)"),
  cRow("     [x] JMM (H4) relajado · [ ] locks por-estructura finos"),
  cBar("└", "┘"),
  "                     ▲ todo descansa en la planta 1 ▲",
].join("\n");

const ROUTE_ASCII = [
  "  ✔ HECHO             ✔ HECHO             ✔ HECHO             ✔ HECHO (subconj.)",
  " cerrar monitor      OS threads + GIL    API de Thread       sacar el GIL",
  " IMSE · wait(t)  ──► park/unpark     ──► interrupt       ──► frame-local sin lock",
  " · GC-safe           (os-gil = oráculo)  getState            → JMM·CAS·AQS ✔ · ACÁ: volumen j.u.c",
].join("\n");

// =============================================================
//  HTML
// =============================================================

const BRAND = "#B7410E";       // rust orange
const BRAND_DK = "#7A2D0A";    // rust oscuro
const INK = "#23201E";         // casi negro cálido

const html = `<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="utf-8">
<title>Roadmap del JDK en Rust</title>
<style>
  * { -webkit-print-color-adjust: exact !important; print-color-adjust: exact !important; }
  @page {
    size: A4;
    margin: 24mm 18mm 22mm 18mm;
    @top-left-corner { content: ""; background: ${BRAND_DK}; margin:0; padding:0; }
    @top-left {
      content: "JDK en Rust · Roadmap técnico";
      font-family: 'Segoe UI','Calibri',sans-serif; font-size: 9.5pt; font-weight: 600;
      color: #fff; background: ${BRAND_DK}; padding: 3.5mm 0 3.5mm 4mm; margin:0; vertical-align: middle;
    }
    @top-center { content: ""; background: ${BRAND_DK}; margin:0; padding:0; }
    @top-right {
      content: "Documento técnico";
      font-family: 'Segoe UI','Calibri',sans-serif; font-size: 9.5pt; font-style: italic;
      color: #F3D9C9; background: ${BRAND_DK}; padding: 3.5mm 4mm 3.5mm 0; margin:0; vertical-align: middle;
    }
    @top-right-corner { content: ""; background: ${BRAND_DK}; margin:0; padding:0; }
    @bottom-left { content: "${AUTOR}"; font-family:'Segoe UI',sans-serif; font-size: 8pt; color: #999; }
    @bottom-right { content: "Página " counter(page) " de " counter(pages); font-family:'Segoe UI',sans-serif; font-size: 8pt; color: #999; }
  }
  @page :first {
    margin: 0;
    @top-left-corner{content:"";background:transparent;} @top-left{content:"";background:transparent;}
    @top-center{content:"";background:transparent;} @top-right{content:"";background:transparent;}
    @top-right-corner{content:"";background:transparent;} @bottom-left{content:"";} @bottom-right{content:"";}
  }
  html, body { font-family:'Segoe UI','Calibri',sans-serif; font-size: 10.8pt; color: ${INK}; line-height: 1.45; margin:0; padding:0; }
  h1 { color: ${BRAND}; font-size: 21pt; margin: 1.3em 0 .45em; border-bottom: 2.5px solid ${BRAND}; padding-bottom:.18em; page-break-after: avoid; }
  h2 { color: ${BRAND_DK}; font-size: 14.5pt; margin: 1.1em 0 .35em; page-break-after: avoid; }
  h3 { color: ${INK}; font-size: 11.5pt; margin: .7em 0 .25em; page-break-after: avoid; }
  p { margin: .45em 0; }
  ul, ol { margin: .35em 0 .55em 1.15em; }
  li { margin: .14em 0; }
  code { font-family:'Consolas','Courier New',monospace; font-size: 9pt; background:#F3EEEA; padding: 1px 4px; border-radius: 3px; color:#7A2D0A; }
  pre { font-family:'Consolas','Courier New',monospace; font-size: 9pt; background:#FBF7F4; border-left: 3px solid ${BRAND}; padding:.7em 1em; margin:.6em 0; white-space: pre; overflow: hidden; page-break-inside: avoid; line-height: 1.3; }
  blockquote { margin:.6em 0; padding:.45em .9em; border-left: 4px solid ${BRAND}; background:#FBF4EF; color:#5b5550; font-style: italic; }
  table { border-collapse: collapse; width: 100%; margin:.5em 0 1em; page-break-inside: avoid; font-size: 9.8pt; }
  th { background:${INK}; color:#fff; padding:6px 9px; text-align:left; font-weight:600; border:1px solid ${INK}; border-bottom: 3px solid ${BRAND}; }
  td { padding:5px 9px; border:1px solid #E2DAD4; vertical-align: top; }
  tbody tr:nth-child(even) td { background:#FAF6F2; }

  .badge { display:inline-block; font-size:7.5pt; font-weight:700; padding:1px 7px; border-radius:10px; color:#fff; vertical-align: middle; letter-spacing:.3px; }
  .b-base { background:#2E7D32; }
  .b-adv  { background:#1565C0; }
  .b-sum  { background:#6A1B9A; }

  .hito { page-break-inside: avoid; margin: .2em 0 1em; padding-left:.2em; }
  .hito h3 { margin:.6em 0 .25em; font-size:11.5pt; color:${BRAND_DK}; }
  .hito-id { display:inline-block; background:${BRAND}; color:#fff; font-weight:700; font-size:9pt; padding:1px 7px; border-radius:4px; margin-right:4px; }
  .hito-items { list-style:none; margin:.2em 0 .3em; padding:0; }
  .hito-items li { margin:.2em 0; padding-left:.2em; }
  .exito { margin:.2em 0 .2em; font-size:10pt; color:#3a6b3a; background:#F1F7F1; border-radius:4px; padding:3px 9px; }
  .exito strong { color:#2E7D32; }

  .cover { height: 297mm; width: 210mm; margin:0; padding:0; display:flex; flex-direction:column; align-items:center; justify-content:center; page-break-after: always; position:relative; box-sizing:border-box; }
  .cover::before { content:""; position:absolute; top:0; left:0; right:0; height:20mm; background: linear-gradient(to right, ${BRAND_DK}, ${BRAND}); }
  .cover::after  { content:""; position:absolute; bottom:0; left:0; right:0; height:8mm; background: linear-gradient(to right, ${BRAND_DK}, ${BRAND}); }
  .cover-emblem { font-family:'Consolas',monospace; font-size:13pt; color:${BRAND}; letter-spacing:2px; margin-bottom:6mm; }
  .cover-sub { color:${BRAND_DK}; font-size:15pt; text-align:center; margin-bottom:10mm; letter-spacing:1px; }
  .cover-title { font-size:38pt; font-weight:800; text-align:center; color:${INK}; line-height:1.08; }
  .cover-tagline { color:${BRAND}; font-size:16pt; text-align:center; margin-top:7mm; font-style:italic; }
  .cover-rule { width:62mm; height:3px; background:${BRAND}; margin:7mm auto; }
  .cover-meta { position:absolute; bottom:18mm; left:0; right:0; text-align:center; color:#777; font-size:10pt; }
  .cover-meta strong { color:${BRAND_DK}; }

  .toc-list { list-style:none; padding:0; margin:.6em 0; }
  .toc-list li { display:flex; padding:4px 0; border-bottom:1px dotted #D8CFC8; font-size:10.5pt; }
  .toc-list li.l2 { padding-left:1.6em; color:#5b5550; }
  .toc-list .num { flex:1; }
  .toc-list .pg { color:${BRAND}; font-weight:600; }
  .legend { margin:.6em 0; font-size:10pt; }
  .legend .badge { margin-right:4px; }
  .section-break { page-break-before: always; }
</style>
</head>
<body>

<!-- Portada -->
<div class="cover">
  <div class="cover-emblem">&lt;/&gt; fn main()</div>
  <div class="cover-sub">Proyecto personal · Sistemas de bajo nivel</div>
  <div class="cover-rule"></div>
  <div class="cover-title">Construir un JDK<br>desde cero en Rust</div>
  <div class="cover-rule"></div>
  <div class="cover-tagline">Roadmap técnico por hitos</div>
  <div class="cover-meta">
    Autor: <strong>${AUTOR}</strong><br>
    Fecha: <strong>${FECHA}</strong>
  </div>
</div>

<!-- TOC -->
<div class="section-break">
  <h1>Contenido</h1>
  <ul class="toc-list">
    <li><span class="num">Introducción y alcance</span><span class="pg">3</span></li>
    <li class="l2"><span class="num">Los tres pilares</span><span class="pg">3</span></li>
    <li class="l2"><span class="num">El problema del bootstrap</span><span class="pg">3</span></li>
    <li class="l2"><span class="num">Orden de construcción</span><span class="pg">3</span></li>
    <li><span class="num">Fase A — La JVM (el runtime)</span><span class="pg">4</span></li>
    <li><span class="num">Concurrencia — ruta al multithread profesional</span><span class="pg">6</span></li>
    <li><span class="num">Fase B — El compilador (javac, en Rust)</span><span class="pg">8</span></li>
    <li><span class="num">Compilador — pasadas y estructuras internas</span><span class="pg">16</span></li>
    <li><span class="num">Fase C — Las bibliotecas</span><span class="pg">17</span></li>
    <li><span class="num">Fase D — Herramientas (la &ldquo;DK&rdquo;)</span><span class="pg">18</span></li>
    <li><span class="num">Fase E — Cerrar el círculo</span><span class="pg">18</span></li>
    <li><span class="num">Más allá del JDK — la plataforma</span><span class="pg">19</span></li>
    <li class="l2"><span class="num">Fase F — burst (optimizador)</span><span class="pg">19</span></li>
    <li class="l2"><span class="num">Fase G — plain_data / value types</span><span class="pg">20</span></li>
    <li><span class="num">Fase H — KajiLibrary (biblioteca dogfooded)</span><span class="pg">21</span></li>
    <li><span class="num">Fase I — Depurador (JVMTI / jdb)</span><span class="pg">26</span></li>
    <li><span class="num">Fase J — jlink (imagen de runtime)</span><span class="pg">28</span></li>
    <li><span class="num">Estado actual</span><span class="pg">30</span></li>
  </ul>
</div>

<!-- Introducción -->
<div class="section-break">
<h1>Introducción y alcance</h1>
<p>Este documento traza la ruta para construir un <strong>JDK educativo desde cero en Rust</strong>: no para competir con HotSpot, sino para <strong>cargar y ejecutar bytecode real</strong> y, eventualmente, compilar y correr código Java escrito y compilado con herramientas propias. El objetivo es doble: aprender sistemas de bajo nivel a fondo y completar un reto de ingeniería de gran calibre.</p>
<p>Un JDK no es una sola pieza. Son <strong>tres pilares</strong> que se sostienen entre sí:</p>

<h2>Los tres pilares</h2>
${table(["Pilar", "Qué hace", "Lenguaje"], TBL_PILARES)}

<h2>El problema del bootstrap</h2>
<p>Las bibliotecas necesitan al <strong>compilador</strong> (para compilarse) y a la <strong>JVM</strong> (para ejecutarse). Si el compilador se escribiera en Java, se necesitaría a sí mismo para compilarse — el clásico problema del huevo y la gallina.</p>
<blockquote>Decisión de diseño: el compilador se escribe <strong>en Rust</strong>, igual que la JVM. Así Rust compila al compilador, el compilador compila las bibliotecas, y la JVM las ejecuta. El ciclo queda roto.</blockquote>

<h2>Orden de construcción</h2>
<p>La <strong>JVM va primero</strong>, sin excepción: tanto la salida del compilador como las bibliotecas son inertes sin un motor que las ejecute, y no se puede ni <em>probar</em> que el compilador genera bytecode correcto sin algo que lo corra.</p>
<pre>A · JVM ──→ B · Compilador ──→ C · Bibliotecas ──→ E · Cerrar el círculo
(motor)     (.java → .class)    (en Java)           (todo junto)
   └── D · Herramientas se completan en el camino (javap, java, jar…)</pre>
<p class="legend">
  <strong>Horizontes:</strong>
  ${badge('B')} el núcleo, por aquí empezamos &nbsp;
  ${badge('A')} más duro, segunda pasada &nbsp;
  ${badge('C')} lo más difícil, pero vamos a llegar
</p>
<blockquote>Cómo leer el roadmap: es una <strong>ruta ordenada por hitos</strong>. Cada hito tiene un criterio de éxito medible y no se cierra hasta cumplirlo. El orden respeta las dependencias: no se puede el hito N sin el N-1.</blockquote>
</div>

<!-- Fase A -->
<div class="section-break">
<h1>Fase A — La JVM (el motor que ejecuta bytecode)</h1>
${FASE_A.map(hito).join('\n')}
</div>

<!-- Concurrencia (deep-dive de A6) -->
<div class="section-break">
<h1>Concurrencia — ruta al multithread profesional</h1>
<p>La ejecución concurrente (Hito A6) merece su propio mapa, porque es donde el proyecto apunta a <strong>multithread profesional</strong>. Cada planta se apoya en la de abajo: no hay <code>ReentrantLock</code> sin CAS, ni CAS útil sin un modelo de memoria, ni modelo de memoria que <em>importe</em> sin hilos reales que lo hagan necesario. <strong>El edificio está en pie hasta la planta 6</strong>: el substrato pasó de green threads a hilos de SO con paralelismo real, y <code>java.util.concurrent</code> —la cumbre— es hoy el hito <strong>H9</strong> de KajiLibrary. Lo que queda son terminaciones, no estructura: AQS como base común, los <em>fences</em> explícitos, la preempción.</p>
<p>Y hay un giro que este mapa no anticipaba. La planta 6 se dio por hecha <strong>sin</strong> construir la planta 5 debajo suyo <em>en el sentido clásico</em>: en una VM cuyos hilos se intercalan <strong>entre opcodes</strong>, un contador atómico hecho con <code>synchronized</code> es <em>observacionalmente indistinguible</em> de uno lock-free con CAS. Los atómicos existen y funcionan, pero se apoyan en el monitor de la planta 2, no en un CAS de hardware. La torre sigue siendo la ruta correcta hacia el paralelismo real; lo que muestra el atajo es que <strong>la semántica se puede tener antes que la implementación</strong>.</p>

<pre>${esc(TOWER_ASCII)}</pre>

<p class="legend"><strong>Estado:</strong> ✅ hecho &nbsp;&nbsp; 🟡 parcial &nbsp;&nbsp; ⬜ falta</p>
${table(["Planta", "Estado", "Qué falta para &ldquo;profesional&rdquo;"], TBL_CONCURRENCIA)}

<h2>Los dos cimientos reales</h2>
<p>De todo el edificio, <strong>dos primitivas</strong> sostienen el resto: <strong><code>park</code>/<code>unpark</code></strong> (planta 1) y <strong>CAS</strong> (planta 5). De esas dos se deduce <em>casi todo</em> lo de arriba — AQS, los locks, los atómicos, los pools. <strong>Las dos están</strong>, y el salto grande —cambiar el substrato de green threads a hilos de SO— ya se dio: hoy conviven los tres (verde cooperativo · SO con GIL · SO en paralelismo real). Lo que falta arriba es <em>reconstruir sobre ellas</em> lo que hoy descansa en el monitor.</p>

<h2>Ruta crítica</h2>
<pre>${esc(ROUTE_ASCII)}</pre>
<p>El camino canónico — el mismo que recorrieron CPython y las JVM tempranas — es <strong>GIL primero</strong> (hilos de SO con un lock global que serializa el intérprete: simple y correcto, pero todavía sin paralelismo real) y después <strong>sacar el GIL</strong> (locks finos por estructura + TLABs + GC stop-the-world). Recién ahí <code>volatile</code> y los <em>fences</em> dejan de ser decorativos: con green threads no hay reordenamientos reales que tapar; con hilos de SO, omitirlos rompe el código de formas no-deterministas.</p>
<blockquote>Para el LLM, el paralelismo pesado <strong>no</strong> vive en este rascacielos: vive en <strong>kernels off-heap</strong> (rayon/CUDA). Esta torre es para la <strong>corrección</strong> de la concurrencia Java, no para el cómputo numérico — que sale del heap por completo.</blockquote>
</div>

<!-- Fase B -->
<div class="section-break">
<h1>Fase B — El compilador (javac, escrito en Rust)</h1>
<p>El front-end convierte texto en estructuras y la back-end las convierte en bytecode:</p>
<pre>.java → lexer (tokens) → parser (AST) → análisis semántico → generación de bytecode → .class</pre>
${FASE_B.map(hito).join('\n')}
</div>

<!-- Componentes del compilador -->
<div class="section-break">
<h1>Compilador — pasadas y estructuras internas</h1>
<p>El compilador es <strong>multi-pasada</strong> sobre un <strong>único AST</strong>: se construye una vez (parser) y todas las pasadas lo recorren — la 1 lo lee para armar la tabla de símbolos, la 2 lo relee y lo <strong>decora</strong> con tipos y bindings. El objetivo es un compilador <strong>completo</strong> de Java SE 25; la semántica de cada regla está documentada y citada a la JLS 25 en <code>docs/semantica-jdk25.md</code>.</p>

<pre>${esc(PASADAS_ASCII)}</pre>

<p class="legend"><strong>Estado:</strong> ✅ hecho &nbsp;&nbsp; 🚧 parcial &nbsp;&nbsp; ⬜ falta</p>
${table(["Pasada", "Qué hace", "Hito", "Estado"], TBL_PASADAS)}

<p>Las pasadas no inventan código: <strong>ordenan y anotan</strong>. El orden lo imponen las dependencias — <strong>Enter</strong> antes que <strong>Attribute</strong> (por las referencias hacia adelante), Attribute antes que Flow y Desugar (necesitan tipos), Desugar antes que Codegen. La 1 lee "a lo ancho" (declaraciones), la 2 "a lo hondo" (cuerpos).</p>

<h2>Estructuras intermedias</h2>
<p>Además del AST, las pasadas comparten un <strong>bolso de servicios globales</strong> (la <em>Session</em>) y usan estado <strong>transitorio</strong> por recorrido. Dos que se olvidan siempre y duelen después: el <strong>log de errores</strong> (acumular, no frenar en el primero) y el <strong>interner de nombres</strong>.</p>
${table(["Estructura", "Rol", "Vida"], TBL_ESTRUCTURAS)}

<blockquote>Representación: <strong>arenas + IDs</strong> (símbolos por <code>SymbolId</code>, tipos por <code>TypeId</code>, nodos por <code>NodeId</code>) en vez de punteros — la forma idiomática de Rust para grafos con ciclos, y coherente con el <code>metaspace</code> de la JVM, que ya referencia por <code>MethodId</code>/Class ID. La <strong>tabla de símbolos</strong> es un <strong>árbol de <em>scopes</em> enlazados</strong> que persiste (no una pila que se destruye al salir); el <strong>Env</strong> lleva solo el <em>scope</em> actual y sube/baja por esa cadena.</blockquote>
</div>

<!-- Fase C -->
<div class="section-break">
<h1>Fase C — Las bibliotecas (en Java, compiladas por tu javac)</h1>
${FASE_C.map(hito).join('\n')}
</div>

<!-- Fase D y E -->
<div class="section-break">
<h1>Fase D — Herramientas (la &ldquo;DK&rdquo; = Development Kit)</h1>
<p>No se construyen en bloque, sino que se van completando como subproducto de las otras fases.</p>
${table(["Herramienta", "Cuándo se logra", "Horizonte"], TBL_HERRAMIENTAS)}

<h1>Fase E — Cerrar el círculo</h1>
<p>El momento épico: las tres piezas funcionando juntas.</p>
<ul>
  <li>${badge('C')} Compilar las bibliotecas (Fase C) con tu propio <code>javac</code> (Fase B)</li>
  <li>${badge('C')} Ejecutar un programa real que use esas bibliotecas en tu propia JVM (Fase A)</li>
  <li>${badge('C')} Conformance: comportamiento fiel a la especificación</li>
</ul>
<p class="exito"><strong>✓ Éxito:</strong> un <code>.java</code> que escribís → lo compila tu <code>javac</code> → usa tus bibliotecas → corre en tu JVM, sin tocar nada del JDK de Temurin.</p>
</div>

<!-- Más allá del JDK: la plataforma -->
<div class="section-break">
<h1>Más allá del JDK — la JVM como plataforma</h1>
<p>La JVM no es un fin en sí mismo: es la <strong>carrocería</strong> de un proyecto más grande. Como construimos VM <strong>+</strong> compilador propios, controlamos el stack entero y podemos <strong>inventar</strong> más allá de lo que <code>javac</code>/HotSpot permiten. Estos dos tracks son extensiones aspiracionales apoyadas en las fases A–E.</p>

<h1>Fase F — <code>burst</code>: el optimizador (rendimiento)</h1>
<blockquote>Módulo de optimización <strong>opcional</strong> atornillado sobre el intérprete ingenuo, que actúa de <strong>chasis y oráculo de corrección</strong>. <code>burst</code> debe dar resultados <strong>idénticos</strong> y se valida con <strong>differential testing</strong> (mismo programa por los dos caminos → assert de igualdad). Importante: un optimizador <strong>no necesita JIT</strong> — el intérprete tiene su propia caja de herramientas (típico 2–10× sin compilar a nativo).</blockquote>
<p class="exito"><strong>Estado (2026-08-15):</strong> la caja de herramientas del intérprete se abrió primero y rindió lo prometido —<strong>campos −86%, virtuales −45%, llamadas −40%</strong> sin compilar nada, solo dejando de recomputar lo mismo—, y después <strong>el JIT arrancó de verdad</strong>: emisor x86-64 propio (sin dependencias), memoria W^X, OSR, deopt con reconstrucción de estado, alocación en Eden y llamadas por inlining con <em>virtual frames</em>. Hoy compila <strong>444 de 785 métodos del corpus (~57%)</strong>, corre bucles <strong>~120× más rápido</strong> y acelera <strong>55×</strong> el patrón <code>new X(…)</code> en bucle con campos (17,3 millones de opcodes interpretados → 620). El <em>differential testing</em> que este módulo exige no hubo que construirlo: el oráculo <code>green≡os-gil≡os</code> del proyecto <strong>ya lo es</strong>, con el JIT activo en green y apagado en os-parallel — y la suite entera pasa idéntica con <code>JVM_JIT=0</code>. Detalle por hito abajo; la disciplina de medición que lo sostiene está en <code>F-bench</code>.</p>
${FASE_F.map(hito).join('\n')}
</div>

<!-- Fase G -->
<div class="section-break">
<h1>Fase G — <code>plain_data</code> / value types (modelo de datos)</h1>
<blockquote>Los <strong>&ldquo;huérfanos de Object&rdquo;</strong>: tipos planos sin header, sin identidad, sin monitor, <em>flatteables</em>. Viable porque tenemos compilador propio (Fase B) que puede emitir el constructo y una VM que lo trata especial. Es, en chiquito, el principio de representación de un <strong>tensor</strong> — puente directo a sistemas de ML. Inspiración: <em>value classes</em> de Valhalla, <code>struct</code> de .NET.</blockquote>
${FASE_G.map(hito).join('\n')}
</div>

<!-- Fase H — KajiLibrary -->
<div class="section-break">
<h1>Fase H — KajiLibrary (la biblioteca estándar, en Java, dogfooded)</h1>
<p>La versión <strong>concreta y accionable</strong> de la Fase C: la biblioteca escrita a mano en <code>KajiLibrary/</code> (nuevo directorio en la raíz), <strong>compilada por el <code>javac</code> propio</strong> (dogfooding — cada clase que no compile es un ítem concreto de B6) y <strong>ejecutada en KajiJDK</strong>. Clasificada por <em>tiers</em> según su dependencia: <strong>🟢</strong> Java puro (autocontenido, se compila y corre sin depender del runtime) · <strong>🔌</strong> necesita <strong>intrínsecos del VM</strong> (avanza con el track KajiJDK). El objetivo es la <strong>Fase E</strong> (cerrar el círculo): tu <code>.java</code> → tu <code>javac</code> → tus bibliotecas → tu JVM.</p>
<p><strong>Escala hoy: 768 clases públicas</strong> (476 de <code>java.*</code> + 292 de <code>jakarta.*</code>), todas pasando el gate contra el JDK 25 y los jars de referencia — y ese &laquo;25&raquo; dejó de ser una suposición: hasta hace poco <code>api_shape.py</code> invocaba <code>javap</code> a secas y tomaba el del <code>PATH</code>, un JDK 21. Lo destapó <code>Inflater.close()</code>, que figuraba como divergencia nuestra siendo que el método existe: lo que faltaba era <code>implements AutoCloseable</code>, que entró en Java 22. Hoy el gate resuelve el oráculo por orden y <strong>anuncia cuál usó y en qué release</strong>, porque un PASS sin saber contra qué no dice nada. Cambiarlo de 21 a 25 no produjo <strong>una sola divergencia nueva</strong> en 746 clases. En los paquetes que abrimos, <code>java.*</code> va <strong>476 de 646</strong> (74%), y <strong>trece paquetes están cerrados al 100%</strong>: <code>java.util.function</code>, <code>java.util.zip</code>, <code>java.lang.constant</code>, <code>java.lang.invoke</code>, <code>java.math</code>, <code>java.util.random</code>, todo <code>java.time</code> —incluidos <code>chrono</code>, <code>temporal</code>, <code>format</code> y <code>zone</code>—, más <code>jakarta.validation</code> y <code>jakarta.persistence</code>, la primera expansión externa en cerrar entera. <strong>Pero los trece no valen lo mismo</strong>, y el documento lo dice caso por caso: <code>java.util.zip</code> cierra <em>y</em> se validó ejecutando; <code>java.lang.invoke</code> cierra con la mitad que importa declarada y lanzando.</p>
<p><strong>Procedencia, dicho de frente.</strong> Casi todo esto está escrito de cero: las <strong>512 clases de <code>java.*</code></strong> y las <strong>89 de <code>jakarta.validation</code></strong> no tienen una línea copiada. La excepción es <code>jakarta.persistence</code>, cuyas anotaciones se compilaron primero desde las <em>fuentes</em> de la especificación para obtener sus <code>default</code> exactos; de esos 165 archivos ya se <strong>regeneraron 126 desde el binario</strong> y <strong>quedan 39</strong>, que conservan su cabecera <code>EPL-2.0 OR BSD-3-Clause</code> y su atribución a Oracle/Eclipse. El detalle completo —qué se regeneró, qué no y por qué— vive en <code>THIRD-PARTY.md</code>, en la raíz del repositorio.</p>
<p>El gate compara <strong>firmas</strong>: nuestra superficie pública tiene que ser un <strong>subconjunto</strong> de la del oráculo. Que exija subconjunto y no igualdad es lo que permite <strong>omitir un miembro</strong> que el compilador todavía no puede emitir y quedarse con la clase — la técnica que cerró las últimas 7 de JPA. Pero es una vara dura y a la vez insuficiente: <strong>pasar el gate no dice que la clase funcione</strong>. Los peores bugs de este track fueron invisibles para él, y eso dejó de ser una intuición — está <strong>medido</strong>: escaneando el bytecode ya emitido, <strong>35 clases gatean limpio y no pueden ejecutar</strong> (26 por el #110, 14 por el #112, 1 por el #119, con 6 en común).</p>
${FASE_H.map(hito).join('\n')}
</div>

<!-- Fase I — Herramientas del JDK -->
<div class="section-break">
<h1>Fase I — Depurador (JVMTI / jdb) — el detalle de la Fase D</h1>
<blockquote>El <strong>detalle accionable</strong> del depurador de la <strong>Fase D — Herramientas</strong> (que da el <em>overview</em> del toolchain). Replicar el <strong>toolchain</strong> más allá de <code>javac</code>/<code>java</code>, empezando por el depurador. La espina es el <strong>depurador</strong>, y su arquitectura es el stack <strong>JPDA</strong>: <code>JVMTI</code> (los ganchos, en la VM) ← <code>JDWP</code> (protocolo de cable) → <code>JDI</code> (API cliente) ← <code>jdb</code>. Se construye sobre lo que ya existe — <code>jvm-step</code> ya para en cada opcode y muestra pila + locales, así que la introspección está casi hecha. <strong>Dos caminos sobre el mismo JVMTI</strong>: un <code>jdb</code> <strong>pragmático</strong> in-process (I1) y uno <strong>fiel</strong> por protocolo (I3–I4), con la <strong>fidelidad</strong> (I5) como cumbre — que un IDE real attachee sin notar que la VM es propia. <code>javap</code> ya está tildado (<code>javap-clon</code>); el resto del toolchain (<code>jar</code>/<code>javadoc</code>/<code>jshell</code>) queda como ampliación del hito.</blockquote>
${FASE_I.map(hito).join('\n')}
</div>

<!-- Fase J — jlink -->
<div class="section-break">
<h1>Fase J — <code>jlink</code>: la imagen de runtime — el detalle de la Fase D</h1>
<blockquote>El <strong>detalle accionable</strong> de <code>jlink</code>, que la <strong>Fase D — Herramientas</strong> listaba como «largo plazo». jlink no necesita un <code>java.base</code> <em>completo</em>, necesita uno <strong>bien definido</strong>: resuelve un grafo de módulos y empaqueta lo alcanzable, así que con la biblioteca que tengamos sale una imagen más chica y nada más. Cada hito se verifica <strong>diferencialmente</strong> contra la herramienta real (<code>java --describe-module</code>, <code>jlink</code>, <code>jimage</code>), igual que el parser contra <code>javap</code>.</blockquote>
${FASE_J.map(hito).join('\n')}
</div>

<!-- Estado -->
<div class="section-break">
<h1>Estado actual</h1>
<p><strong>Fase A / Hitos A0–A5 logrados; A6 en progreso. Fase B / front-end + semántica (pasadas 1 y 2) + chequeo de declaraciones + análisis de flujo + desugar + Codegen (núcleo completo, con StackMapTable e <code>invokedynamic</code>).</strong> El proyecto compila <strong>sin warnings</strong>, pasa <strong>838 tests</strong> (JVM + el compilador propio), produce <strong>fixtures byte-idénticos</strong> a <code>javap</code> y ya <strong>ejecuta bytecode real con objetos, GC generacional, green threads, monitores y un sistema de tipos completo</strong>: aritmética, control de flujo, recursión, <code>switch</code> (<code>tableswitch</code>/<code>lookupswitch</code>), un <strong>heap</strong> con objetos/herencia/campos/arrays, <strong>dispatch dinámico</strong>, <strong>excepciones</strong>, <strong>inicialización de clases</strong>, <strong>class loaders</strong>, un <strong>recolector generacional</strong> (young por copia + Old mark·sweep·compact, con <strong>referencias débiles</strong> de <code>java.lang.ref</code>), <strong>hilos</strong> sobre tres sustratos (verde cooperativo, hilos de SO con <strong>GIL</strong>, y hilos de SO en <strong>paralelismo real</strong>) con <strong>monitores</strong> (<code>synchronized</code> de bloques y métodos, <code>wait</code>/<code>notify</code>), y un <strong>verificador de bytecode JVMS-estricto</strong>.</p>
<p><strong>Al día (2026-08-13).</strong> El motor se unificó con la línea avanzada del repositorio remoto: la concurrencia dejó de ser sólo <em>green threads</em> y hoy corre sobre <strong>tres sustratos</strong> (verde cooperativo · hilos de SO serializados por un <strong>GIL</strong> · hilos de SO en <strong>paralelismo real</strong> con <em>safepoints</em> stop-the-world), con <code>invokedynamic</code> completo y las arenas <code>eden</code>/<code>atomic_region</code>. El <strong>depurador</strong> (JVMTI/JDWP/JDI) se reaplicó sobre esa arquitectura: el <code>jdb</code> real de Temurin lo maneja end-to-end — breakpoints por línea, <code>where</code>, <code>locals</code> y <code>threads</code> multi-hilo enumerando el registro real de hilos. La <strong>fusión de <code>java.lang</code></strong> —que KajiLibrary sea la biblioteca única y <code>boot/</code> desaparezca— está <strong>parcial</strong>: el hueco bajó de 22 miembros a 7, y 6 de esos son divergencias deliberadas (ctors privados donde el otro los tenía públicos, sintéticos del enum, <code>Enum.valueOf</code> ausente por diseño); el único real es el flag <code>ACC_VOLATILE</code> de <code>Thread.interrupted</code>. Los dos bloqueos que restan son del <strong>compilador</strong>, no de la biblioteca: el desugaring del <code>for-each</code> erasa el retorno genérico a <code>Object</code> (finding #113) y <code>volatile</code> se pierde al emitir (#115).</p>
<p><strong>Arrancó la Fase J — <code>jlink</code>.</strong> Como esos dos bloqueos son del track del compilador, el trabajo se movió a lo que no depende de ellos: la imagen de runtime. <strong>J0</strong> (leer un <code>module-info.class</code> a <code>ModuleDescriptor</code>) y <strong>J1</strong> (resolver el grafo JPMS) están <strong>logrados y verificados diferencialmente</strong> contra el JDK real: el descriptor de <code>java.base</code> sale idéntico a <code>java --describe-module</code> (233 líneas), y el conjunto que resolvemos coincide con el del <code>jlink</code> real en las <strong>68 raíces comparables</strong> del JDK. La clave conceptual: jlink <em>no</em> necesita un <code>java.base</code> completo —le importa el <strong>grafo</strong>, no cuántos métodos tenga la biblioteca—, así que es trabajo desbloqueado. Con <strong>J2</strong> el círculo se cierra: <code>jlink</code> ya <strong>produce una imagen</strong> de 427 clases con nuestro <code>java.base</code>, y un programa que usa <code>ArrayList</code>/<code>StringBuilder</code> <strong>corre desde ella</strong>. Con <strong>J3</strong> sabemos <em>leer</em> ese contenedor: los tres comandos de <code>jimage</code> salen idénticos a los del tool real sobre la imagen de 145 MB del JDK. Y con <strong>J4</strong> también lo escribimos: el <code>jimage</code> de Temurin lee las imágenes que produce nuestro <code>jlink</code>, y sus <code>extract</code> salen byte a byte idénticos al origen. <strong>J5</strong> hace que la VM <strong>bootee desde una imagen</strong> —incluso desde la de un JDK real—, y <strong>J6</strong> agrega el pipeline de plugins. La <strong>Fase J queda cerrada</strong>: la VM bootea desde una imagen <em>y</em> hace valer la encapsulación de JPMS (un paquete no exportado da <code>IllegalAccessError</code> al ejecutar, aunque compile), y los cuatro plugins andan. Y el <strong>inflate propio</strong> (DEFLATE completo, escrito de cero) cierra la última pieza: la VM lee imágenes comprimidas reales — los 7440 recursos de una imagen <code>zip-6</code> del JDK salen byte a byte. Lo único que queda afuera es <em>comprimir</em> de verdad, que pide un encoder LZ77 + Huffman; escribir sigue siendo <code>zip-0</code>.</p>
<p><strong>Al día (2026-08-18) — la biblioteca pasó de «forma correcta» a «corre de verdad».</strong> Tres hitos nuevos cerraron en KajiLibrary. <strong>H9 — <code>java.util.concurrent</code></strong> entero <strong>en Java puro</strong>, con sólo dos natives agregados (<code>Thread.currentThread()</code> y <code>Object.wait(long)</code>): la observación que lo habilita es que, en una VM cuyos hilos se intercalan <strong>entre opcodes</strong>, un atómico hecho con <code>synchronized</code> es <em>indistinguible</em> de uno lock-free con CAS. <strong>H10 — <code>jakarta.persistence</code></strong> (JPA 3.2) quedó en <strong>990 match / 0 mismatch / 0 extra</strong> sobre 204 clases. Y <strong>H11</strong> llenó los huecos de <code>java.base</code> con una <strong>flota de agentes en paralelo</strong> (~200 clases): <code>java.util.function</code> quedó <strong>43/43</strong>, y <code>java.util</code>/<code>java.io</code>/<code>java.lang</code> subieron fuerte. <strong>La biblioteca entera pasa el gate: 3074 match / 0 mismatch / 0 extra, 47 allowed, PASS</strong> sobre <strong>681 clases públicas</strong>.</p>
<p><strong>Pero el cambio importante del período es de método, no de volumen.</strong> Hasta ahora la biblioteca se validaba comparando <strong>forma de API</strong> contra el JDK y corriendo el algoritmo portado en <strong>Java real</strong>. Las dos cosas resultaron insuficientes de una forma que conviene dejar escrita: nuestro <code>TimeUnit.cvt</code> estaba <strong>validado 112/112 contra el JDK</strong> y sin embargo <strong>toda</strong> conversión de grueso a fino <em>trapeaba</em> en nuestra VM (leía <code>Long.MAX_VALUE</code> con <code>getfield</code>, finding #110) — sólo la forma <code>src == dst</code> esquivaba el bug, que es justo la que cubría la validación. Por eso H9 se apoya en <strong>24 tests de comportamiento que corren la biblioteca sobre el intérprete real</strong>, y por eso <code>java.util.concurrent</code> es hoy el <strong>único paquete verificado end-to-end</strong>: <code>java.time</code> figura 100% en forma y <strong>nunca se ejecutó</strong>. La regla que queda: <strong>validar en Java real no prueba nada sobre nuestra VM</strong>.</p>
<p><strong>Y el dogfooding devolvió un diagnóstico, no sólo una lista de bugs.</strong> Seis findings del compilador (<strong>#108, #111, #114, #118, #119, #120</strong>) comparten una única raíz: <strong>cuando la resolución falla, el compilador emite silencio en vez de un error</strong> — la llamada desaparece del bytecode y deja operandos colgados o la pila vacía, así que el <code>.class</code> pasa el gate de forma y explota (o peor, calla) en ejecución. Cuatro de ellos son puntos ciegos del <strong>lector de classpath</strong> (<code>Exceptions</code> #104, <code>ACC_STATIC</code> #110, <code>ACC_VARARGS</code> #118, la tabla de métodos del supertipo #120): lo que <em>escribimos</em> está bien, lo que <em>leemos</em> se interpreta mal. La recomendación para el track del compilador cabe en una línea — <strong>una llamada que no resuelve tiene que ser error de compilación</strong>—: eso convierte seis bugs mudos en seis mensajes. Caso testigo: <code>WeakHashMap</code> <strong>gatea limpio y no puede correr</strong> (#119), sin workaround posible desde el fuente. Y el #101 resultó tener <strong>dos mitades</strong>, de las que solo una estaba documentada: como <em>tipo</em>, un anidado importado funciona —aunque erase a <code>Object</code> en el descriptor—, pero como <strong>calificador de un miembro estático es inalcanzable desde fuera de su archivo por cualquier vía</strong>. Las tres formas fallan igual (<code>Outer.Nested.miembro</code>, el anidado importado, y el <code>import static</code> de la constante), lo que costó omitir <code>MethodHandleDesc.ofConstructor</code> y los diez <code>BSM_*</code> de <code>ConstantDescs</code>: todos necesitan el <em>valor</em> <code>Kind.STATIC</code>, y no hay forma de escribirlo. Del lado del runtime queda un bug de una línea con impacto real: un <code>wait(ms)</code> que <strong>expira</strong> no se saca del <em>wait-set</em> del monitor, y un <code>notify</code> posterior termina bloqueando al hilo que corre.</p>
<p><strong>Dos defectos más del compilador, y los dos aparecieron por escribir código normal.</strong> El <strong>#125</strong> es que el emisor <strong>no soporta <code>super.metodo(...)</code></strong>: el <code>super(...)</code> de constructor anda, pero el acceso calificado por <code>super</code> a un método —un <code>invokespecial</code> sobre <code>this</code> con el método del supertipo— no. Saltó apenas se escribió GZIP, y no por casualidad: <strong>es la forma canónica de «extender sin reemplazar»</strong>, así que aparece en cuanto hay una jerarquía de decoradores. Lo incómodo es que <strong>invalida un workaround que el propio archivo de findings recomendaba</strong> («evitarlos o usar <code>super.m()</code>»). El <strong>#126</strong> es que el <strong>retorno de un override contra un supertipo del classpath se erasa a <code>Object</code></strong>: <code>CallSite</code> declara <code>abstract MethodHandle getTarget()</code> y compilada sola emite el descriptor correcto, pero el override en <code>MutableCallSite</code> emite <code>()Ljava/lang/Object;</code>. Lo revelador es que <strong>el parámetro del mismo tipo sale bien</strong> —<code>setTarget(MethodHandle)</code> es correcto en esa misma clase—, así que el tipo resuelve perfecto y lo que se pierde es específicamente el retorno <em>cuando el método es un override</em>. Es familia del #123, pero más amplio: ahí había covarianza y acá el tipo de retorno es idéntico al del supertipo.</p>
<p><strong>De la anécdota a la medición.</strong> «El gate no prueba que funcione» era una intuición repetida; ahora es un número. Dos scripts (<code>tools/scan110.py</code> y <code>scan112.py</code>, versionados como chequeo de regresión) recorren el <strong>bytecode ya emitido</strong> y responden dos preguntas concretas: qué <code>getfield</code> apunta a un campo que en su clase dueña es <code>static</code> (#110), y qué constante <code>static final</code> viaja solo en el atributo <code>ConstantValue</code> sin <code>&lt;clinit&gt;</code> que la asigne (#112). El resultado: <strong>26 clases</strong> tocadas por el #110 —de 94 accesos cross-class, 36 pares (clase, campo) mal emitidos— y <strong>14</strong> por el #112, con 65 constantes que <strong>valen 0 en runtime</strong>. Unidas, y sumando <code>WeakHashMap</code>: <strong>35 clases que gatean limpio y están muertas al ejecutar</strong>. El #110 está brutalmente concentrado —<strong>21 de las 26 son <code>java.time</code></strong>, leyendo constantes de <code>ChronoField</code>/<code>ChronoUnit</code>—, lo que confirma con evidencia lo que el hito H4 no podía ver: el paquete figura 100% en forma y nunca corrió. El #112 es más chico y de peor carácter: <strong>no trapea, devuelve 0 en silencio</strong> — las 12 constantes de <code>Modifier</code> hacen que <code>isPublic</code>/<code>isStatic</code> mientan sin avisar.</p>
<p><strong>Los dos arreglados, en el compilador.</strong> El <strong>#110</strong> era una línea que tiraba información: el lector de <code>.class</code> leía los <em>access flags</em> de cada campo y los <strong>descartaba</strong> (<code>r.u2()?; // access</code>), así que un campo del classpath nacía sin <code>ACC_STATIC</code> y el emisor —que decide bien— elegía <code>getfield</code>. Ahora el flag se conserva y viaja al símbolo. El <strong>#112</strong> se arregló del lado que hace javac: <strong>plegar la constante en el punto de uso</strong> (JLS §13.1), tanto para las declaradas en la unidad como para las que vienen del classpath, para lo cual el lector también pasó a leer el atributo <code>ConstantValue</code>. Ambos cambios llevan un comentario que explica el defecto, porque los mergea otra sesión. <strong>Consecuencia operativa:</strong> lo compilado antes del fix sigue roto — hay que <strong>recompilar <code>java.time</code> entero</strong>, la misma nota que ya existía para el #21.</p>
<ul>
  <li><strong>ClassReader</strong> (cursor big-endian) y <strong>constant pool</strong> completo (<code>ConstantPoolEntry</code>, 17 tags + <code>Tombstone</code>), con árbol de referencias en <code>pretty_class_visualizer.rs</code>.</li>
  <li><strong>Header</strong>: versiones, <code>access_flags</code> (+ métodos <code>is_*</code>), <code>this_class</code>/<code>super_class</code> con <code>class_name()</code>. <code>from_path() → Result</code> (sin panics).</li>
  <li><strong>Code</strong> con <strong>desensamblado completo</strong> (opcodes + <code>tableswitch</code>/<code>lookupswitch</code>/<code>wide</code>) y comentarios <code>// …</code> resueltos.</li>
  <li><strong><code>StackMapTable</code> (los 7 frame types)</strong>, cada frame en su clase bajo <code>parser/stack_map_table/</code>, con <code>verification_type_info</code>.</li>
  <li><code>javap</code> <strong>brief y <code>-v</code> byte-idénticos</strong> (incl. cabecera Classfile / Last modified / SHA-256). Flags de visibilidad (<code>-public</code>/<code>-protected</code>/<code>-package</code>/<code>-p</code>).</li>
  <li>Renderers factorizados en <code>parser/printers/</code>: <code>verbose</code> (orquestación) + <code>file_header</code> + <code>pool_comments</code> + <code>member_dump</code> + <code>brief</code> + <code>dump_common</code> + <code>visibility</code>.</li>
</ul>
<p><strong>Pendiente de A0</strong> (no esencial, no bloquea avanzar): atributos <code>Signature</code> (reescribe la declaración → parser de firmas genéricas), <code>BootstrapMethods</code>, <code>InnerClasses</code>/<code>EnclosingMethod</code>, <code>Exceptions</code>, <code>ConstantValue</code>, anotaciones, <code>Record</code>, <code>PermittedSubclasses</code>, <code>NestHost</code>/<code>NestMembers</code>, <code>LocalVariableTable</code>/<code>Type</code>; y flags de contenido (<code>-c</code>/<code>-l</code>/<code>-s</code>).</p>
<p><strong>A1–A2 — el intérprete.</strong> Loop de despacho sobre una <strong>pila de frames</strong>; cada frame referencia su bytecode por índice (<code>MethodId</code>) en el <em>Method Area</em> (<code>metaspace</code>, con resolución de llamadas por el código del constant pool). Opcodes: <code>iconst</code>/<code>iload</code>/<code>istore</code>, <code>iadd</code>/<code>isub</code>/<code>imul</code>, <code>goto</code>/<code>if_icmpgt</code>, <code>invokestatic</code>/<code>ireturn</code>. Corre <strong>factorial</strong> y <strong>fibonacci recursivos</strong>. Visualizador <code>jvm-step</code> paso a paso con vista de dos paneles de la call stack.</p>
<p><strong>A3 — objetos y heap.</strong> El <strong>heap</strong> es una arena de bytes (<code>Vec&lt;u8&gt;</code>) con <code>malloc</code> bump (sin liberar; el GC llega en A5). Los objetos se disponen como <code>[class_id | mark | campos]</code> con <em>layout</em> que respeta la herencia; cada clase recibe un <strong>Class ID</strong> (UUID) y un <em>mirror</em> en el heap para sus estáticos (estilo HotSpot). Opcodes: <code>new</code>, <code>dup</code>, <code>getfield</code>/<code>putfield</code>, <code>aload</code>/<code>astore</code>, los cuatro <code>invoke*</code> y la familia de arrays (<code>newarray</code>/<code>anewarray</code>, <code>arraylength</code>, <code>iaload</code>/<code>iastore</code>/<code>aaload</code>/<code>aastore</code> + byte/char/short). El <strong>dispatch dinámico</strong> usa <em>vtables</em> por clase (slot del tipo estático, método del tipo real) e <em>itable</em> para interfaces — verificado: <code>Animal a = new Dog(); a.sound()</code> → <code>Dog.sound</code>.</p>
<p><strong>A4 — robustez del runtime.</strong> <strong>Excepciones</strong>: <code>athrow</code> + tabla de excepciones + <em>stack unwinding</em> por la pila de frames, con <code>finally</code> (catch-all + re-throw) y las <strong>implícitas</strong> que la VM sintetiza (NPE, índice fuera de rango, cast inválido, tamaño negativo). <strong>Verificador</strong> de bytecode (type-checking en una pasada con el <code>StackMapTable</code>) que corre antes de ejecutar. <strong>Inicialización</strong> de clases <code>&lt;clinit&gt;</code> perezosa y super-primero. <strong>Class loaders</strong> bootstrap/app con delegación parent-first (las <code>java.lang.*</code> propias viven en <code>boot/</code>). Resolución que falla con <strong>errores de linkage</strong> (<code>NoClassDefFoundError</code>/<code>NoSuchMethodError</code>) en vez de paniquear. <strong>Robustez fina cerrada (2026-08):</strong> <code>Throwable</code> con <code>getMessage</code>/<code>toString</code> y <strong>stack traces</strong> capturados por la VM en el throw; <code>ExceptionInInitializerError</code> (JVMS §5.5: un <code>&lt;clinit&gt;</code> que lanza propaga, se envuelve, y deja la clase <em>erroneous</em> → <code>NoClassDefFoundError</code> al reuso); <strong>default methods</strong> de interfaces (JSR 335 — la vtable fusiona superinterfaces, regla <em>maximally-specific</em>). Pendiente fino: chequeos de acceso e identidad <code>(nombre, loader)</code> (no producibles con javac — ver <code>holdon.md</code>).</p>
<p><strong>A5 — nativos + GC.</strong> <strong>Puente nativo</strong> (<code>System.out.println</code> imprime de verdad) e <em>intrinsics</em> (<code>getClass</code>, <code>hashCode</code>, <code>Math</code>, <code>arraycopy</code>, <code>String</code>/<code>Class</code>). <strong>Recolector de basura</strong> que arrancó en mark &amp; sweep y creció a un colector con <strong>compactación</strong> (mover objetos + reescribir punteros), <strong>free list</strong> con <em>coalescing</em>, <strong>política de fragmentación</strong> configurable, <strong>disparadores</strong> (out-of-space / occupancy / allocation-rate / explícito) sobre un <em>safepoint</em>, y <strong>marcado transitivo</strong> correcto (sigue campos, arrays y estáticos).</p>
<p><strong>A6 (en progreso) — cumbre del runtime.</strong> <strong>Verificador de bytecode JVMS-estricto</strong>: además de la cobertura completa de opcodes (incluido <code>switch</code>), verifica <strong>con o sin <code>StackMapTable</code></strong> — inferencia por punto fijo como fallback, con <em>cross-check</em> de la tabla contra la inferencia — sobre un <strong>gate estructural</strong> (§4.9), con <strong>tipos de locales</strong> estrictos + <code>max_stack</code>, reglas de <strong>objetos sin inicializar</strong>, <strong>handlers de excepción</strong> tipados y <strong>acceso/linkage</strong> (regla <code>protected</code>, <code>count</code> de <code>invokeinterface</code>). <strong>Sistema de tipos completo</strong>: los cuatro tipos numéricos (<code>int</code>/<code>long</code>/<code>double</code>/<code>float</code>) <strong>ejecutados y verificados</strong> — cómputo, conversiones, comparaciones, división con <code>ArithmeticException</code>, <strong>categoría-2</strong> (params, campos, estáticos, arrays, frames del <code>StackMapTable</code>) y el <strong>lattice de referencias</strong> (covarianza de arrays + <em>join</em>/LUB). <strong>GC generacional</strong>: arena dividida en <code>Eden | S0 | S1 | Old</code>; los objetos nacen en Eden, un <strong>minor</strong> por <strong>copia</strong> evacúa los pocos sobrevivientes a un <em>survivor</em> (o los <strong>promueve</strong> a Old por edad) reescribiendo referencias con <em>forwarding</em>, un <strong>write barrier</strong> mantiene un <strong>remembered set</strong> de punteros <code>old→young</code> (las raíces que el minor escanea, sin recorrer todo Old), y el <strong>major</strong> hace mark-sweep-compact sobre Old. <strong>Referencias débiles</strong> (<code>java.lang.ref</code>): <code>WeakReference</code> + <code>ReferenceQueue</code> — el major trata el <code>referent</code> como débil y, al morir, lo limpia (<code>get()</code> → <code>null</code>) y <strong>encola</strong> la referencia; cimiento de <code>PhantomReference</code>/<code>Cleaner</code>.</p>
<p><strong>Cobertura del set de opcodes — 199/199 alcanzables.</strong> El intérprete despacha todos los opcodes del JVMS que un class file moderno puede contener. Los últimos en cerrarse fueron <code>nop</code> (0x00), <code>goto_w</code> (0xc8) — el <code>goto</code> de offset de 4 bytes, para targets a más de ±32 KB, implementado ensanchando <code>jump_to</code> hacia un <code>jump_to_wide</code> común en vez de duplicar la aritmética — y el prefijo <strong><code>wide</code></strong> (0xc4), que reejecuta el opcode siguiente con un índice de local de <strong>16 bits</strong>: la única manera de direccionar los slots pasados el 255 en un método con más de 256 locales (<code>wide iinc</code> mide 6 bytes, el resto 4). Ese último salió barato por una propiedad que el diseño ya tenía: los handlers de <code>variable_operations</code> reciben <code>slot: usize</code> y <strong>nunca se enteran del ancho</strong>, así que ensanchar es puro <em>decoding</em> y el módulo no cambió de comportamiento. Verificado por <em>differential testing</em>: <code>WideLocals.java</code> declara 300 locales para forzar a <code>javac</code> a emitir el prefijo (<code>istore_w 299</code>, <code>iinc_w 299, 35</code>, <code>iload_w 299</code>), y el mismo <code>.class</code> da <code>42</code> tanto en el <code>java</code> de JDK 25 como en nuestra VM. <strong>De los 4 restantes, 3 son una decisión y no deuda</strong>: <code>jsr</code>/<code>ret</code>/<code>jsr_w</code> están prohibidos por <strong>JVMS §4.9.1</strong> en class files de versión 50.0+ (Java 6 en adelante) — desde que existe el <code>StackMapTable</code> y el verificador por <em>type-checking</em>, las subrutinas quedaron fuera del modelo, y <code>javac</code> compila <code>finally</code> duplicando el bloque. La postura del proyecto es <strong>leer sí, ejecutar no</strong>: el desensamblador soporta <code>jsr</code>/<code>ret</code>/<code>jsr_w</code>/<code>ret_w</code> porque un <code>javap</code> debe renderizar <em>cualquier</em> class file legal (lo exige A0, medido sobre <code>java.base</code>), mientras el gate estructural del verificador los rechaza y el intérprete no los despacha. Curiosamente <em>ejecutarlos</em> sería lo más fácil que queda —unas 10 líneas—; el costo está en una variante <code>ReturnAddress</code> que toca todos los <code>match</code> sobre <code>Value</code> y, sobre todo, en verificar subrutinas polimórficas, la parte más difícil del type-checker del JVMS. Razonamiento completo en la nota de diseño <code>subrutinas-jsr-ret.md</code>. Contarlos como faltantes mezclaría una decisión con una tarea: el número honesto es <strong>199 de 199 alcanzables — completo</strong>.</p>
<p><strong><code>invokedynamic</code> (0xba) — el subsistema.</strong> El opcode solo no hace nada: no nombra ningún método, sino un <em>bootstrap</em> que <strong>produce</strong> el destino la primera vez. Esa indirección es lo que le permitió a Java agregar lambdas, concatenación de strings y records sin inventar un opcode por cada una — el lenguaje pone la política en el bootstrap y la VM queda fija. Corren <strong>5 de las 6 fábricas</strong> que emite <code>javac</code>: <code>StringConcatFactory</code>, <code>SwitchBootstraps.typeSwitch</code> (patrones de tipo y de enum), <code>ObjectMethods</code> (los tres métodos de un <code>record</code>, desde una sola entrada de <code>BootstrapMethods</code> y distinguidos por el <em>nombre</em> del call site), <code>LambdaMetafactory.metafactory</code> y <code>ConstantBootstraps.invoke</code>. La sexta, <code>altMetafactory</code>, necesita serialización (<code>java.io</code>) y queda fuera de alcance. Dos decisiones de diseño: los <em>bootstrap methods</em> se modelan como <strong>intrínsecos</strong> —el mismo criterio de <code>intrinsecos.md</code>—, con <code>java.lang.constant</code> escrito en <strong>Java</strong>; y <code>LambdaMetafactory</code> <strong>genera una clase real</strong> al vuelo (con el escritor de <code>.class</code>): implementa la interfaz funcional, con campos para las capturas y un método SAM que reenvía al handle — igual que el JDK, que la spinnea con ASM. El objeto lambda es entonces una instancia común, despachada por el itable normal, sin atajo. Ruta, correcciones y mediciones: <code>invokedynamic-ruta.md</code>.</p>
<p><strong>La VM puede invocar Java (<code>call_java</code>).</strong> Empuja un frame propio y lo corre con un bucle de <code>run_one</code> anidado, devolviendo el resultado. Resultó ser el <strong>caso general de lo que la inicialización de clases venía haciendo</strong>: <code>&lt;clinit&gt;</code> era la variante sin argumentos ni resultado, así que el mecanismo ya existía entero y sólo estaba especializado. Importa porque <strong>los intrínsecos dejan de ser terminales</strong>: un nativo que sólo puede calcular y devolver está obligado a reimplementar lo que necesite de la biblioteca — así fue como <code>String.valueOf</code> terminó a medias en Rust. Con esto, <code>String.valueOf(Object)</code> llama al <code>toString()</code> del objeto, un <code>record</code> pregunta el <code>equals</code>/<code>hashCode</code> de sus componentes en vez de comparar referencias, y una constante dinámica ejecuta su bootstrap. Eso <strong>cerró 0xba</strong>: el <strong>modelo de objetos de <code>java.lang.invoke</code></strong> (<code>MethodHandle</code>/<code>MethodType</code>/<code>Lookup</code>) vive en Java, con <code>MethodHandle.invoke</code>/<code>invokeWithArguments</code> nativos —polimorfia de firma, interceptados antes de la resolución—, el único intrínseco que corresponde. El premio se cobró: <code>ConstantBootstraps.invoke</code> es <strong>ahora Java</strong> (<code>handle.invokeWithArguments(args)</code>), lo que obligó a hacer el <strong>cache de condy raíz de GC</strong>. El <code>ldc</code> de <code>MethodHandle</code>/<code>MethodType</code> —que <code>javac</code> nunca emite— se probó con <strong>class files hechos a mano por el escritor de <code>.class</code></strong> (su estreno). Con mirrors de primitivos (<code>int.class</code> → <code>Integer.TYPE</code>) y los kinds static/virtual/special/constructor, y <code>LambdaMetafactory</code> generando clases reales, <code>invokedynamic</code> queda completo.</p>
<p><strong><code>multianewarray</code> (0xc5).</strong> El último en cerrarse, y el que más enseña sobre el modelo de datos: <strong>Java no tiene arrays rectangulares</strong>. <code>new int[2][3]</code> no es un bloque plano sino dos <code>[I</code> colgando de un <code>[[I</code>, cada nivel un objeto real — por eso las filas se reemplazan por separado y <code>a[0].length</code> no tiene por qué igualar <code>a[1].length</code>. La instrucción lleva el descriptor del array <em>mismo</em> (<code>[[I</code>, no el elemento como <code>anewarray</code>) más un contador de dimensiones, porque sólo se materializan <strong>esos</strong> niveles: <code>new int[3][]</code> deja los internos en <code>null</code>. Los conteos se validan <em>todos</em> antes de alocar nada, para que un largo negativo en una dimensión posterior no deje un array a medio construir; los niveles superiores guardan referencias y sólo el más interno usa el ancho real del elemento (una fila <code>byte[]</code> ocupa un byte por elemento, no cuatro); y las referencias hijo se escriben por <code>store_reference</code>, nunca por un <code>write_u32</code> crudo, porque son exactamente los punteros <code>old→young</code> que el <em>remembered set</em> debe registrar. <strong>Ejecutarlo era la mitad del trabajo:</strong> hubo que modelarlo en el <code>transfer</code> del verificador, ya que un opcode sin modelar produce un <code>VerifyError</code> marcado <code>unsupported</code> — que por diseño hace que el llamador avise y siga, o sea que la clase habría corrido con el verificador claudicando en silencio.</p>
<p><strong>Green threads (Fase 0).</strong> <code>java.lang.Thread</code> + <code>Thread.start()</code> nativo, un scheduler <strong>cooperativo</strong> round-robin que conmuta en el safepoint (entre opcodes), un call stack por-thread, y el GC tomando las raíces de <strong>todos</strong> los threads. Verificado: dos workers intercalándose con <code>main</code> (que los espera por spin-wait) dan <code>100</code>, y el step-visualizer muestra los cambios de contexto, el spawn y la terminación de cada thread. Es el modelo de los <em>virtual threads</em> de Java 21 (scheduling en espacio de usuario), con un solo carrier.</p>
<p><strong>Monitores.</strong> Cada objeto tiene su monitor intrínseco (lock reentrante con dueño + <em>blocked-set</em> + <em>wait-set</em>). <code>synchronized</code> funciona en sus dos formas: <strong>bloques</strong> (opcodes <code>monitorenter</code>/<code>monitorexit</code>) y <strong>métodos</strong> (<code>ACC_SYNCHRONIZED</code>, <em>sin</em> opcode — el VM toma el monitor del receptor o del <code>Class</code> al empujar el frame y lo suelta al popearlo, por <code>return</code> <em>o</em> por <em>unwind</em> de excepción, vía un único punto de release imposible de saltear). La señalización <code>wait</code>/<code>notify</code>/<code>notifyAll</code> libera el monitor (guardando el conteo de reentrada), parquea al hilo en el <em>wait-set</em> y lo re-adquiere al despertar. Verificado con exclusión mutua real (dos hilos × 100 incrementos → 200, contra el control sin lock que pierde updates) y el <em>handshake</em> productor/consumidor (<code>wait</code> → <code>notify</code> → 42). <strong>Pendiente del monitor</strong>: <code>IllegalMonitorStateException</code>, <code>wait(timeout)</code>, interrupciones y la interacción monitor↔compactación del GC (hoy se keyea por offset).</p>
<p><strong>Arquitectura en capas (estilo <em>service</em>).</strong> El heap se encapsuló tras un <strong><code>HeapService</code></strong> que es su <strong>único dueño</strong>: toda escritura de referencia pasa por un portón (<code>store_reference</code>) que aplica el <strong>write barrier</strong> de forma no-bypasseable — el seam donde irá la sincronización cuando los threads pasen a ser de SO. El runtime es la <strong><code>JVM</code></strong> (composition root) sobre <code>HeapService</code> + <code>MetaspaceService</code> + el sistema de threads.</p>
<p><strong>Fase B — el compilador (front-end + semántica + chequeo + flujo + desugar).</strong> El <code>javac</code> propio (crate <strong>desacoplado</strong> en <code>src/javac/</code>, cuyo único contrato es el formato <code>.class</code>) tiene el front-end entero, las <strong>dos pasadas</strong> semánticas, el análisis de flujo y el desugar. <strong>Front-end:</strong> <strong>lexer</strong> (las 115 <code>TokenKind</code> de JDK 25, <em>maximal munch</em>, BOM), <strong>parser</strong> por <em>recursive descent</em> con <strong>todas las sentencias</strong> (incl. <code>switch</code> con patterns/guards, <code>try</code>-with-resources, <code>synchronized</code>), <code>enum</code>/<code>record</code>/<code>@interface</code> y <strong>genéricos completos</strong> — argumentos de tipo, <em>wildcards</em>, cotas y el diamante, partiendo <code>&gt;&gt;</code>/<code>&gt;&gt;&gt;</code> con un <em>undo log</em> para no corromper el <em>backtracking</em>. El <strong>lenguaje moderno</strong> también entró: <strong>lambdas</strong> (con el desambiguador <code>(</code>-cast-vs-paréntesis-vs-parámetros por <em>lookahead</em> de la flecha), <strong>referencias a método</strong> (incl. <code>T[]::new</code>), <strong>clases anónimas y locales</strong>, el <strong>type witness</strong> (<code>Collections.&lt;String&gt;emptyList()</code>, que además se <strong>aplica</strong> —fija los parámetros de tipo en vez de inferirlos—) y las <strong>anotaciones</strong> (§9.7), que antes se <strong>descartaban</strong> y ahora se cuelgan de la declaración. Cada forma se parsea entera y se guarda fiel en el AST; compilarlas es de fases posteriores, y hasta entonces la barrera del emisor las corta. <strong>Pasada 1 (Enter/MemberEnter):</strong> tabla de símbolos con paquetes y tipos <strong>anidados</strong>, detección de duplicados y <strong>ciclos de herencia</strong>, un <strong>class finder real</strong> que lee <code>.class</code> del classpath (lector propio) <strong>incluido el atributo <code>Signature</code></strong> — así <code>java.util.List</code> carga con su <code>&lt;E&gt;</code> y <code>List.get</code> devuelve <code>E</code>, no <code>Object</code> — resolución con <strong>errores posicionados</strong>, y el <strong>grafo tipado persistido</strong> como salida. <strong>Pasada 2 (Attribute):</strong> resuelve nombres y tipa los cuerpos <strong>decorando el AST in situ</strong> (tipo + <em>binding</em> por nodo, slot por local con categoría-2, al estilo <code>JCTree.type</code>/<code>sym</code>); con <strong>overload resolution en 3 fases</strong> (estricta → boxing → varargs, con <em>most specific</em> y ambigüedad) y <strong>genéricos</strong> reales — subtipado con <em>containment</em> de wildcards (invariancia), sustitución del receptor, <code>lub</code>/<code>glb</code> e <strong>inferencia</strong> de los argumentos de tipo de una llamada (Cap. 18). El álgebra de tipos vive en un módulo <code>types</code> (el <code>Types</code> de javac) y la inferencia en <code>infer</code> (el <code>Infer</code>). <strong>Pasada Flow (<code>flow</code>):</strong> sobre el AST decorado, la <strong>asignación definitiva</strong> del Cap. 16 — con los dos conjuntos duales <em>definitely assigned</em> / <em>definitely unassigned</em>, los flujos <em>when-true/when-false</em> de <code>&amp;&amp;</code>/<code>||</code>/<code>!</code>, las reglas de variables <strong><code>final</code></strong> (no reasignar, asignación única, multi-catch implícito), la <strong>alcanzabilidad</strong> (§14.21) y el <code>break</code>/<code>continue</code> <strong>etiquetados</strong> (§14.15/§14.16: la pila de contextos de <code>break</code> lleva la etiqueta a la que responde cada uno). <strong>Pasada Desugar (<code>desugar</code>):</strong> baja el azúcar de <strong>sentencias</strong> (<code>for-each</code> → <code>for</code> indexado o <code>while</code>+<code>Iterator</code>, <code>try</code>-with-resources → <code>try/finally</code>+<code>close()</code>, <code>assert</code> → <code>if(!cond) throw</code>) y de <strong>expresiones</strong> (concatenación de <code>String</code> → cadena de <code>StringBuilder</code>, asignación compuesta <code>x op= y</code> → <code>x = (T)(x op y)</code> con el cast de reducción, y <strong>varargs</strong> → array en el <em>call site</em>) — verificado re-tipando el árbol bajado. En esta iteración se sumaron cuatro <em>rewrites</em> más: (1) <strong><code>++</code>/<code>--</code> en posición de descarte</strong> (§15.14.2/§15.15.2) — como sentencia (<code>x++;</code>, <code>--x;</code>) o en el <code>update</code> de un <code>for</code> — se reescriben a <code>x += 1</code> reusando el <em>lowering</em> de la asignación compuesta (mismo cast de reducción), mientras que en posición de <em>valor</em> (<code>y = x++</code>) quedan para el codegen (<code>iinc</code>/<code>dup</code>); (2) la <strong><code>switch</code>-expresión</strong> en posición de cola (§15.28) — <code>T v = switch…</code>, <code>return switch…</code>, <code>x = switch…</code>, <code>yield switch…</code> — se convierte en una <code>switch</code>-<em>sentencia</em> cuyos brazos <strong>asignan</strong> el valor (con temporal cuando hace falta); requiere <code>default</code> y etiquetas constantes, y los brazos con <strong>bloque</strong> o de <strong>dos puntos</strong> con <code>yield</code> (incluso adentro de un bucle o un <code>if</code>) se bajan reescribiendo cada <code>yield e</code> a <code>{ v = e; break $sw; }</code> con un <code>break</code> <strong>etiquetado</strong> sobre el <code>switch</code> generado —un <code>break</code> pelado solo saldría del bucle— mientras que el exhaustivo sin <code>default</code> y las switch-expresiones embebidas quedan como expresión sobre la pila; (3) <strong>try-with-resources con excepción suprimida</strong> (§14.20.3.1) — corrección de un bug: antes la excepción del <code>close()</code> <em>tapaba</em> la del cuerpo; ahora se preserva la primaria y la del <code>close()</code> queda <strong>suprimida</strong> (<code>Throwable.addSuppressed</code>), con la traducción completa del JLS (<code>$primary=null; try{…}catch($t){$primary=$t;throw;}finally{ if(r!=null){ if($primary!=null) try{r.close();}catch($x){$primary.addSuppressed($x);} else r.close(); } }</code>); (4) <strong><code>switch</code> sobre <code>String</code></strong> (§14.11) → <strong>dos</strong> switches sobre <code>int</code>: el primero mapea <code>s.hashCode()</code> a un índice sintético vía <code>equals()</code> (cadena <code>if/else</code> para las colisiones de hash) y el segundo replica los brazos indexados (preservando <em>fall-through</em> y <code>default</code>), con el <code>String.hashCode()</code> calculado en tiempo de compilación (algoritmo del JDK: <code>31·h + c</code> sobre unidades UTF-16, aritmética <code>i32</code> que envuelve); y (5) <strong><code>switch</code> sobre <code>enum</code></strong> (§14.11) con el <strong><code>$SwitchMap</code> fiel a javac</strong>: <code>switch(color)</code> → <code>switch(C$1.$SwitchMap$Color[color.ordinal()])</code>, donde <code>$SwitchMap$Color</code> es un <code>int[]</code> sintético alojado en una <strong>clase anidada <code>C$1</code></strong> y poblado en un <code>&lt;clinit&gt;</code> con un <code>try { map[Color.C.ordinal()] = k; } catch (NoSuchFieldError e) {}</code> por constante (robusto ante recompilación separada del enum). Esto requirió una <strong>máquina de síntesis a nivel clase</strong>: el miembro <code>Member::StaticInit</code> (con lo que los bloques <code>static { }</code> del usuario, antes <strong>descartados</strong>, ahora se conservan y analizan), la tabla de símbolos <strong>mutable</strong> en el desugar (para registrar la clase y el campo sintéticos, que la re-atribución tiene que ver) y el <code>enum extends Enum</code> implícito (§8.9, de donde sale <code>ordinal()</code>). y (6) <strong><code>switch</code> con <em>type patterns</em></strong> (§14.11.1) → una cadena de <code>instanceof</code> dentro de un bloque <strong>etiquetado</strong>: cada brazo es un <code>if</code> <strong>suelto</strong> (no <code>else if</code>) para que una <strong>guarda que falla</strong> caiga al <code>case</code> siguiente, el binding se materializa con un cast (<code>T v = (T) $t</code>, porque el <code>instanceof</code> del AST no lleva variable), se sale con <code>break</code> etiquetado —omitido cuando el brazo ya retorna, para no generar código inalcanzable— y un selector <code>null</code> sin <code>case null</code> lanza <strong>NPE</strong>. Esto exigió además que <strong>Attribute</strong> <em>bindee</em> la variable de patrón (visible en la guarda y en el cuerpo) y que <strong>Flow</strong> la vea siempre asignada dentro del brazo, como la del <code>catch</code>. y (7) el <strong><em>guard</em> <code>$assertionsDisabled</code></strong> del <code>assert</code> (§14.10): la aserción pasa a <code>if (!$assertionsDisabled &amp;&amp; !cond) throw …</code>, con un campo sintético <code>static final boolean</code> y el <code>&lt;clinit&gt;</code> que lo calcula de <code>C.class.desiredAssertionStatus()</code> — es lo que hace que una aserción <strong>no cueste nada</strong> con las aserciones apagadas (el default), porque al ser <code>static final</code> el JIT lo pliega. Con eso la pasada <strong>no tiene reescrituras pendientes</strong>, y además <strong>materializa los miembros implícitos de un <code>record</code></strong> (campos, constructor canónico y <em>accessors</em>): sus símbolos ya venían de <code>enter</code> —por eso <code>p.x()</code> resolvía— pero sin cuerpo en el AST el <code>.class</code> los referenciaba <strong>sin declararlos</strong>, y un <code>record</code> compilaba a una clase <strong>vacía</strong>. Y ya está la <strong>síntesis equivalente del <code>enum</code></strong> (§8.9.3): las constantes como campos, el <code>$VALUES</code>, el constructor privado <code>(String, int)</code> —lo único que puede darle a <code>java.lang.Enum</code> su nombre y su ordinal— y <code>values()</code>/<code>valueOf()</code>. Destrabarla obligó a cerrar antes la <strong>invocación explícita de constructor</strong> (<code>super(...)</code>/<code>this(...)</code>, §8.8.7.1), que <strong>no existía</strong> —el parser la producía y la atribución la rechazaba, así que no se podía heredar de ninguna clase con constructor parametrizado— y a sumar <strong><code>java.lang.Enum</code></strong> a <code>bootstrap/</code>+<code>boot/</code>, sin la cual el <code>.class</code> emitido no se verificaba ni corría. En el camino salieron a la luz cuatro fugas <strong>silenciosas</strong> del back-end, ya cerradas: el emisor <strong>nunca generaba <code>&lt;clinit&gt;</code></strong> (el <code>$VALUES</code>, el <code>$SwitchMap</code> y el guard del <code>assert</code> quedaban declarados y en cero), los <strong>inicializadores de campo</strong> (<code>int v = 7;</code>) <strong>no llegaban al <code>.class</code></strong>, <code>a.length</code> se tipaba <code>Unresolved</code> —y <code>i &lt; a.length</code> salía comparando con <code>if_acmpne</code>— y un <code>new</code> de un tipo sin resolver no emitía nada. Falta el <code>enum</code> con constantes <strong>parametrizadas</strong> (<code>ROJO("rojo")</code>) o constructor propio; el boxing/erasure queda para <code>TransTypes</code>. Desde entonces el desugar ganó tres reescrituras grandes: <strong>LambdaToMethod</strong> (lambdas → método sintético <code>lambda$…</code> + <code>invokedynamic</code> con <code>LambdaMetafactory</code>, capturando locales y <code>this</code>), las <strong>referencias a método</strong> y el <code>record</code> por <code>ObjectMethods</code> (ambos por un nodo <code>Indy</code> genérico), y la <strong>captura de <code>this$0</code></strong> de las clases internas de instancia. Un binario <strong><code>javac-step</code></strong> recorre las cuatro fases <strong>paso a paso</strong> (tokens → AST → tabla de símbolos → chequeo), el modo <code>--desugar</code> muestra el árbol bajado, y la semántica está citada a la JLS 25 en <code>docs/pasada2-attribute.md</code> y <code>docs/semantica-jdk25.md</code>.</p>
<p><strong>Fase B — Codegen (B3): objetos y flujo de control.</strong> El <code>compile()</code> corre ya las <strong>pasadas</strong> en el orden de javac (<code>Enter → Attribute → Check → Flow → Desugar → re-Attribute → Codegen</code>): <strong>Flow antes que Desugar</strong>, para que sus errores apunten al código fuente y no al bajado, y una <strong>re-atribución</strong> después, porque las reescrituras dejan nodos frescos sin <code>ty</code>/<code>binding</code>/<code>slot</code> y el emisor los necesita para elegir el opcode. Un programa con errores semánticos <strong>no emite</strong> <code>.class</code>. El <strong>escritor</strong> (<code>class_writer</code>) es propio y desacoplado: constant pool con deduplicación —incluidas las entradas <code>String</code>, <code>Long</code>, <code>Double</code> y <code>Float</code>, con el <strong>hueco</strong> que la JVMS exige tras cada <code>Long</code>/<code>Double</code> (ocupan dos entradas, y si no se reserva se corren todos los índices)— más la sección de <strong>campos</strong> y los atributos <code>Code</code>/<code>SourceFile</code>. El <strong>emisor</strong> cubre hoy: literales de todos los tipos (<code>ldc2_w</code> para los anchos), la <strong>promoción numérica binaria</strong> (§5.6.2) con los doce opcodes <code>x2y</code> —<code>longA + 1</code> inserta el <code>i2l</code>, y los desplazamientos quedan exceptuados (§15.19)—, <strong>objetos</strong> (<code>new</code>+<code>dup</code>+<code>invokespecial</code>, <code>getfield</code>/<code>putfield</code>, <code>getstatic</code>/<code>putstatic</code>, <code>invokevirtual</code>, <code>checkcast</code>), <strong>asignación</strong> a local y a campo, y <strong>flujo de control</strong> completo (<code>if</code>/<code>else</code>, <code>while</code>, <code>do-while</code>, <code>for</code>, <code>break</code>, <code>continue</code>) con etiquetas y parcheo de saltos, comparaciones por categoría y <code>&amp;&amp;</code>/<code>||</code> compilados como <strong>saltos</strong> —cortocircuito real, no un booleano calculado—. Verificado <strong>ejecutando en la JVM propia</strong>: <code>fib(10)=55</code> recursivo con rama, <code>fact(5)=120</code>, <code>break</code>/<code>continue</code>, aritmética <code>long</code>/<code>double</code>, y un objeto completo (<code>new M(10); m.add(5); m.get()</code> → 15). Tres bugs de corrección salieron de ahí: los constructores no emitían el <code>super()</code> implícito (§8.8.7, dejando el <code>this</code> sin inicializar), el <code>.class</code> no declaraba sus <strong>campos</strong> (referenciaba un <code>fieldref</code> inexistente) y faltaba el <code>Unary</code> de negación. También emite <strong>excepciones</strong>: <code>try</code>/<code>catch</code> con su tabla, <code>throw</code>, y el <code>finally</code> <strong>duplicado</strong> en la salida normal y en un handler <em>catch-all</em> que re-lanza (§14.20.2) —la v69 ya no acepta <code>jsr</code>/<code>ret</code>, así que duplicar es la única vía—. Eso, de paso, <strong>reubicó</strong> el <em>inlining</em> del <code>finally</code>, que teníamos anotado como tarea pendiente del <em>desugar</em>: no es una reescritura de AST sino trabajo del emisor. Y está la <strong><code>StackMapTable</code></strong> (§4.7.4), la pieza que separa «corre en mi JVM» de «corre en cualquier JVM»: el frame de cada destino de salto se calcula como el <strong>merge</strong> de los estados que llegan ahí (un slot que no coincide en todos los caminos pasa a <code>Top</code>), siempre en forma <code>full_frame</code>, y los <em>handlers</em> llevan <code>stack = [E]</code> con el frame <strong>forzado</strong>, porque no los alcanza ningún salto. La validación es el <strong><em>cross-check</em> del verificador propio</strong> contra su propia inferencia por punto fijo — que ya cazó un bug real: decidir «¿hace falta frame acá?» al fijar la etiqueta se saltaba el tope de los bucles, porque el <code>goto</code> hacia atrás se emite <em>después</em>. Y tiene <strong>barrera de seguridad</strong>: <code>generate()</code> devuelve <code>Result</code>, así que una construcción no soportada <strong>falla con posición</strong> en vez de emitir un <code>.class</code> a medias. Eso destapó al toque dos agujeros que el propio desugar venía alimentando en silencio —<code>instanceof</code> (todo <em>pattern switch</em>) y la creación de <strong>arrays</strong> (todo <em>varargs</em>)—, ya cerrados: el emisor cubre hoy <code>newarray</code>/<code>anewarray</code>, <code>arraylength</code>, las familias <code>Xaload</code>/<code>Xastore</code>, los inicializadores y el <code>instanceof</code>, de modo que un <em>pattern switch</em> y una llamada varargs <strong>compilan y corren</strong>. Y se cerró el resto del flujo de control: el <strong>ternario</strong> con sus dos ramas <strong>promovidas al tipo del todo</strong> (§15.25), el <strong><code>switch</code></strong> como <code>tableswitch</code> o <code>lookupswitch</code> según la <strong>densidad</strong> de sus etiquetas —con el relleno de alineación a 4 bytes y los desplazamientos de 4, preservando el <em>fall-through</em> de la forma de dos puntos y cortándolo en la de flecha—, los <code>break</code>/<code>continue</code> <strong>con y sin etiqueta</strong> sobre una pila de sentencias «de las que se puede salir» —un <code>switch</code> interpuesto captura un <code>break</code> pero <strong>no</strong> un <code>continue</code> (§14.16)— más el <strong>bloque etiquetado</strong>, y <code>synchronized</code> con <code>monitorexit</code> en <strong>las dos</strong> salidas, la normal y un handler <em>catch-all</em> que re-lanza. De yapa, el <strong><code>switch</code> sobre <code>String</code></strong> pasó a compilar de punta a punta: el desugar ya lo bajaba a dos switches sobre <code>int</code>, pero ninguno de los dos se podía emitir. Cerrar eso destapó el último agujero <strong>silencioso</strong> del emisor, y en la pieza más delicada: la pila de operandos se llevaba como un <strong>simple contador de altura</strong>, pero un frame del <code>StackMapTable</code> tiene que declarar <strong>los tipos</strong> de lo que hay en ella. Mientras todos los destinos de salto caían en bordes de sentencia —con la pila vacía— no se notaba; el ternario los mete en <strong>medio de una expresión</strong>: en <code>new M(a &gt; 0 ? 1 : 2)</code> el <code>new</code> y su <code>dup</code> siguen vivos en la pila, y el frame los declaraba como pila vacía. El <code>.class</code> salía igual —la barrera no ve esto, porque el emisor cree estar emitiendo bien— y lo rechazaba recién el verificador. La pila pasó a llevarse <strong>tipada</strong> (48 puntos de ajuste), lo que de paso hizo representable el tipo <strong><code>uninitialized</code></strong> (tag 8), que identifica un objeto recién creado por el <strong>offset de su <code>new</code></strong> y, corrido su <code>&lt;init&gt;</code>, pasa a definitivo en todas sus apariciones —pila y locales— (§4.10.2.4). <strong>Después</strong> se sumó el <strong><code>invokedynamic</code></strong> (opcode <code>0xba</code>): el pool ganó <code>MethodHandle</code>/<code>MethodType</code>/<code>InvokeDynamic</code> y el atributo <code>BootstrapMethods</code>, y un nodo <code>Indy</code> <strong>genérico</strong> (bootstrap + argumentos estáticos tipados) sirve tanto al <code>LambdaMetafactory</code> de las lambdas/<em>method refs</em> como al <code>ObjectMethods</code> de un <code>record</code> — con el emisor empujando las capturas y el verificador propio validando el call site. Y se cerró el frente de <strong>clases internas, locales y anónimas</strong>: la <strong>captura del entorno</strong> —<code>this$0</code> de la instancia envolvente y <code>val$x</code> de los locales <em>effectively-final</em>, con un pase <code>hoist_anonymous</code> que baja cada <code>new T(){…}</code> a una local sintética <code>Outer$N</code>—, las tres formas <strong>corren y verifican</strong>. Por último, la <strong>emisión de interfaces propias</strong>: el <code>.class</code> de una <code>interface</code> sale con <code>ACC_INTERFACE</code> y sus métodos <strong>abstractos sin <code>Code</code></strong>, y se sumó <strong><code>invokeinterface</code></strong> (con <code>InterfaceMethodref</code>) para las llamadas sobre un receptor de interfaz —lo que desbloqueó las <strong>anónimas sobre una interfaz</strong> (<code>new Runnable(){…}</code>, el caso más común) y las lambdas contra interfaces funcionales propias—.</p>
<p class="exito"><strong>✓ Siguiente (compilador):</strong> el <strong>frente grande de <code>invokedynamic</code> quedó cerrado</strong> —<strong>lambdas</strong> (con un <code>LambdaToMethod</code> propio), <strong>referencias a método</strong> (las cuatro formas del §15.13.1) y el <code>equals</code>/<code>hashCode</code>/<code>toString</code> de un <strong><code>record</code></strong> (por <code>ObjectMethods</code>) se tipan, emiten y <strong>verifican</strong>; la ejecución del call site vive en la JVM (KajiJDK)—. Se cerraron además las <strong>clases internas, locales y anónimas</strong> —captura del entorno completa (<code>this$0</code> + <code>val$</code>, con un pase <code>hoist_anonymous</code> que baja la anónima a una local sintética <code>Outer$N</code>), las tres <strong>corren y verifican</strong>, incluidas las formas <strong>cualificadas</strong> (<code>Outer.this</code> → cadena de <code>this$0</code>, con desambiguación de campo sombreado y multinivel; y <code>outer.new Inner()</code>, que empuja el calificador como <code>this$0</code>) y los <strong>argumentos de super</strong> (<code>new ArrayList(10){…}</code>), la <strong>colisión de nombres</strong> (dos locales homónimas se renombran a únicas <code>1L</code>/<code>2L</code> con alcance léxico por bloque), la <strong>local-en-local</strong> (anidada como tipo de su local envolvente) y el <strong>multinivel implícito</strong> (acceso a un miembro del abuelo vía <code>this$0</code> encadenado) —las clases internas quedan <strong>sin colas</strong>—; el parser (B1) quedó <strong>sin bordes</strong> (anotaciones de tipo/uso <code>List&lt;@NonNull String&gt;</code>, <code>&lt;@Foo T&gt;</code>, <code>new &lt;T&gt;Foo()</code>, y las de <strong>nivel de array</strong> <code>String @A []</code>); y la <strong>emisión de interfaces propias</strong> cerró el caso más común de anónima (<code>new Runnable(){…}</code>) sumando <code>invokeinterface</code>. Y se cerró el <strong>boxing dirigido por tipo</strong> (<em>TransTypes</em>): una pasada propia inserta <code>Integer.valueOf</code>/<code>x.intValue</code> en cada contexto de conversión (asignación, <code>return</code>, argumentos, operandos, cuerpo de lambda), cerrando el caso insignia de las <em>poly expressions</em> —<code>Function&lt;Integer,Integer&gt; f = x -&gt; x + 1</code> de punta a punta, validado por el verificador (ejecutar <code>valueOf</code> es de la JVM)—. Y se cerró la <strong>inferencia en posición de argumento</strong> (dos fases §15.12.2.6): un argumento <strong>poly</strong> —lambda, <em>method ref</em>, diamante— se re-atribuye con el tipo de su parámetro como <em>target</em> una vez resuelta la sobrecarga, lo que por fin baja una <strong>lambda-argumento</strong> (<code>call(x -&gt; x + 1)</code>) a <code>invokedynamic</code>. Y se cerró el <strong><code>enum</code> con constantes parametrizadas</strong> (<code>ROJO("rojo")</code>): al ctor propio se le antepone <code>(String, int)</code> + <code>super(...)</code> y cada constante se construye con sus argumentos. Y se **liquidaron los sueltos** que quedaban: <code>@Override</code>/<code>throws</code> en el chequeo de override (§8.4.8.3/§9.6.4.4), <code>RuntimeVisibleAnnotations</code> (§4.7.16), el <strong>plegado de constantes</strong> en un <code>case</code> (§15.28), las <strong>formas comprimidas del <code>StackMapTable</code></strong> (§4.7.4), los <strong>inicializadores de campo atribuidos</strong> (§8.3.2), la <strong>desambiguación de sobrecargas por el arg lambda</strong> (§15.12.2.5) y el <strong>boxing <em>wrapper</em>→primitivo más ancho</strong> (<code>long l = anInteger</code>). Y se cerró el primer <strong>frente grande de Java SE 25</strong>: <strong><code>sealed</code>/<code>non-sealed</code> + exhaustividad de <code>switch</code></strong> (§8.1.1.2/§9.1.1.4/§8.1.6/§14.11.1.1) — el parser toma las keywords restringidas y el <code>permits</code>, el grafo gana los <strong>subtipos autorizados</strong> (con el <code>permits</code> implícito de la unidad), <strong>Check</strong> valida las reglas de sellado y exige <strong>exhaustividad</strong> a la <code>switch</code>-expresión y a la sentencia con <em>patterns</em> (jerarquía <code>sealed</code> recursiva, todas las constantes de un <code>enum</code>, <code>true</code>/<code>false</code> de un <code>boolean</code>; una guarda no cubre), y el <code>.class</code> lleva el atributo <strong><code>PermittedSubclasses</code></strong> (§4.7.31) — de yapa se corrigió que <code>case RED -&gt; 1</code> se leía como lambda. Con eso la lista <strong>tracked</strong> del compilador queda vacía. Y se cerró el segundo frente de <strong>Java SE 25</strong>: los <strong>text blocks</strong> (§3.10.6) — el parser decodifica el <code>"""…"""</code> con el algoritmo del JLS (normalización de terminadores, descarte de la línea de apertura, <strong>indentación incidental</strong> mínima + blancos finales, y luego los escapes con <code>s</code> y la <strong>continuación de línea</strong> <code>&lt;LF&gt;</code>), dando un <code>String</code> común que se emite como cualquier literal. Y se cerró el tercer frente: la <strong><em>capture conversion</em></strong> (§5.1.10) con <strong>variables de captura frescas</strong> (un <code>RType::Capture{id,upper,lower}</code> con cotas inline e <code>id</code> fresco para la <em>freshness</em>): se aplica en acceso a miembros y en argumentos, el subtipado gana <code>CAP &lt;: upper</code> y <code>s &lt;: CAP</code> por la cota inferior, y el caso insignia <strong><code>swap(List&lt;?&gt;)</code> → <code>swapHelper(List&lt;T&gt;)</code></strong> ahora <strong>infiere</strong> <code>T</code>. Y se cerró el cuarto frente: los <strong><em>bridge methods</em></strong> (§8.4.8.3/§15.12.4.5) — el emisor sintetiza los puentes (<code>ACC_BRIDGE</code>+<code>ACC_SYNTHETIC</code>) que hacen que la sobrescritura funcione en bytecode cuando la <em>erasure</em> difiere, por un <strong>parámetro genérico</strong> borrado (<code>Comparable&lt;Foo&gt;</code> → <code>compareTo(Object)</code>, <code>Node&lt;Integer&gt;.set(Integer)</code> → <code>set(Object)</code>) o por un <strong>retorno covariante</strong> (<code>A.get():Object</code> / <code>B.get():String</code>), reenviando al método real con <code>checkcast</code> + <code>invokevirtual</code>; verifica y <strong>despacha</strong> de punta a punta. Y se cerró el último frente grande: los <strong>módulos</strong> (§7.7/§4.7.25) — el parser toma el <code>module-info.java</code> con las cinco directivas (keywords restringidas), el emisor produce un <code>module-info.class</code> fiel (<code>ACC_MODULE</code>, atributo <strong><code>Module</code></strong> con <code>CONSTANT_Module</code>/<code>Package</code> y el <code>requires java.base</code> mandated), y <strong>Check</strong> valida la buena formación (directivas duplicadas, auto-<code>requires</code>); la resolución del grafo es de runtime (JPMS, track aparte). <strong>Con esto, los cinco frentes grandes de Java SE 25 quedan cerrados del lado del compilador</strong> —<em>sealed</em> + exhaustividad, text blocks, captura de wildcards, <em>bridge methods</em> y módulos (B5)—. Lo que le queda al compilador **no** son features de lenguaje sino **fidelidad** (B6). Ya se cerraron los cuatro primeros atributos: <strong>✅ <code>Signature</code></strong> (§4.7.9) —los genéricos ya **no se pierden** en el binario—, <strong>✅ <code>InnerClasses</code></strong> (§4.7.6), <strong>✅ <code>EnclosingMethod</code></strong> (§4.7.7) —anidamiento y método envolvente de local/anónimas— <strong>✅ <code>Record</code></strong> (§4.7.30) —un <code>record</code> ahora extiende <code>java.lang.Record</code>, es <code>final</code> y lleva el atributo con sus componentes, así <code>isRecord()</code>/<code>getRecordComponents()</code> andan (y se cerraron dos bugs latentes: el parseo de un record genérico y la erasure del descriptor de una variable de tipo)— y <strong>✅ <code>NestHost</code>/<code>NestMembers</code></strong> (§4.7.28/§4.7.29) —el <em>nest</em> plano (host + nestmates transitivos) que da acceso privado entre anidadas—. <strong>✅ <code>NestHost</code>/<code>NestMembers</code></strong> (el <em>nest</em>) y <strong>✅ <code>MethodParameters</code></strong> (§4.7.24, los nombres de parámetros para la reflexión). De la **inferencia** ya se cerraron el lado escritura de la captura por llamada y el <strong>lub con intersección</strong> (<code>Number &amp; Comparable</code>); queda solo el <em>solver</em> transitivo de §18 (bounds cruzados). Del lado de los <strong>atributos ya no queda fidelidad reflexiva pendiente</strong>: se cerraron <code>Exceptions</code>, <code>LineNumberTable</code>, <code>LocalVariableTable</code>, <code>ConstantValue</code> y <strong>todas</strong> las type annotations (<code>RuntimeVisibleTypeAnnotations</code>, §4.7.20 — parámetros de tipo, cotas, extends/implements, throws, field/return/param y los targets de <code>Code</code>: cast/<code>instanceof</code>/<code>new</code>/local), todo verificado byte-fiel contra el <code>javap</code> de Temurin.</p>
<p><strong>Fase H — KajiLibrary (la biblioteca, dogfooded).</strong> La biblioteca propia vive en <code>KajiLibrary/</code>, se compila con el <code>javac</code> propio y corre en KajiJDK. <strong>H1</strong> cerró los <em>tiers</em> 0–5 (el esqueleto: <code>Object</code>/<code>String</code>/<code>StringBuilder</code>, excepciones, interfaces funcionales, colecciones); <strong>H2</strong> (utilidades y conveniencia de <code>java.util</code>) está <strong>en curso</strong>; <strong>H3</strong> (<code>java.util.stream</code>), <strong>H4</strong> (<code>java.time</code>) y <strong>H5</strong> (<code>java.util.regex</code>) tienen su <strong>núcleo cerrado</strong>, y <strong>H6</strong> (<code>java.util.Formatter</code>/<code>String.format</code>) está <strong>diseñado</strong>. Lo acoplado al runtime —<code>java.nio</code>, <code>java.util.concurrent</code> y la reflexión completa— queda en el backlog (<strong>H7</strong>): esa parte avanza por el track de la JVM, no por el de la biblioteca.</p>
<p class="exito"><strong>✓ Siguiente:</strong> con <strong>H4 (JMM relajado)</strong>, <strong>H5 (CAS — <code>compareAndSwap</code> + los <code>Atomic*</code>)</strong> y el <strong>núcleo + primeras utilidades de H6</strong> (<strong>AQS</strong> exclusivo+compartido → <code>ReentrantLock</code>/<code>Condition</code>, <code>ReentrantReadWriteLock</code>, <code>Semaphore</code>, <code>CountDownLatch</code>, <code>CyclicBarrier</code>, <code>ArrayBlockingQueue</code>/<code>LinkedBlockingQueue</code>/<code>PriorityBlockingQueue</code>/<code>DelayQueue</code>, el framework <code>Executor</code> (<code>ThreadPoolExecutor</code> + <code>ScheduledThreadPoolExecutor</code>), <code>ConcurrentHashMap</code> y <code>CompletableFuture</code>) ya hechos y validados por el oráculo, sigue el <strong>volumen restante de <code>java.util.concurrent</code></strong> (deques, <em>resize</em>/lecturas lock-free del <code>ConcurrentHashMap</code>) y los <strong>locks finos por estructura</strong> (ver la sección «Concurrencia»). Los green threads quedan como substrato elegible y <strong>oráculo determinista</strong> de corrección (<code>green≡os-gil≡os</code>) — clave para cazar el <strong>bug residual</strong> del modo paralelo (referencia <em>stale</em> bajo GC intenso; guard parcial landeado, próximo paso ThreadSanitizer). El paralelismo de cómputo del LLM vive en <strong>kernels off-heap</strong> (rayon/CUDA).</p>
<p><strong>Fase I — el depurador (JPDA: JVMTI · JDWP · jdb).</strong> La VM propia se depura, y no solo desde adentro. El back-end <strong>JVMTI</strong> (módulo <code>jvmti.rs</code>) es un modelo <em>push</em>: un <code>JvmtiAgent</code> registra <em>callbacks</em> (<code>breakpoint</code>/<code>single_step</code>/<code>method_entry</code>/<code>method_exit</code>/<code>exception</code>) que la VM dispara en medio de <code>step()</code>, pasándole un <code>JvmtiEnv</code> <strong>acotado</strong> para re-inspeccionar/controlar la ejecución — con el <em>fast-path</em> por <code>Capabilities</code> («pagás por lo que activás») y, la pieza fina, <strong>ganarle al borrow checker sin <code>unsafe</code></strong>: la VM <strong>posee</strong> al agente pero al disparar un evento <strong>destructura <code>self</code> en campos disjuntos</strong> (<code>agent</code> ⊥ los del <code>env</code>), y acotar el <code>env</code> es lo que lo hace compilar. Sobre eso corren <strong>dos front-ends</strong>: el <strong><code>jdb</code> pragmático</strong> (I1, <em>in-process</em>) —un binario cuyo prompt (<code>step</code>/<code>cont</code>/<code>break</code>/<code>where</code>/<code>locals</code>/<code>print</code>) corre <em>dentro</em> del callback— y el <strong>camino fiel por protocolo</strong> (I3): el <strong>JDWP</strong> completo —codec puro (handshake, framing, serialización tipada big-endian), <em>command handlers</em> (<code>VirtualMachine</code>/<code>EventRequest</code>) sobre una <code>JdwpSession</code>, transporte genérico sobre <code>Read</code>+<code>Write</code>, y el <strong>bridge JVMTI↔JDWP</strong> que <strong>empuja</strong> los eventos como <em>Composite packets</em> y <strong>aplica</strong> los <code>EventRequest</code> del cliente a la VM real (un breakpoint pedido por cable <strong>frena de verdad</strong>)—. El binario <strong><code>jvm-jdwp</code></strong> expone la VM como servidor <code>dt_socket</code> (equivale a <code>-agentlib:jdwp=…,server=y,suspend=y</code>): <strong>probado end-to-end</strong> con un cliente JDWP <strong>externo</strong> (otro proceso) que attachea, handshakea, pide single-step y recibe los eventos frenando/reanudando por el socket hasta el retorno. Ambos caminos son <strong>dos front-ends sobre el mismo JVMTI</strong>. Encima del cable está la <strong>JDI</strong> (I4, <code>jdi.rs</code>): la API cliente de alto nivel que habla por <em>mirrors</em> tipados (<code>Vm</code>/<code>Location</code>/<code>Event</code>) en vez de bytes —contraparte del <em>bridge</em> del lado del debugger, depende solo del codec—, con el binario <strong><code>jdi-attach</code></strong> que maneja la ejecución por socket <strong>dos procesos, todo propio</strong> (reemplazó al cliente de Python). Y la <strong>fidelidad</strong> (I5) trajo las dos capacidades que hacen a un depurador <strong>de verdad</strong>: <strong>(a) inspección de pila y variables</strong> con la VM parada (<code>ThreadReference.Frames</code>, <code>StackFrame.GetValues</code>) leyendo el mismo <code>JvmtiEnv</code>; y <strong>(b) breakpoints por línea de fuente</strong> (<code>ClassesBySignature</code>, <code>ReferenceType.Methods</code>, <code>Method.LineTable</code>) — el cliente traduce <code>Add.java:3</code> a <code>(methodID, índice)</code>. El muro acá fue que listar métodos con su <code>MethodId</code> real exige resolverlos (<code>&amp;mut metaspace</code>) pero en un callback el metaspace es inmutable; la salida es un <strong>snapshot</strong> capturado al attachear, y como <code>resolve_method</code> <strong>cachea</strong>, el <code>methodID</code> del snapshot es <em>el mismo</em> que la VM usa al correr, así el breakpoint frena. <strong>Probado en vivo</strong> con cliente propio: <code>jdi-attach --break 3</code> contra <code>jvm-jdwp Add.add 3 4</code> resuelve <code>LAdd;.add:3</code>, la VM <strong>frena en el breakpoint</strong>, se leen los locales (<code>slot0=3, slot1=4</code>) y reanuda a <code>Some(Int(7))</code>. Y —la vara de la cumbre— <strong>validado contra el <code>jdb</code> real de Temurin</strong> (el mismo <code>com.sun.jdi</code> que usa IntelliJ Platform): attachea por <code>dt_socket</code> <strong>sin saber que la VM es propia</strong>, pone un breakpoint por línea y lo frena — <code>VM Started: Add.add(), line=3 bci=0</code> · <code>Breakpoint hit: Add.add(), line=3 bci=0</code> · <code>where → [1] Add.add (Add.java:3)</code>. Destrabar ese attach pidió cerrar la superficie que un JDI real emite —las variantes <strong><code>WithGeneric</code></strong>, la negociación (<code>Capabilities</code>, <code>ClassPaths</code>, grupos de hilos), el evento automático <strong><code>VMStart</code></strong>, y los <strong>ids no-cero</strong> (JDWP reserva el <code>0</code> para <em>null</em>: classID, threadID y <code>methodID = MethodId + 1</code>)—, todo hallado iterando contra <code>jdb</code> como oráculo. Y se cerró el resto de la superficie interactiva, cada gap hallado contra <code>jdb</code>: <strong><code>locals</code></strong> por nombre y valor (<code>Method.VariableTable</code> desde la <code>LocalVariableTable</code> del <code>.class</code> compilado con <code>-g</code>); los <strong>field watchpoints</strong> (I2) por protocolo —<code>watch Watched.value</code> → <code>Field (Watched.value) is 0, will be 7</code>—, que necesitaron el gancho central <code>fire_field_watchpoint</code> (decodifica el opcode de campo read-only, sin tocar los handlers), el modificador <code>FieldOnly</code>, y las caps <code>canWatchField*</code>; y la <strong>inspección de objetos</strong> que ese display exige —el <code>JvmtiEnv</code> ganó acceso <em>read-only</em> al <strong>heap</strong> y <code>ObjectReference.ReferenceType/GetValues</code> leen la clase real de un objeto (por el mirror de su header) y el valor de sus campos (por su offset precomputado)—. Con eso la sesión interactiva de <code>jdb</code> queda <strong>100% limpia</strong> —<code>threads</code>, <code>classes</code>, <code>stop at</code>/<code>cont</code>, <code>where</code>, <code>locals</code>, <code>watch</code>, todos sin errores—. Lo único fuera de alcance es correr el IntelliJ <strong>gráfico</strong> (no headless acá); <code>jdb</code> es el mismo <code>com.sun.jdi</code> que hay debajo. <strong>El hito I queda completo</strong>: JVMTI (I0–I2, con field watchpoints), JDWP (I3), JDI (I4) y fidelidad validada contra el debugger real (I5).</p>
<p><strong>Fase B — el compilador (front-end + semántica + chequeo + flujo + desugar).</strong> El <code>javac</code> propio (crate <strong>desacoplado</strong> en <code>src/javac/</code>, cuyo único contrato es el formato <code>.class</code>) tiene el front-end entero, las <strong>dos pasadas</strong> semánticas, el análisis de flujo y el desugar. <strong>Front-end:</strong> <strong>lexer</strong> (las 115 <code>TokenKind</code> de JDK 25, <em>maximal munch</em>, BOM), <strong>parser</strong> por <em>recursive descent</em> con <strong>todas las sentencias</strong> (incl. <code>switch</code> con patterns/guards, <code>try</code>-with-resources, <code>synchronized</code>), <code>enum</code>/<code>record</code>/<code>@interface</code> y <strong>genéricos completos</strong> — argumentos de tipo, <em>wildcards</em>, cotas y el diamante, partiendo <code>&gt;&gt;</code>/<code>&gt;&gt;&gt;</code> con un <em>undo log</em> para no corromper el <em>backtracking</em>. El <strong>lenguaje moderno</strong> también entró: <strong>lambdas</strong> (con el desambiguador <code>(</code>-cast-vs-paréntesis-vs-parámetros por <em>lookahead</em> de la flecha), <strong>referencias a método</strong> (incl. <code>T[]::new</code>), <strong>clases anónimas y locales</strong>, el <strong>type witness</strong> (<code>Collections.&lt;String&gt;emptyList()</code>, que además se <strong>aplica</strong> —fija los parámetros de tipo en vez de inferirlos—) y las <strong>anotaciones</strong> (§9.7), que antes se <strong>descartaban</strong> y ahora se cuelgan de la declaración. Cada forma se parsea entera y se guarda fiel en el AST; compilarlas es de fases posteriores, y hasta entonces la barrera del emisor las corta. <strong>Pasada 1 (Enter/MemberEnter):</strong> tabla de símbolos con paquetes y tipos <strong>anidados</strong>, detección de duplicados y <strong>ciclos de herencia</strong>, un <strong>class finder real</strong> que lee <code>.class</code> del classpath (lector propio) <strong>incluido el atributo <code>Signature</code></strong> — así <code>java.util.List</code> carga con su <code>&lt;E&gt;</code> y <code>List.get</code> devuelve <code>E</code>, no <code>Object</code> — resolución con <strong>errores posicionados</strong>, y el <strong>grafo tipado persistido</strong> como salida. <strong>Pasada 2 (Attribute):</strong> resuelve nombres y tipa los cuerpos <strong>decorando el AST in situ</strong> (tipo + <em>binding</em> por nodo, slot por local con categoría-2, al estilo <code>JCTree.type</code>/<code>sym</code>); con <strong>overload resolution en 3 fases</strong> (estricta → boxing → varargs, con <em>most specific</em> y ambigüedad) y <strong>genéricos</strong> reales — subtipado con <em>containment</em> de wildcards (invariancia), sustitución del receptor, <code>lub</code>/<code>glb</code> e <strong>inferencia</strong> de los argumentos de tipo de una llamada (Cap. 18). El álgebra de tipos vive en un módulo <code>types</code> (el <code>Types</code> de javac) y la inferencia en <code>infer</code> (el <code>Infer</code>). <strong>Pasada Flow (<code>flow</code>):</strong> sobre el AST decorado, la <strong>asignación definitiva</strong> del Cap. 16 — con los dos conjuntos duales <em>definitely assigned</em> / <em>definitely unassigned</em>, los flujos <em>when-true/when-false</em> de <code>&amp;&amp;</code>/<code>||</code>/<code>!</code>, las reglas de variables <strong><code>final</code></strong> (no reasignar, asignación única, multi-catch implícito), la <strong>alcanzabilidad</strong> (§14.21) y el <code>break</code>/<code>continue</code> <strong>etiquetados</strong> (§14.15/§14.16: la pila de contextos de <code>break</code> lleva la etiqueta a la que responde cada uno). <strong>Pasada Desugar (<code>desugar</code>):</strong> baja el azúcar de <strong>sentencias</strong> (<code>for-each</code> → <code>for</code> indexado o <code>while</code>+<code>Iterator</code>, <code>try</code>-with-resources → <code>try/finally</code>+<code>close()</code>, <code>assert</code> → <code>if(!cond) throw</code>) y de <strong>expresiones</strong> (concatenación de <code>String</code> → cadena de <code>StringBuilder</code>, asignación compuesta <code>x op= y</code> → <code>x = (T)(x op y)</code> con el cast de reducción, y <strong>varargs</strong> → array en el <em>call site</em>) — verificado re-tipando el árbol bajado. En esta iteración se sumaron cuatro <em>rewrites</em> más: (1) <strong><code>++</code>/<code>--</code> en posición de descarte</strong> (§15.14.2/§15.15.2) — como sentencia (<code>x++;</code>, <code>--x;</code>) o en el <code>update</code> de un <code>for</code> — se reescriben a <code>x += 1</code> reusando el <em>lowering</em> de la asignación compuesta (mismo cast de reducción), mientras que en posición de <em>valor</em> (<code>y = x++</code>) quedan para el codegen (<code>iinc</code>/<code>dup</code>); (2) la <strong><code>switch</code>-expresión</strong> en posición de cola (§15.28) — <code>T v = switch…</code>, <code>return switch…</code>, <code>x = switch…</code>, <code>yield switch…</code> — se convierte en una <code>switch</code>-<em>sentencia</em> cuyos brazos <strong>asignan</strong> el valor (con temporal cuando hace falta); requiere <code>default</code> y etiquetas constantes, y los brazos con <strong>bloque</strong> o de <strong>dos puntos</strong> con <code>yield</code> (incluso adentro de un bucle o un <code>if</code>) se bajan reescribiendo cada <code>yield e</code> a <code>{ v = e; break $sw; }</code> con un <code>break</code> <strong>etiquetado</strong> sobre el <code>switch</code> generado —un <code>break</code> pelado solo saldría del bucle— mientras que el exhaustivo sin <code>default</code> y las switch-expresiones embebidas quedan como expresión sobre la pila; (3) <strong>try-with-resources con excepción suprimida</strong> (§14.20.3.1) — corrección de un bug: antes la excepción del <code>close()</code> <em>tapaba</em> la del cuerpo; ahora se preserva la primaria y la del <code>close()</code> queda <strong>suprimida</strong> (<code>Throwable.addSuppressed</code>), con la traducción completa del JLS (<code>$primary=null; try{…}catch($t){$primary=$t;throw;}finally{ if(r!=null){ if($primary!=null) try{r.close();}catch($x){$primary.addSuppressed($x);} else r.close(); } }</code>); (4) <strong><code>switch</code> sobre <code>String</code></strong> (§14.11) → <strong>dos</strong> switches sobre <code>int</code>: el primero mapea <code>s.hashCode()</code> a un índice sintético vía <code>equals()</code> (cadena <code>if/else</code> para las colisiones de hash) y el segundo replica los brazos indexados (preservando <em>fall-through</em> y <code>default</code>), con el <code>String.hashCode()</code> calculado en tiempo de compilación (algoritmo del JDK: <code>31·h + c</code> sobre unidades UTF-16, aritmética <code>i32</code> que envuelve); y (5) <strong><code>switch</code> sobre <code>enum</code></strong> (§14.11) con el <strong><code>$SwitchMap</code> fiel a javac</strong>: <code>switch(color)</code> → <code>switch(C$1.$SwitchMap$Color[color.ordinal()])</code>, donde <code>$SwitchMap$Color</code> es un <code>int[]</code> sintético alojado en una <strong>clase anidada <code>C$1</code></strong> y poblado en un <code>&lt;clinit&gt;</code> con un <code>try { map[Color.C.ordinal()] = k; } catch (NoSuchFieldError e) {}</code> por constante (robusto ante recompilación separada del enum). Esto requirió una <strong>máquina de síntesis a nivel clase</strong>: el miembro <code>Member::StaticInit</code> (con lo que los bloques <code>static { }</code> del usuario, antes <strong>descartados</strong>, ahora se conservan y analizan), la tabla de símbolos <strong>mutable</strong> en el desugar (para registrar la clase y el campo sintéticos, que la re-atribución tiene que ver) y el <code>enum extends Enum</code> implícito (§8.9, de donde sale <code>ordinal()</code>). y (6) <strong><code>switch</code> con <em>type patterns</em></strong> (§14.11.1) → una cadena de <code>instanceof</code> dentro de un bloque <strong>etiquetado</strong>: cada brazo es un <code>if</code> <strong>suelto</strong> (no <code>else if</code>) para que una <strong>guarda que falla</strong> caiga al <code>case</code> siguiente, el binding se materializa con un cast (<code>T v = (T) $t</code>, porque el <code>instanceof</code> del AST no lleva variable), se sale con <code>break</code> etiquetado —omitido cuando el brazo ya retorna, para no generar código inalcanzable— y un selector <code>null</code> sin <code>case null</code> lanza <strong>NPE</strong>. Esto exigió además que <strong>Attribute</strong> <em>bindee</em> la variable de patrón (visible en la guarda y en el cuerpo) y que <strong>Flow</strong> la vea siempre asignada dentro del brazo, como la del <code>catch</code>. y (7) el <strong><em>guard</em> <code>$assertionsDisabled</code></strong> del <code>assert</code> (§14.10): la aserción pasa a <code>if (!$assertionsDisabled &amp;&amp; !cond) throw …</code>, con un campo sintético <code>static final boolean</code> y el <code>&lt;clinit&gt;</code> que lo calcula de <code>C.class.desiredAssertionStatus()</code> — es lo que hace que una aserción <strong>no cueste nada</strong> con las aserciones apagadas (el default), porque al ser <code>static final</code> el JIT lo pliega. Con eso la pasada <strong>no tiene reescrituras pendientes</strong>, y además <strong>materializa los miembros implícitos de un <code>record</code></strong> (campos, constructor canónico y <em>accessors</em>): sus símbolos ya venían de <code>enter</code> —por eso <code>p.x()</code> resolvía— pero sin cuerpo en el AST el <code>.class</code> los referenciaba <strong>sin declararlos</strong>, y un <code>record</code> compilaba a una clase <strong>vacía</strong>. Y ya está la <strong>síntesis equivalente del <code>enum</code></strong> (§8.9.3): las constantes como campos, el <code>$VALUES</code>, el constructor privado <code>(String, int)</code> —lo único que puede darle a <code>java.lang.Enum</code> su nombre y su ordinal— y <code>values()</code>/<code>valueOf()</code>. Destrabarla obligó a cerrar antes la <strong>invocación explícita de constructor</strong> (<code>super(...)</code>/<code>this(...)</code>, §8.8.7.1), que <strong>no existía</strong> —el parser la producía y la atribución la rechazaba, así que no se podía heredar de ninguna clase con constructor parametrizado— y a sumar <strong><code>java.lang.Enum</code></strong> a <code>bootstrap/</code>+<code>boot/</code>, sin la cual el <code>.class</code> emitido no se verificaba ni corría. En el camino salieron a la luz cuatro fugas <strong>silenciosas</strong> del back-end, ya cerradas: el emisor <strong>nunca generaba <code>&lt;clinit&gt;</code></strong> (el <code>$VALUES</code>, el <code>$SwitchMap</code> y el guard del <code>assert</code> quedaban declarados y en cero), los <strong>inicializadores de campo</strong> (<code>int v = 7;</code>) <strong>no llegaban al <code>.class</code></strong>, <code>a.length</code> se tipaba <code>Unresolved</code> —y <code>i &lt; a.length</code> salía comparando con <code>if_acmpne</code>— y un <code>new</code> de un tipo sin resolver no emitía nada. Falta el <code>enum</code> con constantes <strong>parametrizadas</strong> (<code>ROJO("rojo")</code>) o constructor propio; el boxing/erasure queda para <code>TransTypes</code>. Desde entonces el desugar ganó tres reescrituras grandes: <strong>LambdaToMethod</strong> (lambdas → método sintético <code>lambda$…</code> + <code>invokedynamic</code> con <code>LambdaMetafactory</code>, capturando locales y <code>this</code>), las <strong>referencias a método</strong> y el <code>record</code> por <code>ObjectMethods</code> (ambos por un nodo <code>Indy</code> genérico), y la <strong>captura de <code>this$0</code></strong> de las clases internas de instancia. Un binario <strong><code>javac-step</code></strong> recorre las cuatro fases <strong>paso a paso</strong> (tokens → AST → tabla de símbolos → chequeo), el modo <code>--desugar</code> muestra el árbol bajado, y la semántica está citada a la JLS 25 en <code>docs/pasada2-attribute.md</code> y <code>docs/semantica-jdk25.md</code>.</p>
<p><strong>Fase B — Codegen (B3): objetos y flujo de control.</strong> El <code>compile()</code> corre ya las <strong>pasadas</strong> en el orden de javac (<code>Enter → Attribute → Check → Flow → Desugar → re-Attribute → Codegen</code>): <strong>Flow antes que Desugar</strong>, para que sus errores apunten al código fuente y no al bajado, y una <strong>re-atribución</strong> después, porque las reescrituras dejan nodos frescos sin <code>ty</code>/<code>binding</code>/<code>slot</code> y el emisor los necesita para elegir el opcode. Un programa con errores semánticos <strong>no emite</strong> <code>.class</code>. El <strong>escritor</strong> (<code>class_writer</code>) es propio y desacoplado: constant pool con deduplicación —incluidas las entradas <code>String</code>, <code>Long</code>, <code>Double</code> y <code>Float</code>, con el <strong>hueco</strong> que la JVMS exige tras cada <code>Long</code>/<code>Double</code> (ocupan dos entradas, y si no se reserva se corren todos los índices)— más la sección de <strong>campos</strong> y los atributos <code>Code</code>/<code>SourceFile</code>. El <strong>emisor</strong> cubre hoy: literales de todos los tipos (<code>ldc2_w</code> para los anchos), la <strong>promoción numérica binaria</strong> (§5.6.2) con los doce opcodes <code>x2y</code> —<code>longA + 1</code> inserta el <code>i2l</code>, y los desplazamientos quedan exceptuados (§15.19)—, <strong>objetos</strong> (<code>new</code>+<code>dup</code>+<code>invokespecial</code>, <code>getfield</code>/<code>putfield</code>, <code>getstatic</code>/<code>putstatic</code>, <code>invokevirtual</code>, <code>checkcast</code>), <strong>asignación</strong> a local y a campo, y <strong>flujo de control</strong> completo (<code>if</code>/<code>else</code>, <code>while</code>, <code>do-while</code>, <code>for</code>, <code>break</code>, <code>continue</code>) con etiquetas y parcheo de saltos, comparaciones por categoría y <code>&amp;&amp;</code>/<code>||</code> compilados como <strong>saltos</strong> —cortocircuito real, no un booleano calculado—. Verificado <strong>ejecutando en la JVM propia</strong>: <code>fib(10)=55</code> recursivo con rama, <code>fact(5)=120</code>, <code>break</code>/<code>continue</code>, aritmética <code>long</code>/<code>double</code>, y un objeto completo (<code>new M(10); m.add(5); m.get()</code> → 15). Tres bugs de corrección salieron de ahí: los constructores no emitían el <code>super()</code> implícito (§8.8.7, dejando el <code>this</code> sin inicializar), el <code>.class</code> no declaraba sus <strong>campos</strong> (referenciaba un <code>fieldref</code> inexistente) y faltaba el <code>Unary</code> de negación. También emite <strong>excepciones</strong>: <code>try</code>/<code>catch</code> con su tabla, <code>throw</code>, y el <code>finally</code> <strong>duplicado</strong> en la salida normal y en un handler <em>catch-all</em> que re-lanza (§14.20.2) —la v69 ya no acepta <code>jsr</code>/<code>ret</code>, así que duplicar es la única vía—. Eso, de paso, <strong>reubicó</strong> el <em>inlining</em> del <code>finally</code>, que teníamos anotado como tarea pendiente del <em>desugar</em>: no es una reescritura de AST sino trabajo del emisor. Y está la <strong><code>StackMapTable</code></strong> (§4.7.4), la pieza que separa «corre en mi JVM» de «corre en cualquier JVM»: el frame de cada destino de salto se calcula como el <strong>merge</strong> de los estados que llegan ahí (un slot que no coincide en todos los caminos pasa a <code>Top</code>), siempre en forma <code>full_frame</code>, y los <em>handlers</em> llevan <code>stack = [E]</code> con el frame <strong>forzado</strong>, porque no los alcanza ningún salto. La validación es el <strong><em>cross-check</em> del verificador propio</strong> contra su propia inferencia por punto fijo — que ya cazó un bug real: decidir «¿hace falta frame acá?» al fijar la etiqueta se saltaba el tope de los bucles, porque el <code>goto</code> hacia atrás se emite <em>después</em>. Y tiene <strong>barrera de seguridad</strong>: <code>generate()</code> devuelve <code>Result</code>, así que una construcción no soportada <strong>falla con posición</strong> en vez de emitir un <code>.class</code> a medias. Eso destapó al toque dos agujeros que el propio desugar venía alimentando en silencio —<code>instanceof</code> (todo <em>pattern switch</em>) y la creación de <strong>arrays</strong> (todo <em>varargs</em>)—, ya cerrados: el emisor cubre hoy <code>newarray</code>/<code>anewarray</code>, <code>arraylength</code>, las familias <code>Xaload</code>/<code>Xastore</code>, los inicializadores y el <code>instanceof</code>, de modo que un <em>pattern switch</em> y una llamada varargs <strong>compilan y corren</strong>. Y se cerró el resto del flujo de control: el <strong>ternario</strong> con sus dos ramas <strong>promovidas al tipo del todo</strong> (§15.25), el <strong><code>switch</code></strong> como <code>tableswitch</code> o <code>lookupswitch</code> según la <strong>densidad</strong> de sus etiquetas —con el relleno de alineación a 4 bytes y los desplazamientos de 4, preservando el <em>fall-through</em> de la forma de dos puntos y cortándolo en la de flecha—, los <code>break</code>/<code>continue</code> <strong>con y sin etiqueta</strong> sobre una pila de sentencias «de las que se puede salir» —un <code>switch</code> interpuesto captura un <code>break</code> pero <strong>no</strong> un <code>continue</code> (§14.16)— más el <strong>bloque etiquetado</strong>, y <code>synchronized</code> con <code>monitorexit</code> en <strong>las dos</strong> salidas, la normal y un handler <em>catch-all</em> que re-lanza. De yapa, el <strong><code>switch</code> sobre <code>String</code></strong> pasó a compilar de punta a punta: el desugar ya lo bajaba a dos switches sobre <code>int</code>, pero ninguno de los dos se podía emitir. Cerrar eso destapó el último agujero <strong>silencioso</strong> del emisor, y en la pieza más delicada: la pila de operandos se llevaba como un <strong>simple contador de altura</strong>, pero un frame del <code>StackMapTable</code> tiene que declarar <strong>los tipos</strong> de lo que hay en ella. Mientras todos los destinos de salto caían en bordes de sentencia —con la pila vacía— no se notaba; el ternario los mete en <strong>medio de una expresión</strong>: en <code>new M(a &gt; 0 ? 1 : 2)</code> el <code>new</code> y su <code>dup</code> siguen vivos en la pila, y el frame los declaraba como pila vacía. El <code>.class</code> salía igual —la barrera no ve esto, porque el emisor cree estar emitiendo bien— y lo rechazaba recién el verificador. La pila pasó a llevarse <strong>tipada</strong> (48 puntos de ajuste), lo que de paso hizo representable el tipo <strong><code>uninitialized</code></strong> (tag 8), que identifica un objeto recién creado por el <strong>offset de su <code>new</code></strong> y, corrido su <code>&lt;init&gt;</code>, pasa a definitivo en todas sus apariciones —pila y locales— (§4.10.2.4). <strong>Después</strong> se sumó el <strong><code>invokedynamic</code></strong> (opcode <code>0xba</code>): el pool ganó <code>MethodHandle</code>/<code>MethodType</code>/<code>InvokeDynamic</code> y el atributo <code>BootstrapMethods</code>, y un nodo <code>Indy</code> <strong>genérico</strong> (bootstrap + argumentos estáticos tipados) sirve tanto al <code>LambdaMetafactory</code> de las lambdas/<em>method refs</em> como al <code>ObjectMethods</code> de un <code>record</code> — con el emisor empujando las capturas y el verificador propio validando el call site. Y se cerró el frente de <strong>clases internas, locales y anónimas</strong>: la <strong>captura del entorno</strong> —<code>this$0</code> de la instancia envolvente y <code>val$x</code> de los locales <em>effectively-final</em>, con un pase <code>hoist_anonymous</code> que baja cada <code>new T(){…}</code> a una local sintética <code>Outer$N</code>—, las tres formas <strong>corren y verifican</strong>. Por último, la <strong>emisión de interfaces propias</strong>: el <code>.class</code> de una <code>interface</code> sale con <code>ACC_INTERFACE</code> y sus métodos <strong>abstractos sin <code>Code</code></strong>, y se sumó <strong><code>invokeinterface</code></strong> (con <code>InterfaceMethodref</code>) para las llamadas sobre un receptor de interfaz —lo que desbloqueó las <strong>anónimas sobre una interfaz</strong> (<code>new Runnable(){…}</code>, el caso más común) y las lambdas contra interfaces funcionales propias—.</p>
<p class="exito"><strong>✓ Siguiente:</strong> del lado del compilador, el <strong>frente grande de <code>invokedynamic</code> quedó cerrado</strong> —<strong>lambdas</strong> (con un <code>LambdaToMethod</code> propio), <strong>referencias a método</strong> (las cuatro formas del §15.13.1) y el <code>equals</code>/<code>hashCode</code>/<code>toString</code> de un <strong><code>record</code></strong> (por <code>ObjectMethods</code>) se tipan, emiten y <strong>verifican</strong>; la ejecución del call site vive en la JVM (KajiJDK)—. Se cerraron además las <strong>clases internas, locales y anónimas</strong> —captura del entorno completa (<code>this$0</code> + <code>val$</code>, con un pase <code>hoist_anonymous</code> que baja la anónima a una local sintética <code>Outer$N</code>), las tres <strong>corren y verifican</strong>, incluidas las formas <strong>cualificadas</strong> (<code>Outer.this</code> → cadena de <code>this$0</code>, con desambiguación de campo sombreado y multinivel; y <code>outer.new Inner()</code>, que empuja el calificador como <code>this$0</code>) y los <strong>argumentos de super</strong> (<code>new ArrayList(10){…}</code>), la <strong>colisión de nombres</strong> (dos locales homónimas se renombran a únicas <code>1L</code>/<code>2L</code> con alcance léxico por bloque), la <strong>local-en-local</strong> (anidada como tipo de su local envolvente) y el <strong>multinivel implícito</strong> (acceso a un miembro del abuelo vía <code>this$0</code> encadenado) —las clases internas quedan <strong>sin colas</strong>—; el parser (B1) quedó <strong>sin bordes</strong> (anotaciones de tipo/uso <code>List&lt;@NonNull String&gt;</code>, <code>&lt;@Foo T&gt;</code>, <code>new &lt;T&gt;Foo()</code>, y las de <strong>nivel de array</strong> <code>String @A []</code>); y la <strong>emisión de interfaces propias</strong> cerró el caso más común de anónima (<code>new Runnable(){…}</code>) sumando <code>invokeinterface</code>. Y se cerró el <strong>boxing dirigido por tipo</strong> (<em>TransTypes</em>): una pasada propia inserta <code>Integer.valueOf</code>/<code>x.intValue</code> en cada contexto de conversión (asignación, <code>return</code>, argumentos, operandos, cuerpo de lambda), cerrando el caso insignia de las <em>poly expressions</em> —<code>Function&lt;Integer,Integer&gt; f = x -&gt; x + 1</code> de punta a punta, validado por el verificador (ejecutar <code>valueOf</code> es de la JVM)—. Y se cerró la <strong>inferencia en posición de argumento</strong> (dos fases §15.12.2.6): un argumento <strong>poly</strong> —lambda, <em>method ref</em>, diamante— se re-atribuye con el tipo de su parámetro como <em>target</em> una vez resuelta la sobrecarga, lo que por fin baja una <strong>lambda-argumento</strong> (<code>call(x -&gt; x + 1)</code>) a <code>invokedynamic</code>. Y se cerró el <strong><code>enum</code> con constantes parametrizadas</strong> (<code>ROJO("rojo")</code>): al ctor propio se le antepone <code>(String, int)</code> + <code>super(...)</code> y cada constante se construye con sus argumentos. Y se **liquidaron los sueltos** que quedaban: <code>@Override</code>/<code>throws</code> en el chequeo de override (§8.4.8.3/§9.6.4.4), <code>RuntimeVisibleAnnotations</code> (§4.7.16), el <strong>plegado de constantes</strong> en un <code>case</code> (§15.28), las <strong>formas comprimidas del <code>StackMapTable</code></strong> (§4.7.4), los <strong>inicializadores de campo atribuidos</strong> (§8.3.2), la <strong>desambiguación de sobrecargas por el arg lambda</strong> (§15.12.2.5) y el <strong>boxing <em>wrapper</em>→primitivo más ancho</strong> (<code>long l = anInteger</code>). Y se cerró el primer <strong>frente grande de Java SE 25</strong>: <strong><code>sealed</code>/<code>non-sealed</code> + exhaustividad de <code>switch</code></strong> (§8.1.1.2/§9.1.1.4/§8.1.6/§14.11.1.1) — el parser toma las keywords restringidas y el <code>permits</code>, el grafo gana los <strong>subtipos autorizados</strong> (con el <code>permits</code> implícito de la unidad), <strong>Check</strong> valida las reglas de sellado y exige <strong>exhaustividad</strong> a la <code>switch</code>-expresión y a la sentencia con <em>patterns</em> (jerarquía <code>sealed</code> recursiva, todas las constantes de un <code>enum</code>, <code>true</code>/<code>false</code> de un <code>boolean</code>; una guarda no cubre), y el <code>.class</code> lleva el atributo <strong><code>PermittedSubclasses</code></strong> (§4.7.31) — de yapa se corrigió que <code>case RED -&gt; 1</code> se leía como lambda. Con eso la lista <strong>tracked</strong> del compilador queda vacía. Y se cerró el segundo frente de <strong>Java SE 25</strong>: los <strong>text blocks</strong> (§3.10.6) — el parser decodifica el <code>"""…"""</code> con el algoritmo del JLS (normalización de terminadores, descarte de la línea de apertura, <strong>indentación incidental</strong> mínima + blancos finales, y luego los escapes con <code>\s</code> y la <strong>continuación de línea</strong> <code>\&lt;LF&gt;</code>), dando un <code>String</code> común que se emite como cualquier literal. Y se cerró el tercer frente: la <strong><em>capture conversion</em></strong> (§5.1.10) con <strong>variables de captura frescas</strong> (un <code>RType::Capture{id,upper,lower}</code> con cotas inline e <code>id</code> fresco para la <em>freshness</em>): se aplica en acceso a miembros y en argumentos, el subtipado gana <code>CAP &lt;: upper</code> y <code>s &lt;: CAP</code> por la cota inferior, y el caso insignia <strong><code>swap(List&lt;?&gt;)</code> → <code>swapHelper(List&lt;T&gt;)</code></strong> ahora <strong>infiere</strong> <code>T</code>. Y se cerró el cuarto frente: los <strong><em>bridge methods</em></strong> (§8.4.8.3/§15.12.4.5) — el emisor sintetiza los puentes (<code>ACC_BRIDGE</code>+<code>ACC_SYNTHETIC</code>) que hacen que la sobrescritura funcione en bytecode cuando la <em>erasure</em> difiere, por un <strong>parámetro genérico</strong> borrado (<code>Comparable&lt;Foo&gt;</code> → <code>compareTo(Object)</code>, <code>Node&lt;Integer&gt;.set(Integer)</code> → <code>set(Object)</code>) o por un <strong>retorno covariante</strong> (<code>A.get():Object</code> / <code>B.get():String</code>), reenviando al método real con <code>checkcast</code> + <code>invokevirtual</code>; verifica y <strong>despacha</strong> de punta a punta. Y se cerró el último frente grande: los <strong>módulos</strong> (§7.7/§4.7.25) — el parser toma el <code>module-info.java</code> con las cinco directivas (keywords restringidas), el emisor produce un <code>module-info.class</code> fiel (<code>ACC_MODULE</code>, atributo <strong><code>Module</code></strong> con <code>CONSTANT_Module</code>/<code>Package</code> y el <code>requires java.base</code> mandated), y <strong>Check</strong> valida la buena formación (directivas duplicadas, auto-<code>requires</code>); la resolución del grafo es de runtime (JPMS, track aparte). <strong>Con esto, los cinco frentes grandes de Java SE 25 quedan cerrados del lado del compilador</strong> —<em>sealed</em> + exhaustividad, text blocks, captura de wildcards, <em>bridge methods</em> y módulos (B5)—. Lo que le queda al compilador **no** son features de lenguaje sino **fidelidad** (B6). Ya se cerraron los cuatro primeros atributos: <strong>✅ <code>Signature</code></strong> (§4.7.9) —los genéricos ya **no se pierden** en el binario—, <strong>✅ <code>InnerClasses</code></strong> (§4.7.6), <strong>✅ <code>EnclosingMethod</code></strong> (§4.7.7) —anidamiento y método envolvente de local/anónimas— <strong>✅ <code>Record</code></strong> (§4.7.30) —un <code>record</code> ahora extiende <code>java.lang.Record</code>, es <code>final</code> y lleva el atributo con sus componentes, así <code>isRecord()</code>/<code>getRecordComponents()</code> andan (y se cerraron dos bugs latentes: el parseo de un record genérico y la erasure del descriptor de una variable de tipo)— y <strong>✅ <code>NestHost</code>/<code>NestMembers</code></strong> (§4.7.28/§4.7.29) —el <em>nest</em> plano (host + nestmates transitivos) que da acceso privado entre anidadas—. <strong>✅ <code>NestHost</code>/<code>NestMembers</code></strong> (el <em>nest</em>) y <strong>✅ <code>MethodParameters</code></strong> (§4.7.24, los nombres de parámetros para la reflexión). De la **inferencia** ya se cerraron el lado escritura de la captura por llamada y el <strong>lub con intersección</strong> (<code>Number &amp; Comparable</code>); queda solo el <em>solver</em> transitivo de §18 (bounds cruzados). Del lado de los <strong>atributos ya no queda fidelidad reflexiva pendiente</strong>: se cerraron <code>Exceptions</code>, <code>LineNumberTable</code>, <code>LocalVariableTable</code>, <code>ConstantValue</code> y <strong>todas</strong> las type annotations (<code>RuntimeVisibleTypeAnnotations</code>, §4.7.20 — parámetros de tipo, cotas, extends/implements, throws, field/return/param y los targets de <code>Code</code>: cast/<code>instanceof</code>/<code>new</code>/local), todo verificado byte-fiel contra el <code>javap</code> de Temurin. Lo que falta para un reemplazo <em>drop-in</em> de <code>javac</code> es la <strong>recuperación de errores</strong> a nivel expresión y el <em>solver</em> transitivo del Cap. 18. En paralelo, lo demás es del <strong>track de la JVM</strong> (ejecución, resolución de módulos, concurrencia). Del lado de la JVM, seguir la concurrencia hacia el <strong>multithread profesional</strong> (ver «Concurrencia»): cerrar el monitor y dar el salto de substrato — OS threads + GIL (park/unpark real) → sacar el GIL → modelo de memoria (<code>volatile</code>/fences) → <code>java.util.concurrent</code>. El paralelismo de cómputo del LLM vive en <strong>kernels off-heap</strong> (rayon/CUDA).</p>
</div>

</body>
</html>`;

const outHtml = require("path").join(__dirname, "Roadmap_JDK.html");
fs.writeFileSync(outHtml, html, "utf8");
console.log("HTML OK ->", outHtml, "(" + html.length + " bytes)");
