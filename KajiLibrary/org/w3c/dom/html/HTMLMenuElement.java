package org.w3c.dom.html;

/**
 * A `<menu>`. Obsolete in HTML 4.
 */
public interface HTMLMenuElement extends HTMLElement {

    /** The `compact` attribute. */
    boolean getCompact();

    /** It sets the `compact` attribute. */
    void setCompact(boolean compact);
}
