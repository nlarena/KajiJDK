package jakarta.validation;

// KajiLibrary's jakarta.validation.ConstraintValidatorContext — the context in which a constraint is
// validated, used to build custom violation messages. A KajiLibrary subset (the message-building
// nested types are filled in with the core API).
public interface ConstraintValidatorContext {
}
