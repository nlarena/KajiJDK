package jakarta.validation;

// KajiLibrary's jakarta.validation.Payload — a marker interface for attaching custom metadata objects
// to a constraint (assignable to a constraint's payload() element).
public interface Payload {
}
