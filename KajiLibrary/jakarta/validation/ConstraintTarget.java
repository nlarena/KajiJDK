package jakarta.validation;

// KajiLibrary's jakarta.validation.ConstraintTarget — which element a constraint on a method applies to.
public enum ConstraintTarget {

    IMPLICIT,
    RETURN_VALUE,
    PARAMETERS;
}
