package jakarta.validation;

// KajiLibrary's jakarta.validation.ElementKind — the kind of element a Path node represents.
public enum ElementKind {

    BEAN,
    PROPERTY,
    METHOD,
    CONSTRUCTOR,
    PARAMETER,
    CROSS_PARAMETER,
    RETURN_VALUE,
    CONTAINER_ELEMENT;
}
